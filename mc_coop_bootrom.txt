# LT300 boot ROM - Monte Carlo cooperative+adversarial confirmed annotations
# 3 cycles x 40 samples; generator agents -> adversarial critic agents; only 'confirmed' kept.
# 115 confirmed unique annotations.

0x006010 [video/M76V020] (confirmed) PUSHA at the entry of a video/blanking service routine: it saves all general registers, then sets DS=0x40 (kernel data segment) and reads the M76V020 control shadow [0x7b9], clears bit 0x40 (and al,0xbf) and writes it to video control port 0x343 to update display mode, then loads the active screen-state word [0x7ba] into [0x978].  // PUSHA (60) at routine start; following 'mov ax,0x40'/'mov ds,ax', 'mov al,[0x7b9]'/'mov dx,0x343'/'out dx,al'
0x0060e4 [INT21-DOS] (confirmed) Loads DX with the offset of a '$'-terminated label string (0x119) for the immediately following INT 21h AH=9 print, part of a register/stack-frame diagnostic dump printing [bp+0],[bp-0x12],[bp-0x14],SS,[bp+6] each via hex-print helper call 0x6139.  // mov dx,0x119 ; mov ah,0x9 ; int 0x21; preceded by call 0x6139 and mov dx,0x111 sequence
0x0060f0 [INT21-DOS] (confirmed) Loads DX=0x121 with the offset of a '$'-terminated label string for the immediately following INT 21h AH=09h print, in the register/stack-frame diagnostic dump that prints SS via hex-print helper 0x6139 then this label (same dump as accepted 0x60e4).  // mov ax,ss / call 0x6139 / mov dx,0x121 / mov ah,0x9 / int 0x21
0x00617e [string/data] (confirmed) Data misdecoded as code: this 0xFA ('cli') sits inside a little-endian word/offset table in the early firmware data region; it is bracketed by bogus add/sbb decodes, a retf (0x616C), and 0x01CB filler words (0x618D-0x6195), confirming it is table/pointer data rather than an executed CLI.  // FA between push es and 'add sp,sp / add ax,0x4fa', followed by retf and repeated 01CB filler
0x0061ed [INT21-DOS] (confirmed) Bounds-checks the capture/log buffer write index DI=[0x1ce] against limit 0x173 in a char-output routine: if below limit it appends AL to [di] and bumps the index, before also echoing the char to VRAM segment 0xC000 via the [0x1d0] 16-byte wrap pointer.  // mov di,[0x1ce] / cmp di,0x173 / jnc 0x61fa / mov [di],al / mov di,0xc000 / mov es,di / stosb
0x00638b [INT21-DOS] (confirmed) Restores SI (saved at 0x6381) after the call to the [0x874]-flag subroutine at 0x63a7, in a character-fetch loop that reads bytes via call 0x63b5 and skips LF(0x0A)/CR(0x0D) before reprocessing.  // 0x638B 5E 'pop si' matches push si at 0x6381; followed by call 0x63b5 then cmp al,0x0a/0x0d
0x0063c5 [INT21-DOS] (confirmed) Loads the file handle from [0x132] into BX in preparation for the INT 21h AH=3Fh (DOS read) on the next lines; reads up to 0x80 (128) bytes from the open config/script file into buffer at SI=0x9CA. Part of the line-reader/getchar refill routine for a config or download file.  // 8B1E3201 mov bx,[0x132] then B8003F mov ax,0x3f00 (AH=3Fh read) then CD21 int 0x21, cx=0x80 buffer
0x00644a [INT21-DOS] (confirmed) Sets AH=2 (INT 21h DOS char-output function) before the INT 21h on the next line, to print the second BCD digit of a two-digit field. The preceding XCHG DL,DH swapped the two packed digits so the high digit is output first then this low digit; part of a time/clock formatting routine (prints "HH" then ':' (0x3A) then space).  // B402 mov ah,0x2 followed by CD21 int 0x21; preceded by 86D6 xchg dl,dh and B23A mov dl,0x3a (':')
0x006492 [INT21-DOS] (confirmed) Loads AH=9 for the INT 21h print-$-terminated-string call at 0x6494, after DX was selected among message offsets 0x229/0x237/0x21b based on a status byte [bx+0xa7] (sign/==2 tests) - prints a self-test status/result message.  // prior mov dx,0x229/0x237/0x21b chosen by test al,al(js)/cmp al,2(jz); 0x6492 B409 'mov ah,0x9' then int 0x21
0x0064ec [printer] (confirmed) String-output loop to the parallel printer: after LODSB fetches the next byte it is compared to '$' (0x24) as the terminator; non-'$' chars are sent via INT 17h AH=0 (print character, DX=0) at 0x64f4 after first polling printer status (INT 17h AH=2) at 0x64dc. Part of the MFGTEST printer-port string output.  // lodsb then cmp al,0x24 ('$'), with int 0x17 (printer) calls and xor ah,ah/xor dx,dx surrounding
0x0065cf [INT21-DOS] (confirmed) Sets DOS print-$-string function (AH=0x9) for the immediately following INT 21h that prints the message at DX=0x511, in the self-test that just programmed video chip-select port 0xff8c with 0xd000+[0x525]; labels a test/status message.  // mov dx,0x511 / >>mov ah,0x9 / int 0x21 ; preceding out 0xff8c (mov ax,0xd000 / add al,[0x525])
0x006658 [video] (confirmed) Backspace/destructive-erase via INT 10h teletype (AH=0Eh): emits BEL-handling then BS(0x08)/space/BS sequence. This first 'mov ah,0x0e' sets the teletype output function before 'int 0x10', writing the just-tested character or erasing it on the terminal's local screen.  // 'mov ah,0xe / int 0x10' then 'mov al,0x20 ... int 0x10 / mov al,0x8 ... int 0x10' (space+backspace erase); follows cmp ah,0x23
0x00682c [selftest] (confirmed) Zeroes the 16-bit counter/state word [0x76f] before the self-test reads the video status port (in al,0x340 just above) and prints message 0x56a, then walks the 8-byte flash/memory block descriptors at [0x8b1]. Initializes a byte-count or error accumulator for the MFGTEST memory/flash sizing routine.  // C7066F070000 mov word [0x76f],0x0 immediately before mov dx,0x56a / AH=9 INT 21h and the [0x8b1] descriptor loop
0x006854 [INT21-DOS] (confirmed) Sets CX=DI (the remaining bytes to next 0x1000/4K paragraph boundary, computed as 0x1000-(dx&0xFFF)) as the byte count for the block-clear/copy call to 0x6949; part of the allocator/initializer loop walking 8-byte descriptors from [bx] up to 0x8F1, filling memory in 4K-aligned chunks.  // 8BCF mov cx,di after sub di,si / cmp cx,di; E8F000 call 0x6949; loop bx+=8 until 0x8f1
0x006b18 [INT21-DOS] (confirmed) Loads DX=0x4CC, the offset of a '$'-terminated status/result message, for the immediately following INT 21h AH=09h print on the self-test success path (after printing the value [0x761] via hex helper 0x6C0E at 0x6B15); the routine then halts in an infinite 'jmp short 0x6b1f' loop, the MFGTEST pass/report endpoint.  // mov dx,0x4cc; mov ah,0x9; int 0x21; then EBFE (jmp $) self-loop; preceded by call 0x6c0e hex-print
0x006b3e [video/M76V020] (confirmed) Sets bit 1 (or al,0x2) of the M76V020 mode/control byte being assembled from DX's 0x2000 flag, before merging with the RAM shadow [0x7B9] and writing to video control port 0x343; builds the column/mode control word for the graphics gate array.  // 0C02 or al,0x2 after test dx,0x2000; then mov dx,0x343 / and ah,[0x7b9] / out dx,al / mov [0x7b9],al
0x006B52 [video/M76V020] (confirmed) Begins computing the VRAM segment for a display fill: copies DX (paragraph base, just popped) into AX, masks to within a 64K window (and ax,0xfff) and adds 0xC000 (the VRAM segment, GCS2 region ~0xC0000), then mov ds,ax so DS points at video RAM before the block operation via call 0xa564. The OUT to 0x343 just above set the M76V020 mode/control byte (shadow [0x7b9]).  // 'mov ax,dx' / 'and ax,0xfff' / 'add ax,0xc000' / 'mov ds,ax'; 0xc000 = VRAM segment, port 0x343 video control above
0x006baf [INT21-DOS] (confirmed) Moves the just-opened file handle (returned in AX by INT 21h AH=3Dh open at 0x6ba0, also saved to [0x79f]) into BX in preparation for the INT 21h AX=4202h (LSEEK from end, CX:DX=0) on the next lines to determine the file's size.  // mov [0x79f],ax / mov bx,ax / mov ax,0x4202 / int 0x21
0x006c89 [INT40-comm] (confirmed) After INT 40h AH=4 (kernel get-pointer service), tests whether the returned far pointer ES:BX is null by OR-ing the segment (AX=ES) with offset BX; if zero (jnz fails) the routine returns without processing.  // 0x6C87 int 0x40 (AH=4) then 0x6C89 8CC0 'mov ax,es' / or ax,bx / jnz 0x6c90 / ret
0x006d39 [string/data] (confirmed) This byte (0x0E) is part of the INT 4Fh character/control dispatch table at file 0x6D2C, not code. It is the low byte of the repeated default-handler word offset 0x0E07; the dispatch is performed by 'jmp [cs:bx+0xd2c]' at 0x6E02. ndisasm misdecodes table entries 0x0E,0x07 as 'push cs / pop es'.  // ROM dump at 0x6D2C shows repeated '0E 07' word entries; 'jmp [cs:bx+0xd2c]' at 0x6E02 indexes this table
0x006d4c [string/data] (confirmed) Data misdecoded as code: this 0x07 byte is part of a jump/dispatch table (run of 0E 07 = push cs/pop es pairs interleaved with table words). It is one entry of the INT 4Fh/escape-handler vector table that the routine at 0x6DF4 indexes via JMP [cs:bx+0xd2c]. Not an executed instruction.  // long uniform run of 0E/07 byte pairs at 0x6D31-0x6D58, indexed by jmp [cs:bx+0xd2c] at 0x6E02
0x006d55 [string/data] (confirmed) Data, not code: this 0x0E byte is one half of the repeated default-handler word 0x0E07 filling the INT 4Fh character/control dispatch table at file 0x6D2C; ndisasm misdecodes the 0x0E/0x07 table words as 'push cs / pop es'. The dispatch is done via 'jmp [cs:bx+0x6d2c]' at 0x6E02.  // long run of alternating 0E (push cs) / 07 (pop es) = word 0x0E07 default entries
0x006d7e [string/data] (confirmed) Data, not code: low byte (0x07) of a word entry in the INT 4Fh dispatch table at 0x6D2C. The surrounding 0x0E/0x07 pairs are the default handler offset 0x0E07 misdecoded as 'pop es'.  // ROM 0x6D7C: '07 0E 07 0E B2 0E ...' little-endian word offsets into INT4F handler table
0x006df0 [string/data] (confirmed) Data byte (0x6C) misdecoded as 'insb'; it sits inside a run of 0x0E/0x07 filler/table bytes that is jump-table or padding data preceding the INT 4Fh-related handler at 0x6df5, not a real I/O-string instruction.  // single 0x6C amid long '0E 07' (push cs/pop es) run, no I/O port setup; not reached by real control flow
0x006df5 [INT40-comm] (confirmed) Saves BX at entry of a function-dispatch handler that builds a jump-table index from AH (bl=ah, bh=0, bx*=2), range-checks <0xC6, and does jmp [cs:bx+0xd2c]; this is the prologue PUSH BX before BX is clobbered as the index.  // 0x6DF5 53 'push bx' followed by mov bl,ah / xor bh,bh / add bx,bx / cmp bx,0xc6 / jmp [cs:bx+0xd2c]; later pop bx at 0x6E0F
0x006E18 [INT21-DOS] (confirmed) JZ tests the byte just fetched by LODSB against '$' (0x24): this is the terminator check of a $-terminated string print loop that emits each character via INT 10h AH=0Eh teletype (the AH=9-equivalent string printer). On match it branches to the exit (pop si/bx/ax; iret).  // preceding 'lodsb' / 'cmp al,0x24'; jz to 0x6e20 which pops and irets; the fall-through does 'mov ah,0x0e'/'int 0x10'
0x006e7d [INT40-comm] (confirmed) ISR epilogue: restores AX (POP AX) before IRET, completing the register teardown of an interrupt/kernel handler that loaded DS=0x0400, fetched [0x914] then [bx+0x4] (a pointer field of the active-page control block).  // pop ds; add sp,2; >>>>pop ax; iret directly after reading [0x914]->[bx+0x4]
0x006E83 [INT40-comm] (confirmed) MOV DS,AX (AX=0x40) sets DS to the kernel data segment (physical 0x400) at the start of a control-block lookup: the routine then walks page/session control blocks on a 0xB8-byte stride from 0x18DE up to 0x245E, comparing the search key AX against each block's [bx+0x4] field (active flag at [bx+0xa7]) to locate the matching control block, returning STC on no match.  // 'mov ax,0x40'/'mov ds,ax'; following 'mov bx,0x18de'/'cmp byte [bx+0xa7],0x0'/'cmp ax,[bx+0x4]'/'add bx,0xb8'/'cmp bx,0x245e'
0x006f0d [INT40-comm] (confirmed) ISR/handler epilogue: restores the caller's DS (saved on entry) prior to the IRET at 0x6F10, after the routine has converted a binary value to a scaled/decimal result (mul 100, div 0x37). Part of register teardown POP DS/BX/AX then IRET.  // 0x6F0D 1F 'pop ds' immediately followed by pop bx (5B), pop ax (58), iret (CF) at 0x6F10
0x006F7B [selftest] (confirmed) Sets DX=0 then ES=0 to begin a memory-presence/sizing scan at segment 0: it writes a test byte (stosb) and runs 'repe scasb' over 0xFFFF bytes looking for the written pattern, walking 0x1000-paragraph segments. Part of the RAM/flash sizing self-test (the preceding code wrote the complemented pattern and toggled video control bit at port 0x343).  // 'mov dx,0x0' then 'mov es,dx' / 'xor di,di' / 'stosb' / 'mov cx,0xffff' / 'repe scasb'
0x007071 [timer/ICU] (confirmed) STOSW stores the handler offset word 0x11DB into the IVT during interrupt-vector installation; DI=0xBC selects IVT entry 0x2F (0xBC/4). This is one of an unrolled series of MOV DI,off / MOV AX,offset / STOSW writes that pre-load the offset halves of several interrupt vectors (0x2F, then 0x5C at 0x170, 0x2A at 0xA8, 0x40 at 0x100, 0x21 at 0x84) before their CS segment words are written.  // preceding 'mov di,0xbc' / 'mov ax,0x11db'; STOSW (AB) to ES:DI with ES=0 (IVT); 0xBC/4 = INT 0x2F
0x007119 [INT21-DOS] (confirmed) Begins printing a fixed status/banner message via INT 21h AH=09h: 'push cs / mov dx,0x14f8 / pop ds' sets DS:DX to the CS-relative $-terminated string at 0x14F8, then AH=9/INT 21h prints it (reached when the prior optional message at [0x986] was not shown).  // 'push cs / mov dx,0x14f8 / pop ds / mov ah,0x9 / int 0x21'; jump target of 'jnz 0x7119'
0x007135 [selftest] (confirmed) Loads CX=0x8008 as the 4th test data pattern for the call to 0x714E, the M76V020 video gate-array register test loop (writes patterns to I/O 0x342, reads status from 0x340, accumulates errors into [0x7f0]/[0x7f1]/[0x7f2]); this is the last of four pattern calls (0x240,0x880,0x2020,0x8008) in the MFGTEST video-register verification.  // mov cx,0x8008 immediately followed by 'call 0x714e'; callee drives DX=0x342 OUT / DX=0x340 IN test loop
0x007331 [INT40-comm] (confirmed) Bounds/threshold compare in a table walk: CX holds an entry's field [bx+0x1e] multiplied by 4 (two add cx,cx), compared against limit DX; jc 0x734e branches when below threshold. Selects/validates a table entry.  // 0x7327 mov cl,[bx+0x1e]; 0x732D/0x732F add cx,cx (x4); 0x7331 3BCA 'cmp cx,dx' then jc 0x734e
0x007481 [INT40-comm] (confirmed) Reads byte at offset 0x15 of the loaded module/exec structure (ES=[0x7d5] = the loaded executable's segment, SI=[es:0] = first record) and stores it to terminal state byte [0x7af]; extracts a config/mode field from the downloaded module header into the kernel's working flags.  // mov es,[0x7d5]; mov si,[es:si]; >>>> mov al,[es:si+0x15]; mov [0x7af],al ([0x7d5] is the loaded-segment pointer set at 0x7227)
0x0074c0 [INT40-comm] (confirmed) Pointer-advance (INC SI twice = +2 bytes per word entry) inside a 6-entry word-table linear search: scans the 6-word table at 0x7ba for AX (==0) and on no-match falls through to STC/RET (not found). The matched-entry index is stored at [0x7c6] on success. Part of a config/mode table lookup.  // mov si,0x7ba; mov cx,6; cmp ax,[si]; jz 0x74c6; inc si/inc si; loop 0x74bc; stc/ret; mov [0x7c6],si
0x00756e [INT40-comm] (confirmed) Ring-buffer empty test in an INT 40h AH=0 (serial/keyboard get-char) handler: after loading head pointer SI=[bx+0x90] and comparing to tail [bx+0x8e] of the input queue (control block at DS:[0x914]=segment 0x40), JZ skips the 'mov ax,[si]' peek when head==tail (buffer empty).  // mov si,[bx+0x90] / cmp si,[bx+0x8e] / jz 0x7572 / mov ax,[si]
0x0075f0 [video/M76V020] (confirmed) REP MOVSW copying 0xCE (206) words of font/screen-init data from source 0x2094 into display row buffer at DI=0x124a; one of four consecutive rep-movsw blocks filling row buffers 0xe4a/0x104a/0x124a/0x144a during display initialization (matches accepted 0x75f0).  // mov si,0x2094 / mov di,0x124a / mov cx,0xce / rep movsw
0x00763e [INT4F-dispatch] (confirmed) Tests the byte just fetched by LODSB (AL) for the sign bit in the escape/control-code dispatch table walk: JS at 0x7640 branches when the high bit (end-of-string/terminator marker) is set, otherwise the byte is compared to AH and recursively processed via call 0x7629/0x760d. Part of the EMULATOR escape-sequence matcher.  // lodsb; test al,al; js 0x7652; cmp al,ah; call 0x7629 (self) / call 0x760d; table base si=0x2230
0x00767f [video/M76V020] (confirmed) Advances destination pointer DI by 510 (0x1FE) bytes between single-word (movsw) copies, striding across display/buffer rows while building a table at 0xa4a, matching the row-stride layout of the terminal's screen buffers.  // add di,0x1fe interleaved with repeated 'movsw' starting at di=0xa4a
0x00770c [INT4F-dispatch] (confirmed) Branch in a control-code action dispatcher that clears mode/attribute flag bits in the terminal state byte [0x7f6]: JMP short skips one AND-mask case (0x7f6 &= 0xFB) to reach the test of [0x7f6]&0x05 at 0x7713 that decides whether to invoke the screen/mode-update routine call 0x7935. Implements terminal mode-reset (e.g. ANSI/escape RM) handling.  // and byte [0x7f6],0xfe; jmp 0x7713; test byte [0x7f6],5; jnz; and byte [0x7f5],0xfb; call 0x7935 (mode update); chain of and [0x7f6],mask cases
0x007859 [INT40-comm] (confirmed) Loads CX=0x3E8 (1000) as a count/timeout argument for the immediately following INT 40h call (AH=2 set at 0x7857, DX=0x7D subfunction); invokes a kernel comm/system service in the alternate branch of the input-queue handler (taken when head!=tail comparison at 0x783F-0x7843 differs), companion to the AH=2/DX=0 INT 40h at 0x7850.  // B9E803 mov cx,0x3e8 between 'mov ah,0x2' and 'mov dx,0x7d / int 0x40 / ret'
0x0078c3 [video/M76V020] (confirmed) Reads a 16-bit value from the selected display row-buffer: BX was loaded with one of the row-buffer bases (0xa4a/0xe4a/0x124a/0x144a) chosen by AL bits, then BX += CH*2 as a word index; 'mov dx,[bx]' fetches that row/cell word and returns it in DX (companion to the 0xe4a/0x104a/0x124a/0x144a row buffers filled at 0x75xx).  // preceding mov bx,0x{a4a,e4a,124a,144a}; mov al,ch; xor ah,ah; add ax,ax; add bx,ax then mov dx,[bx]; ret
0x0079cd [keyboard] (corrected) INT character-available query: loads kernel ptr [0x914] into BX, checks pending flag [bx+0x98] and head [bx+0x90] vs tail [bx+0x8e]; returns AL=0x00 when buffer is empty (head==tail and flag clear) and AL=0xFF when a character is available. The AL=0xFF meaning in the claim is backwards.  // mov bx,[0x914] after push ds/mov bx,0x40/mov ds,bx; then cmp byte [bx+0x98],0 and cmp [bx+0x90],[bx+0x8e]; mov al,0xff/iret on empty
0x007a06 [INT16-keyboard] (confirmed) ISR epilogue: restores the caller's BX (saved on entry) before 'add sp,2' (discards saved AX/flags slot) and IRET. Tail of a keyboard/comm interrupt handler that had set DS=0x0400, tested the per-page flag [bx+0x98] via [0x914], and cleared it.  // pop ds; >>>>pop bx; add sp,byte +0x2; iret following the [bx+0x98] flag clear and the INT 16h path at 0x7a0e
0x00996d [INT21-DOS] (confirmed) First SHR DX,1 of a 4-bit logical right shift of the 32-bit value (size+0xF) in DX:AX, computing a paragraph (16-byte) count via four SHR DX/RCR AX pairs; part of the INT 21h memory allocator sizing a block before adding base [0x820].  // D1EA shr dx,1 begins sequence of D1EA/D1D8 pairs after add ax,0xf / adc dx,0; then add ax,[0x820]
0x009971 [INT21-DOS/memory-alloc] (confirmed) One step of a 32-bit shift-right-by-4 (>>4) to convert a byte length (DX:AX, already +0xF) into a paragraph (16-byte) count, via repeated 'shr dx,1 / rcr ax,1'; feeds the heap allocator's size-in-paragraphs (later added to [0x820]).  // add ax,0xf / adc dx,0 / shr dx,1 / rcr ax,1 (x4) / add ax,[0x820]
0x0099b8 [INT21-DOS] (confirmed) Overflow guard in a heap/arena size-clamp: after add bx,cx (block-end = base+available, where cx=[0x822]-[0x820] is the free-region size), JC branches to 0x99c0 on 16-bit carry/overflow to clamp the requested allocation size AX to the available BX. Part of the INT 21h AH=48-style memory allocator computing the granted block size into [0x80e].  // mov cx,[0x822]; sub cx,[0x820]; add bx,cx; jc 0x99c0; cmp ax,bx; jna; mov ax,bx; mov [0x80e],ax
0x009b1d [INT40-comm] (confirmed) Function epilogue: POP SI restores caller's SI as part of restoring DI/SI/DX/CX/BX before RETF 2; this is the far-call return sequence of a kernel API handler (a 9Axxxx:F600 callee) that returns status in carry (the stc/clc at 0x9B15/9B18).  // 5E pop si in the run 5F/5E/5A/59/5B followed by CA0200 retf 0x2; carry set/clear at 9B15/9B18
0x009ba7 [INT40-comm] (corrected) Sets DS=0x0040 (segment 0x40, physical 0x400 — the kernel data segment), so mov [0x874],al stores AL to physical 0xCB4 (0040:0874); records a kernel flag in an INT handler after AH=48h alloc, then pop ds/pop bx/iret. Flag is read at 0x63A7, not 0x638b.  // mov bx,0x40; mov ds,bx; mov [0x874],al; pop ds; pop bx; iret — [0x874] is the kernel flag used elsewhere (e.g. 0x638b)
0x009c92 [INT40-comm] (confirmed) Far-return-with-success epilogue of an INT 40h/kernel block-handler: after popping di/si/dx/cx/ds/es/bx and 'add sp,2' (discarding the saved flags slot) the preceding CLC sets carry=0 (success) and 'retf 2' returns to the INT caller while discarding 2 stack bytes. Marks the OK exit of the memory/control-block routine whose next entry begins at 0x9c95.  // opcode CA0200 (retf 2) preceded by F8 (clc) and the di/si/dx/cx/ds/es/bx pop chain + 'add sp,2'
0x009d5c [INT21-DOS/memory-alloc] (confirmed) Decrements AX to the previous paragraph after call 0xa961 returns a segment, so DS points at the block header that is then checked for signature 'CL' (0x434c, CONFIG.BLK/heap block magic).  // dec ax ; mov ds,ax ; cmp word [si],0x434c ('CL')
0x009dfb [INT40-comm] (confirmed) Zeroes the word at [si] (the link/forward-pointer or count field at offset 0 of a memory-block/list node) while freeing/resetting the node; part of a linked-list memory manager release routine that also clears the offset-8 word and the offset-0xa flag byte before popping registers and RETF.  // C7040000 mov word [si],0x0 adjacent to C744080000 mov word [si+0x8],0x0 and C6440A00 mov byte [si+0xa],0x0
0x009e3f [INT40-comm] (confirmed) Computes the size/end of a memory block by decrementing BX (BX = base+DI then -1 gives the last/end paragraph), used to initialize the second node of a two-node free-list split; the value is then stored into the node's offset-2 link field [es:si+2] and used as a new segment (mov es,bx).  // 83EB01 sub bx,0x1 right after 03DF add bx,di; result written via 26895C02 mov [es:si+0x2],bx and 8EC3 mov es,bx
0x009ec5 [INT40-comm] (confirmed) Linked-list traversal of kernel control blocks in segment [0x876]: if the in-use flag [es:si+0xa]==1 and the key BX==[es:si+8] do not BOTH match, skip the accumulate of size field [es:si+6] into AX; this branch is the 'key mismatch' skip.  // 0x9EBA cmp byte [es:si+0xa],1 / 0x9EC1 cmp bx,[es:si+8] then 0x9EC5 7504 'jnz 0x9ecb' bypassing add ax,[es:si+6]
0x009fb8 [printer] (confirmed) Sub-function dispatch in an INT 17h/printer-or-video helper (DS set to kernel seg 0x40): after 'cmp ah,0x1', this 'jz 0xa001' takes the AH==1 path; the AH==0 fall-through reads video status port 0x340 (test bit 0x80 busy) and writes the char to port 0x342. Routes the printer/char-output function selector.  // cmp ah,0x1 / ja 0x9ffb / >>jz 0xa001; later in al,0x340 / test al,0x80 / out 0x342
0x00a02b [INT40-comm] (confirmed) Tests bit 4 (0x10) of AL returned by the kernel call to F600:4B49; AL holds device/status flags. Bit 7 (0x80) was tested just before; this checks the 0x10 status bit and branches to set bh|=0x10 (vs bh|=0x28). Part of decoding a serial/device status byte into terminal attribute/state bits in BH.  // A810 test al,0x10 after 9A494B00F6 call 0xf600:0x4b49 and A880 test al,0x80; branches build bh with or bh,0x10/0x28/0x80
0x00a1f7 [INT21-DOS] (confirmed) Return from the filename/field space-padding routine; sets DS=0x40 (kernel data segment 0x0400) just before RET so the caller resumes with the kernel data segment after the rep-stosb space fill and trailing '.' handling.  // 0x0A1F2 mov ax,0x40 / mov ds,ax then 0x0A1F7 C3 'ret'; preceding rep stosb of 0x20 (space)
0x00a394 [INT4F-dispatch] (confirmed) Sets the return code DX=0x2 in a range/table dispatcher (after 'cmp bx,0x164a'/'cmp bx,0x18ca' classifies the index BX); falls into the shared exit 'pop ax / clc / retf 2' returning success with DX=0x02 as the selected handler/parameter value, parallel to the DX=0x80 case at 0xA3A1.  // BA0200 mov dx,0x2 after cmp bx,0x18ca/jnc, before pop ax/clc/retf 2; sibling mov dx,0x80 at 0xA3A1
0x00a495 [INT21-DOS] (confirmed) Memory-allocation/free-list walk (called from heap manager): after pushing the current block pointer SI, call 0xA581 tests/processes a heap block descriptor; JNZ back to 0xA490 on mismatch, else advance SI by 8 (descriptor size) and loop until si reaches 0x8F1. Part of the INT 21h AH=48/4A-style allocator.  // 'call 0xa581 / jnz / add si,byte +0x8 / cmp si,0x8f1 / jc' descriptor-table walk; results stored to [0x18d7]/[0x18d9]
0x00A50E [INT21-DOS/memory-alloc] (confirmed) XOR DI,DI zeroes the destination offset before the heap block-descriptor copy/process: ES was just set to the block segment ([0x18db]+[si]) and the routine then sets up DS:SI from [bp-8] and calls 0xa564 with CX=[bp-0xc] (the 0x12-stride descriptor processor). Part of the INT 21h AH=4A-style memory-block resize/merge in the CONFIG.BLK heap.  // 'xor di,di' after 'mov es,ax' (ax=[0x18db]+[si]); followed by 'lds si,[bp-0x8]' and 'call 0xa564' with cx=[bp-0xc]
0x00a568 [INT21-DOS/memory-alloc] (confirmed) Emits marker/length byte 0x40 (via the following stosb) into the heap-block descriptor name/string buffer being built; this is one case of a small switch (sibling cases write 0x50, 0x3, 0x4) feeding a lodsb/scan-for-high-bit (test al,0x80) terminator loop that finalizes the descriptor entry.  // mov al,0x50/stosb (prev) ; >>mov al,0x40 / stosb / lodsb / dec di / test al,0x80 / loop 0xa568
0x00a626 [INT21-DOS/memory-alloc] (confirmed) Part of a 32-bit logical >>4 (divide-by-16) converting a byte length in CX:AX (already rounded up by +0xF at 0xA620/0xA623) into a paragraph count: 'ror cx,4' rotates the high word, then 'shr ax,4' and 'or ax,cx' merge to form the paragraph size; feeds the heap/free-list block sizing that updates the descriptor's available field [es:bx+6]/[es:bx+4].  // C1C904 ror cx,4 following add ax,0xf / adc cx,0; then shr ax,4 / or ax,cx; sub [es:bx+0x6],ax
0x00a62c [INT21-DOS] (confirmed) Combines the high word (CX, ror'd) and low word (AX, shr 4) of a (length+0xF)>>4 computation into a paragraph (16-byte) count, used to bump the block descriptor fields [es:bx+4]/[es:bx+6] in the memory allocator/arena manager.  // 0x0A620 add ax,0xf / adc cx,0 / ror cx,4 / shr ax,4 then 0x0A62C 0BC1 'or ax,cx'; result subtracted/added to [es:bx+6]/[es:bx+4]
0x00a64e [INT21-DOS/memory-alloc] (confirmed) Calls the heap/block descriptor processor (0xa564) with CX=0x12 (18-byte block-descriptor stride) after pushing es/di/ds/si/ax and the running size (cx+=es) — part of the memory-arena walk that strides 18-byte descriptors (same 0x12 stride/0xa564 callee as accepted 0xa697).  // mov cx,0x12 / call 0xa564 / cmp cx,0x800
0x00a68d [INT21-DOS/memory-alloc] (confirmed) Pops saved DI as part of the epilogue of the memory-block scan routine, restoring registers (si,ds,di,es) that were pushed before the block-walk call 0xa564.  // pop di in sequence 'pop si / pop ds / pop di / pop es' after call 0xa564
0x00a697 [INT21-DOS/memory-alloc] (confirmed) Restores the caller's DS segment (from DX) at the exit of a memory-block walk routine that strides through 18-byte (0x12) block descriptors via call 0xa564, before popping BP and returning AL=0 (fail).  // mov ds,dx after loop 'sub si,0x12 / sub di,0x12 / jnc 0xa67f'; followed by pop bp / xor al,al / ret
0x00A82A [INT40-comm] (confirmed) PUSH BX at the entry of a page/context-switch handler that activates a new screen/page control block: it saves the old active-page pointer [0x978] into [0x918], stores the new page AX into [0x978] and [0x914], then loads the saved cursor row/col (ch/cl=[bx+0x9c/0x9d]) and attributes (dh/dl=[bx+0x9e/0x9f]) to restore that page's display state. Immediately follows the prior task's far-return (retf 2 at 0xa827).  // 'push bx' then 'mov bx,[0x978]'/'mov [0x918],bx'/'mov [0x978],ax'/'mov [0x914],ax'; loads [bx+0x9c..0x9f]
0x00a875 [INT40-comm] (confirmed) In an INT handler (DS=0x40): tests the DX parameter for zero; if nonzero and within the control-block range 0x18de..0x245e it stores DX into state word [0x7fe], validating an active screen/page control-block pointer before fetching [0x918]->[bx+0xa9]. The 0x245e upper bound matches the CB-table limit used at 0x6e9a.  // >>test dx,dx / jz 0xa889 / cmp dx,0x18de / jc / cmp dx,0x245e / jnc / mov [0x7fe],dx
0x00aa6c [timer/ICU] (confirmed) Re-entrancy/busy guard at entry of a CLI-protected routine: after disabling interrupts it loads flag byte [0x94f] and 'test al,al; jz' — if the flag is nonzero it re-enables interrupts (STI at 0xaa73) and returns immediately, otherwise proceeds. Reads the lock/active flag for the cursor/scroll state machine that also tracks [0x946]/[0x948].  // cli at 0xaa6b; mov al,[0x94f]; test al,al; jz 0xaa76; sti; ret guard pattern
0x00abf9 [string/data] (confirmed) Data misdecoded as code: this 0x4B byte is part of a little-endian word/offset table (0x4BDD,0x4BDD,0x4BDF,0x4BE4,0x4BF0) sitting between the 'jmp short' dispatch at 0xABEE/0xABF0 and the resumed handler at 0xABFC; ndisasm renders the DD/DF/4B/E4/F0 bytes as bogus fisttp/dec/lock instructions.  // bytes DD4B DD4B DF4B E4 4B F04B form ascending 0x4Bxx words; real code (push dx) resumes at 0xABFC
0x00ac49 [INT40-comm] (confirmed) Pushes CS so the following near 'call 0xab6d' returns into this segment and the handler can exit via 'retf 2': this is an INT 40h sub-handler that first zeroes terminal state byte [0x7a8] (with DS=0x0400) then invokes the shared worker 0xab6d before the far-return-2 epilogue.  // push cs; call 0xab6d; pop ds; pop bx; retf 0x2 (matching the parallel handler at 0xac3b/0xac52)
0x00acbd [timer] (corrected) CLI guards atomic xchg of countdown word [0x964]; the FF30/FF32/FF34/FF36 OUTs reprogram the 80C188EB Interrupt Control Unit (FF36=mask), not the timer count registers.  // cli; xchg ax,[0x964]; test ax,ax; cmp ax,[0x966]; sti; out to dx=0xff32 / dx=0xff34
0x00ae26 [string/data] (confirmed) Data misdecoded as code: bytes in the character-generator font/glyph bitmap data block of upper ROM (the run continues into the labeled font region at 0x0ae4c); 'mov ch,0xb4' / 'mov [bx+0x2021],al' are bitmap bytes, not real instructions.  // outsb/outsw/xor/and noise preceding; B5B4 88872120 B3B2 ...; adjacent labeled font block 0x0ae4c-0x0ae62 'character-generator font / bitmap data'
0x00ae44 [string/data] (confirmed) Data misdecoded as code: this 0x1C76 sits in the character-generator font/bitmap region (the next bytes at 0xae4c are explicitly labeled font/bitmap data, followed by zero padding at 0xae62). The surrounding xchg/db garbage shows ndisasm is decoding glyph bitmap bytes, not 'sbb al,0x76'.  // immediately followed by labeled '0x0ae4c-0x0ae62 character-generator font / bitmap data' and zero padding block
0x00ae9e [string/data] (confirmed) Data misdecoded as code: bytes inside the character-generator font/glyph bitmap region of upper ROM; ndisasm renders the glyph byte stream as a bogus 'wait call 0x5063:0xa599' far call. Not executable code (the immediately following bytes 0x0AE4C are labeled font/bitmap data in the annotated disasm).  // 9B9A99A56350 -> 'wait call 0x5063:0xa599'; adjacent labeled font block at 0x0ae4c-0x0ae62; surrounding popf/pushf/daa noise
0x00AF1D [timer/ICU] (confirmed) STOSW writes CS as the segment half of an interrupt vector during IVT setup (under CLI). DI=0x20 targets IVT entry 0x08 (the 80C188EB timer/ICU interrupt); the offset 0x5207 was stored just before, and this stores AX=CS. Surrounding code likewise installs vector 0x70/INT 0x1C (off 0x5211) and 0x4C/INT 0x13 (off 0x4FEE), then programs PCB regs FF42/FF46.  // 'mov ax,cs' / STOSW (AB) to ES:DI, DI=0x20 (INT 0x08); under CLI; followed by OUT to 0xff42/0xff46
0x00af51 [timer] (confirmed) Computes a timing/blink-rate reload value: AL=5 is multiplied by the cursor/refresh divisor byte [0x7b7] and the 16-bit product's low byte stored at [0x970]; this seeds the cursor-blink or tick counter that the timer ISR decrements (companion to the descending baud/parameter index ladder that follows at 0xaf5d-0xaf8f).  // mov al,0x5 / mul byte [0x7b7] / mov [0x970],al
0x00af8f [SCC/serial] (confirmed) Branch in a descending threshold ladder mapping an index in AL to a code value in AH (table 5,6,7,8,9,0xc,0x10,0x17...); jna to common exit 0xafb1 when AL<=8 selects code AH=7, a baud-rate/parameter index lookup.  // jna 0xafb1 within repeated 'mov ah,N / cmp al,K / jna 0xafb1' pattern
0x00AFC9 [timer] (confirmed) Unconditional JMP to 0xB0CB taken after gating off the baud/tick timer (the just-executed in al,0xff5e / and al,0xfd / out dx,al cleared timer-control bit 1 of PCB timer FF5E under CLI). Transfers to the timer-reload/blink-rate update path once the displayed attribute/refresh state at [0x7f4] was found changed.  // 'jmp 0xb0cb' immediately after OUT to 0xff5e (timer) with and al,0xfd; preceded by cmp [0x7f4] / jz
0x00b011 [timer/ICU] (confirmed) Increments a sub-tick counter byte [0x972] inside the periodic timer ISR, which also acks via OUT 0xff02,0x8000, bumps the 32-bit tick at [0x968]/[0x96a] and a cursor counter [bx+8], then decrements countdown [0x96d].  // inc byte [0x972] preceded by 'out 0xff02' EOI and adc word [0x96a]; followed by 'dec byte [0x96d] / jnz'
0x00b0b1 [timer] (confirmed) In the baud-rate / timer-reload setup path: after computing a shift/divisor count in CL (loop building AH from a [cs:di] subtraction table), call 0xB1D8 computes the timer reload value; the result is later programmed into PCB timer FF5E (next instructions read/modify FF5E to gate the baud/tick timer).  // call 0xb1d8 immediately followed by 'mov dx,0xff5e / cli / in al,dx / and al,0xfd / out dx,al' (PCB timer-control bit clear)
0x00b145 [keyboard] (confirmed) Initializes a single-bit mask AH=0x80 and bit count CL=8 to begin a bit-scan loop (test ah,al / shr ah / loop) that locates the first differing bit after the byte comparison 'xor al,[di-1] / and al,dh' at 0xB13E; part of the keyboard scancode/keycode compare routine (PUSHA-protected, returns via jmp to key-poll at 0xB0C5/0xB0CB).  // mov ah,0x80; mov cl,0x8; test ah,al; shr ah,1; loop 0xb149 forms an MSB-first bit scan over the XOR difference
0x00b1ab [keyboard] (confirmed) POPA restores all general registers (saved by the PUSHA at routine entry) after a CS:XLATB scancode/keycode translation and the call to 0x768C (key-output/enqueue), in a keyboard scancode-to-code conversion routine; it then jmp's back to the key-poll loop at 0xB14D.  // 0x61 popa; preceded by 2ED7 cs xlatb and E8E1C4 call 0x768c; followed by EB9F jmp 0xb14d
0x00b1ba [timer] (confirmed) In the periodic timer/cursor-blink tick handler: after testing AH (the high byte of the sub-tick counter [0x972]) for nonzero, JNZ selects which threshold to compare the current tick AL against — [0x971] vs [0x970] — to decide whether the blink/baud subtick interval has elapsed before re-arming the timer via call 0x768c.  // cmp byte [0x971],0; mov al,[0x972]; test ah,ah; jnz -> cmp al,[0x971] else cmp al,[0x970]; jc 0xb1d5; mov [0x975],al; call 0x768c; jmp 0xb0d2
0x00b2cc [INT40-comm] (confirmed) After issuing the SCC/host-link command via INT 40h AH=10h (DX=0x302 = Z8530 channel-B control port, CX=0x100), this reloads the current screen/page state word from [0x914] and stores it into [0x978], synchronizing the active-display pointer before re-initializing video (INT 10h mode 7, set-cursor at 0x1800).  // preceded by 'mov dx,0x302 / mov ah,0x10 / int 0x40'; then 'mov ax,[0x914] / mov [0x978],ax' and 'mov ax,0x7 / int 0x10'
0x00b334 [video/M76V020] (confirmed) Inside the periodic display/cursor service: tests the per-page/per-line flag byte [bx+0xA4] where BX=[0x978] (the active screen-state pointer synchronized at 0xB2CC); if zero it bumps the blink/cursor counter [0x97A] and calls 0xB363, gating a cursor-blink or refresh action.  // 80BFA40000 cmp byte [bx+0xa4],0 with bx=[0x978]; inc byte [0x97a]; call 0xb363
0x00b44d [video/M76V020] (confirmed) Loads the cursor/line attribute shadow byte [0x992] in the VRAM render routine (ES=0xD000, BX=0xC500 attribute plane): the byte is masked (and al,0xfd) and conditionally ORed with 0x2 from the shifted AH bit, written to [es:bx] and stored back to [0x992] — building the underline/attribute plane byte for the current cell.  // mov bx,0xc500 / >>mov al,[0x992] / and al,0xfd / shr ah,1 / or al,0x2 / mov [es:bx],al / mov [0x992],al
0x00b58a [video] (confirmed) In a screen-cell/glyph render loop targeting VRAM at segment 0xA000: saves DI (the computed VRAM destination offset, base 0xA000 + (cell&3)<<11) before reading the next character via 'es lodsb' and computing its glyph row offset. Part of the M76V020 video character-write routine.  // 'mov di,0xa000 / and ax,0x3 / shl ax,0xb / add di,ax / push di / es lodsb'; writes to VRAM A000:DI
0x00b5c4 [INT4F-dispatch] (confirmed) Bounds/range check in a table-driven control-code or function dispatcher: after doubling BX (add bx,bx) and comparing BX to the per-entry limit word [bx+si+2], JC branches when the index is within range to use the indexed handler offset (added to SI at 0xB5D7).  // cmp bx,[bx+si+0x2] at 0xB5C1 then 'jc 0xb5d5'; 0xB5D7 'add si,[bx+si+0x2]' uses lds si,[cs:0x8] table
0x00b62e [INT40-comm] (confirmed) Interrupt/handler epilogue: 'pop dx' restores DX as part of the register teardown (pop bp/di/si/dx/ds/bx) preceding IRET, after a block-copy loop ('rep movsw' of 8 words, 'loop 0xb60a') that shifts/scrolls control-block or screen-row table entries; the IRET at 0xB631 returns from the ISR.  // 5A pop dx within pop bp/di/si/dx/ds/bx then CF iret; preceded by rep movsw + loop
0x00b714 [video/M76V020] (confirmed) Compares the column-mode config byte [0x989] to 0x50 (80) to choose a horizontal CRTC timing parameter: within a stream of 'mov al,X / call 0xb7fc' register writes to the video gate array, AL stays 0xc7 for 132-col but is replaced with 0x7 when [0x989]==0x50 (80-column), then sent via call 0xb7fc.  // mov al,0xc7 / >>cmp byte [0x989],0x50 / jz 0xb71d / mov al,0x7 / call 0xb7fc
0x00b831 [video/M76V020] (confirmed) Range/character-class check in the VRAM (ES=0xD000) character-write routine: after AL=low7bits-AH, JC branches to 0xB84C when AL<3, selecting the control-vs-printable rendering path (attribute byte build via [0x994]); part of mapping a host char/attribute pair to the dual-plane display.  // 7219 jc 0xb84c after sub al,ah / cmp al,0x3; es=0xD000 set at 0xB819
0x00bad5 [video/M76V020] (confirmed) Sets ES=0xD000 (VRAM segment) at the entry of the character-write/output routine: SI was loaded 0xD000 then copied to ES, and the routine next fetches cursor offset [0x7ba] and attribute/state [0x9ba] before dispatching on the character in AL for control codes BS(0x08)/CR(0x0D)/BEL(0x07)/LF(0x0A) vs printable rendering into the dual-plane display.  // mov si,0xd000 then mov es,si; followed by cmp al,0x8/0xd/0x7/0xa control-char dispatch and [0x7ba] cursor load
0x00bbdf [video/M76V020] (confirmed) Fills a screen-line region of the VRAM character plane with the space character (0x20) via REP STOSB; the companion fill at 0xBBEB zeroes the attribute plane 0x8000 bytes away — clearing a line of the terminal display.  // 0x0BBDD mov al,0x20 then 0x0BBDF F3AA 'rep stosb'; later add di,0x8000 / mov al,0 / rep stosb (attribute plane)
0x00bbf8 [video/M76V020] (confirmed) Sets attribute-plane bit 0x08 for a character cell: ES points at VRAM (0xD000) and BX holds the byteswapped cell offset just stored at [0x98b]; OR sets the attribute flag in the attribute plane.  // or byte [es:bx],0x8 after 'mov [0x98b],bx'; ES loaded 0xd000 nearby; xchg bl,bh byteswap
0x00bc68 [video/M76V020] (confirmed) Restores the control-block pointer BX (saved earlier) after positioning the hardware cursor on the bottom status line via INT 10h AH=2 (DH=0x18 = row 24); the routine then reads the cell offset from [es:bx+0x2], byteswaps it, and renders status fields (via call 0xbcc5) using attributes from [0x7f4]/[0x976]. Part of the status-line/prompt redraw.  // mov ah,2; mov dx,[0x9ba]; mov dh,0x18; int 0x10; then >>>>pop bx; mov ax,[es:bx+0x2]; xchg al,ah
0x00bd06 [timer/ICU] (confirmed) Sets ES=DI(=0) so ES points at the IVT (segment 0); the routine then writes a far interrupt vector (offset 0x5D1F / CS) to IVT entry at offset 0x38 (INT 0Eh) via STOSW pairs, installing an interrupt handler before unmasking via ICU port 0xFF1C.  // 33FF xor di,di / 8EC7 mov es,di / BF3800 mov di,0x38 / AB stosw / BA1CFF out FF1C
0x00c7f5 [string/data] (confirmed) Data misdecoded as code: this 0x0808 ('or [bx+si],cl') is part of a run of 0x08 bytes in the character-generator font/glyph bitmap region of upper ROM (preceded by 0x77DD 'ja' and 0xF8 garbage); the uniform 0x08 byte stream is glyph/attribute bitmap data, not executable instructions.  // long run of 0808 'or [bx+si],cl' with stray F8, preceded by 77DD bytes
0x00dad4 [string/data] (confirmed) Character-generator font/glyph bitmap data misdecoded as code: the bytes 0x80 0x7E 0x22 0x20 are glyph scan-line rows in the upper-ROM font table, rendered by ndisasm as a bogus 'cmp byte [bp+0x22],0x20'. Not executable.  // bytes 80 7E 22 20 within labeled font/bitmap block 0x0d7f5-0x0dab6+
0x00dada [string/data] (confirmed) Character-generator font/glyph bitmap data misdecoded as code: the bytes 0x20 0x22 are glyph scan-line bytes in the upper-ROM font table (0x0d7f5-0x0dab6+ region), rendered by ndisasm as a bogus 'and [bp+si],ah'. Not executable.  // bytes 20 22 amid glyph rows 80 7E 22 20 3C 20; labeled font/bitmap region [R16]
0x00dade [string/data] (confirmed) Character-generator font/glyph bitmap data misdecoded as code: this 0x00 0x00 pair is a blank glyph row in the 0x0d7f5-0x0dab6+ font table region (surrounding 0x40/0x7E/0x82/0x22/0x20 bytes are glyph scan-line patterns). Not an executable 'add [bx+si],al'.  // surrounding glyph bytes 80 7E 22 20 3C 20; labeled disasm marks this region as character-generator font/bitmap data [R16]
0x00de4b [string/data] (confirmed) Data misdecoded as code: part of a jump/offset table of little-endian word values in the EMULATOR/font region. Surrounding 08xx 'or' decodes and the following ascending 02xx word run (0214 0222 0230 ...) show this is a pointer/dispatch table, not executable instructions.  // 08EA between runs of 08xx then a regular ascending word table 0x1402,0x4c02,0x7602,0x9202 etc.; pattern is data not code
0x00de7d [string/data] (confirmed) Data misdecoded as code: this 0x0348 is one entry of an ascending little-endian word/offset table (0x033A,0x0348,0x0364,0x0372,0x0380...) in the upper-ROM EMULATOR offset/pointer table region; ndisasm renders the table words as bogus 'add' instructions, not real code.  // 034803 amid ascending word run 033A 0348 0364 0372 0380
0x00dfed [string/data] (confirmed) Data misdecoded as code: this is part of a table of 16-bit little-endian word values (e.g. 0x0B6E, 0x0B7C, 0x0B8A...) in the upper-ROM data/font region, an offset or attribute lookup table, not executable instructions.  // monotonic '0B xx' word sequence (0B0C,0B1A,0B28,0B36,0B44,0B52,0B6E,0B7C...) typical of a pointer/offset table; region in upper-ROM data area
0x00e058 [string/data] (confirmed) Data misdecoded as code: this 0x5F byte is part of a little-endian word/offset table in the upper-ROM data region; nearby real references like 'or [0x1409],ax' (0x1409 = the [0x914] control-block area) and the surrounding 09xx/01xx word run show this is pointer/table data, not executable instructions.  // pop di / scasb / or [0x1409],ax — ascending 09xx 01xx word run, not coherent code
0x00e494 [string/data] (confirmed) Data misdecoded as code: part of a table of little-endian 16-bit word values (an offset/pointer or attribute lookup table) in the upper-ROM EMULATOR/font data region; surrounding bytes form ascending word entries that ndisasm renders as bogus add/sbb/ret.  // context of 'ret 0xd019', 'sbb [si],ch', 'add [bx+si+0x1a],ax' — incoherent control flow in upper-ROM data block
0x00e51d [string/data] (confirmed) Data misdecoded as code: this 0x06 byte is one entry of a little-endian word/offset table in the upper-ROM EMULATOR/font data region; the surrounding run of 0x1Cxx bytes ndisasm renders as bogus 'sbb al,imm'/'push es', not executable instructions.  // sequence 06 70 1C / 7E 1C / C4 1C bytes decode as nonsensical push es/jo/jng/les
0x00e541 [string/data] (confirmed) Data misdecoded as code: this 0xB2 0x16 ('mov dl,0x16') is part of a little-endian word/offset table in the upper-ROM EMULATOR/font data region; the surrounding run of bogus sbb/adc/xor/push-ss decodes (e.g. 0x1942, 0x3416, 0x13D0) shows ndisasm is rendering table entries, not executable instructions.  // B216 amid sbb [bp+si+0x16],ax / xor al,0x16 / sbb ch,al garbage
0x00e5ef [string/data] (confirmed) Data misdecoded as code: this 0x5F byte is part of a little-endian word/offset table in the upper-ROM EMULATOR data region (surrounding 0x15A8,0x160A,0x1017,0x1864,0x2100 word entries); ndisasm renders the byte stream as bogus pop di/sbb/in instructions.  // byte 5F inside run of adc/sbb/retf garbage with ascending 0x1xxx word values typical of an offset table
0x00e662 [string/data] (confirmed) Data misdecoded as code: this 0x06 byte is part of a 16-bit word table / font region in upper ROM (surrounding bytes 0x14F2,0x14C8,0x14E4,0x14AA and 0x110F,0x1722,0x1730 are little-endian table entries, with a real instruction island at 0xE66C). Not an executed 'push es'.  // isolated 0x06 amid adc/add garbage decodes (14F2/14C8/14AA) that are word-table values, not a code path; ndisasm renders 'push es'
0x00e685 [string/data] (confirmed) Data misdecoded as code: this is part of a table of 16-bit word values (offsets/pointers) in the font/glyph or string-pointer region of upper ROM. The surrounding bytes form a sequence of little-endian words (0x11xx, 0x12xx, 0x13xx) that ndisasm renders as bogus adc/add; not executable code.  // surrounding bytes 1110 111E 1164 11DE 12C2 12D0 ... form an ascending word table; 0F5E0F decodes as SSE divps which cannot run on an 80188
0x00e694 [string/data] (confirmed) Data misdecoded as code: this 0x46 byte is part of a table of 16-bit little-endian word values (0x1562, 0x15A8, 0x11D4...) in the upper-ROM pointer/offset table; the run of adc/add and stray prefixes (lock, fcom) confirm it is data, not executable instructions.  // surrounding ascending words 1562/15a8/11d4 and bogus F011/D817 fcom decodes; offset/pointer table region
0x00e726 [string/data] (confirmed) Data misdecoded as code: part of a table of 16-bit little-endian word values (0x12EC, 0x147E, 0x1386...) in the upper-ROM offset/pointer table region; ndisasm renders the bytes as bogus add/adc. Not executable code.  // ascending word values 12ec/147e/1386 with stray prefixes (66, lock); surrounded by adc/add misdecodes
0x00f29e [string/data] (confirmed) Data misdecoded as code: this 0x00F6 lies immediately before the ASCII string 'FONTFILE' (bytes 0x46 0x4F 0x4E 0x54 0x46 0x49 0x4C 0x45 at 0xF2A1) in a small data record, followed by 0xFF erased fill; it is a header/data byte for the FONTFILE filename entry, not an executable 'add dh,dh'.  // xxd shows 'FONTFILE' at 0xF2A1-F2A8 right after this byte, then 0xFF fill; in al,0x5d/00F6 are part of the data blob
