0x00006F1E [R1] This `OUT DX,AL` writes to I/O port 0x343, which is part of the chip-select programming for the memory controller, specifically configuring the upper memory chip-select (UMCS) or similar register during the boot initialization sequence.
0x000063A0 [R1] This `INT 21h` with AX=0x3E00 is a DOS-compatible system call to close a file handle (function 0x3E), used by the kernel’s file I/O layer when managing embedded filesystem executables.
0x0000A645 [R1] This `PUSH AX` saves the accumulator before a subroutine call that manipulates segment registers and performs a memory copy or comparison, likely part of the font/glyph table loading or character generator setup.
0x0000A214 [R1] This `MOV AX,0xE000` loads a segment value for a memory comparison routine, used in the manufacturing self-test (MFGTEST) to verify ROM contents by scanning for a signature pattern at segment 0xE000.
0x00009EDE [R1] This `CALL 0x6C0E` invokes a subroutine that prints a numeric value (likely a hex or decimal conversion routine), used in the self-test to display NVM data or error codes during the NVM (non-volatile memory) test.
0x00007342 [R1] Pop si, dx, cx, ds and return — this is the exit path of a subroutine that processes a linked list or table of font/glyph entries, restoring registers after a search or copy loop.
0x00006E0B [R1] Set AH=0xE and call INT 0x10 to write a character in teletype mode — this is part of the terminal emulator’s character output routine, used to display text on screen.
0x00006C99 [R1] Increment BX again (second inc bx) to skip past a word in a data structure — this is part of the ROM filesystem loader that reads an MZ executable header, advancing past the length field to get the next field.
0x0000BBF8 [R1] OR byte [ES:BX], 0x8 — this sets a bit in a video attribute or character-cell memory map, part of the CRT controller’s character-attribute update logic during screen refresh or cursor manipulation.
0x000064AA [R1] Set AH=0x9 and call INT 0x21 to print a $-terminated string — this is part of the setup menu or self-test code that displays a status message (e.g., a test result or configuration prompt).
0x0000645D [R1] This code prints a formatted time or value (hours:minutes) by calling INT 21h AH=02h (display character) for each digit/separator, and the marked call at 0x645D converts a 16-bit value from [bx+0x4] into a displayable hex or decimal string, likely part of the setup menu or self-test status display.
0x00006D55 [R1] This is a sequence of redundant `push cs / pop es` instructions (11 repetitions), which is almost certainly data (e.g., a font bitmap or table) being misdecoded as code — answer DATA.
0x00009E10 [R1] This is the entry point of a subroutine that sets up a new memory segment/arena (initializing a memory control block at offset 0 with segment address in AX), part of the kernel’s memory management or executable loading system (DOS-like PSP/arena initialization).
0x0000A047 [R1] The `cli` at 0xA047 disables interrupts before modifying the UART or video CRTC control register at I/O port 0x341 (likely a UART or CRTC register), then re-enables with `sti` after writing, part of the serial port or video mode initialization code.
0x0000E6AF [R1] This sequence of bytes (including `add ax,0x2100`, `add cx,[bp+si]`, `db 0x0f`, etc.) is clearly data — likely part of a font bitmap, character generator table, or other constant table — answer DATA.
0x000063DA [R1] This instruction computes the remaining byte count (0x80 - AX) for padding a partially-filled XMODEM data block with 0x1A (Ctrl-Z) characters, part of the XMODEM file download routine in DOWNLOAD.EXE.
0x00009B33 [R1] This sets up an INT 21h, function 0x4200 (LSEEK with method SEEK_SET) to reposition the file pointer to the beginning of a file, used in the kernel’s file I/O layer for reading/writing files from the embedded filesystem.
0x0000BBA9 [R1] This conditional jump checks whether a flag at [0x986] is zero; if non-zero, it skips the subsequent cursor/attribute update logic, likely part of the terminal emulator’s display refresh or cursor-position handling in EMULATOR.EXE.
0x00009E75 [R1] This stores a zero into a field at offset 0x8 of a memory control block (MCB) structure, part of the kernel’s memory management (alloc/free) for DOS-compatible process segments.
0x0000A6B0 [R1] This code initializes the interrupt vector table by writing the address of an interrupt handler (0x490D:CS) to vector 0x20 (offset 0x80) and another handler (0x48C6:CS) to vector 0x27 (offset 0x9C), setting up system interrupt handlers for the firmware kernel.
0x000060FA [R1] This instruction calls a subroutine (0x6139) that converts a 16-bit value (here the stack segment from [bp+0x6]) into a hexadecimal string for display, as part of a diagnostic routine that prints register values (SS, SP, etc.) via INT 21h AH=09h string output.
0x000076B5 [R1] This instruction compares the character in AL with a stored byte at address 0x973, which is part of a VT-style escape sequence parser that checks for specific control characters or parameters to dispatch terminal emulation functions.
0x0000BC17 [R1] This instruction sets AH=0 and calls INT 0x40, which is a custom firmware interrupt (likely for NVM or configuration access), initiating a read operation that returns a segment:offset pointer in ES:BX, used to check for valid configuration data.
0x0000B02D [R1] This instruction stores the result of a subroutine call (0xB1D8, which reads a 16-bit value from NVM or hardware registers) into memory location 0x2860, part of a sequence that loads multiple configuration parameters (likely LAN address bytes or terminal settings) from non-volatile memory.
0x0000A6A5 [R1] This instruction saves the current ES segment value to AX, which is part of a memory allocation or segment traversal routine that adjusts segment registers when walking through a linked list or memory block chain in the kernel’s memory manager.
0x00007607 [R1] This instruction pushes DS onto the stack to preserve the data segment before calling a subroutine that looks up a character glyph or attribute from a font/attribute table indexed by AL, likely part of the terminal emulator’s character display or attribute mapping function.
0x00009D7A [R1] This instruction adds a byte from the ES segment (at offset SI+0xA) to BL, computing an index into a jump table for dispatching based on a filesystem or executable header type (e.g., “LC” signature), used by the kernel’s program loader to handle different executable formats.
0x0000AFA7 [R1] This instruction checks if AL is less than or equal to 0x17, part of a cascaded comparison chain that maps a character code (likely from a VT escape sequence or keyboard input) to a display attribute value (AH), which is then stored at 0x971 for use by the terminal emulator’s rendering logic.
0x00006DFC [R1] This instruction compares BX to 0xC6, serving as a bounds check before a jump-table dispatch for INT 10h video service calls (AH function number), where values >= 0xC6 fall through to INT 4Fh (video BIOS extensions) or a default handler in the terminal emulator’s video BIOS wrapper.
0x0000B5DF [R1] This instruction clears the high byte of DX (`xor dh,dh`) during decompression of a run-length encoded font or glyph bitmap, where DL holds the repeat count for a character row.
0x0000B0C7 [R1] This is a `repe cmpsb` string comparison that checks a 15-byte signature or identifier (e.g., a version string or configuration marker) against a stored reference at `0x285e`, likely during terminal emulator initialization or self-test.
0x0000A4B7 [R1] This instruction checks whether the high word of a 20-bit segment:offset address (after rounding up by 0xF) is less than 2, which is part of a memory region validation routine — likely verifying that a pointer falls within the first 128 KB of the address space for a ROM or RAM test or copy operation.
0x0000666C [R1] This compares a counter variable at [0x1CE] against 0x167 (359 decimal), which is part of a backspace/character-deletion handler in the terminal emulator — it prevents the cursor from moving left past the beginning of the line buffer.
0x0000712C [R1] This calls subroutine 0x714E with CX=0x880 (2176 decimal), which is part of a printer port test in the manufacturing self-test (MFGTEST.EXE) — it sends a specific data pattern to the parallel port to verify proper operation.
0x0000B5A5 [R1] This pushes DS onto the stack to save the data segment register, which is part of a font/glyph rendering routine that loads character bitmaps from a segment:offset pointer — it preserves the current data segment before loading a new one for font table access.
0x00006B84 [R1] Pop dx to restore the register after a chip-select or UART register write (port 0x343) that set a bit in [0x7b9] and output it, likely part of a checksum or ROM verification routine that sums bytes from segment 0xC000.
0x0000A953 [R1] Load a byte from a data structure at [bx+0xa5] into al, part of an INT 21h or interrupt handler that retrieves a character or attribute from a terminal emulator or keyboard buffer context.
0x0000B36A [R1] Store al (a masked attribute or status byte) to [0x98f], part of a routine that sets a video or NVM control flag and then writes a byte to video memory at 0xC200, likely updating a cursor or display attribute in the terminal emulator.
0x00009A58 [R1] Increment di to skip a byte while constructing an interrupt vector table entry or a far call stub in segment 0x080C, part of setting up a custom interrupt handler (e.g., INT 4Fh) for the terminal’s DOS-compatible system call layer.
0x00006A25 [R1] Loop (loop 0x6a22) to accumulate a checksum or compute a sum of words from segment 0xC000, part of a ROM or firmware integrity check (self-test) that verifies the contents of the font or code region.
0x000066C0 [R1] This instruction copies the byte read from I/O port 0x300 (UART register) into AH, as part of a loop that reads 16 bytes from the UART ports starting at 0x300 and calls a subroutine at 0x6c6f to process each byte, likely for a serial port test or initialization routine.
0x00009FA4 [R1] This instruction sets AH=0x1 for INT 0x17 (printer BIOS service) to initialize the printer port, part of the printer port test in the manufacturing self-test (MFGTEST.EXE).
0x0000A8A9 [R1] This instruction compares BX to 0x245e as a loop boundary check, part of a search routine that iterates through a data structure (with 0xB8 byte entries) looking for a matching byte at offset 0xA9, likely used in the terminal emulator's escape sequence parsing or attribute lookup.
0x00006BA3 [R1] This instruction calls INT 0x21 with AX=0x3D00 (DOS open file function) to open the file whose path is at DS:DX=0x0793, part of the XMODEM download routine (DOWNLOAD.EXE) that opens a file for receiving firmware updates.
0x0000A04D [R1] This instruction outputs AL to I/O port 0x341 (likely a UART or video control register), setting bits 0x0C (RTS and DTR) to enable serial handshaking, part of the terminal emulator's serial port initialization for communication.
0x0000B954 [R2] This `push es` saves the extra segment register before setting ES to 0xD000 for a memory copy or comparison operation, likely part of the terminal emulator's screen buffer or font rendering routine that manipulates video memory at segment 0xD000.
0x0000B5FB [R2] This `jnc 0xb5e1` (jump if not carry) continues a decompression loop that processes run-length encoded font or glyph bitmap data, where CL tracks remaining row bytes and DL holds the repeat count for a character row.
0x00009B5F [R2] This `int 0x21` with AX=0x3F00 (DOS read file function) reads a second 0x8000-byte block from a file into the next segment, part of the kernel's embedded filesystem loader that loads an MZ executable in multiple segments.
0x000079F2 [R2] This `mov ds,bx` sets the data segment to 0x40 (the BIOS Data Area), allowing access to the keyboard buffer or BIOS flags at [0x914] to check for a pending key or status byte, part of the terminal emulator's keyboard input handler.
0x00009FF0 [R2] This `xor al,0xc8` toggles specific bits (0xC8) in the value read from I/O port 0x340 (likely a UART modem status register), used to construct a status byte in BH for return via IRET, part of a serial port status query handler.
0x0000A28E [R2] This instruction loads a 16-bit value from offset 0xC of a memory control block (MCB) structure pointed to by ES:DI, which is part of the kernel's memory management routine that compares block sizes during memory allocation or deallocation (arena traversal).
0x00007264 [R2] This `pop ds` restores the data segment register after a subroutine call that attempted to open or access a file, and if the carry flag is set (indicating failure), it returns with an error — part of the kernel's file I/O layer for the embedded filesystem.
0x00006C8F [R2] This `ret` instruction returns from a subroutine that checks whether the NVM (non-volatile memory) pointer returned by INT 0x40 is null (ES:BX == 0), indicating no valid configuration data exists — part of the NVM read or configuration validation routine.
0x0000A2F1 [R2] This `push cx` saves the loop counter before entering a character-output loop that uses INT 10h AH=0xE (teletype output) to display a string of CX bytes from DS:SI, part of a terminal emulator or setup-menu text display routine.
0x0000B63C [R2] This `stosb` writes a zero byte (AL=0 from the previous `xor al,al`) to the destination buffer, terminating a font or glyph bitmap row after copying 7 words via `rep movsw`, part of a character generator or font decompression routine.
0x0000FED0 [R2] This writes 0x0F to I/O port 0xFF1E, which is part of the 80186 on-chip interrupt controller's interrupt mask register (IMR) or interrupt request register, initializing the interrupt controller during the boot sequence after setting the stack pointer and segment registers.
0x00006CF2 [R2] This sets CX=0x8000 (32768 decimal) for a loop that sums words from the ROM or memory region starting at DS:SI, computing a 16-bit checksum as part of a ROM integrity verification or self-test routine.
0x0000E476 [R2] This sequence of bytes (including `fcom dword [bx]`, `xchg ax,dx`, `pop ss`, etc.) is clearly data — likely part of a font bitmap, character generator table, or other constant table — answer DATA.
0x00009B46 [R2] This moves DI (a buffer offset or pointer) into DX as the file offset for an INT 21h, function 0x3F00 (read from file) call, part of the kernel’s file I/O layer that reads a 32 KB chunk (0x8000 bytes) from the embedded filesystem into memory.
0x0000BBEF [R2] This `stosw` stores AX (a character/attribute word) into the video display buffer at ES:DI, part of the terminal emulator’s screen update logic that writes a character cell (with attribute) to video memory and then updates the cursor/attribute map at [0x98B].
0x00009DBE [R2] This instruction loads BX with the segment value from offset [SI+0x4] of a memory control block, part of the kernel's memory arena management routine that unlinks or relinks free blocks in the DOS-compatible memory allocation system.
0x0000B519 [R2] This loads SI with a word from [BX] (a pointer to font bitmap data) and adds BP as a base offset, then copies 16 bytes (one character row) via `rep movsb` in a loop that decompresses run-length encoded character glyphs for the terminal emulator's font rendering.
0x0000B6F4 [R2] This loads AL=0x00 and calls subroutine 0xB7FC to write a byte to the CRT controller's data register (port 0x3D5) after setting the index register, part of a sequence that programs the video CRTC registers (e.g., horizontal total, display enable) during terminal emulator initialization or mode set.
0x00009D24 [R2] This stores the current DS segment value into the MCB field at [ES:BX+0x2] (the "owner" or "previous" block pointer), part of the kernel's memory block insertion routine that links a newly allocated or freed block into the arena's doubly-linked list.
0x00007586 [R2] This compares SI (a pointer into a circular keyboard or serial input buffer) with the buffer's end pointer at [BX+0x8E], part of the terminal emulator's input queue management that checks for buffer wrap-around or empty/full conditions before reading the next character.
0x000079FC [R2] This `test al,al` at 0x79FC checks whether the byte at [bx+0x98] (a keyboard or input buffer status flag) is zero, part of an INT 16h keyboard service handler that determines if a key is available or if a previous keypress needs to be cleared before returning.
0x0000AFF2 [R2] This `push bx` at 0xAFF2 saves the BX register at the start of a subroutine that increments a tick counter at [0x968] and updates a timer-related byte at [0x972], part of the terminal emulator’s periodic timer interrupt handler (likely driven by the 80186 timer or system tick) that manages cursor blink or screen refresh timing.
0x00006C17 [R2] This `rol ax,byte 0x4` at 0x6C17 rotates the high nibble of AX into the low nibble, as the first step of a hex-to-ASCII conversion subroutine (called at 0x6C0E) that prints a 16-bit value as two hex digits via INT 10h AH=0Eh, used by the self-test or setup menu to display numeric values (e.g., NVM data or error codes).
0x0000FE84 [R2] This `out dx,al` at 0xFE84 writes 0x80 to I/O port 0xFFB4, which is part of the boot initialization sequence that programs external peripheral chip-selects (PCS) — specifically configuring the control register for the NVM (non-volatile memory) or a UART, setting a high bit to enable or reset the device.
0x00006CB4 [R2] This `loop` instruction repeats the loop starting at 0x6CAD for 8 iterations, copying 8 words from the font/glyph table at segment 0xC000 into a display buffer, part of the terminal emulator's character rendering routine that loads character bitmaps for screen output.
0x0000E183 [R2] This `add al,0xde` instruction is part of a sequence of `add al,xx` bytes that form a data table (likely a font bitmap or character generator table) being misdecoded as code — answer DATA.
0x0000AC9E [R2] This `test ax,ax` checks if the result of a division (computing a screen position or cursor coordinate) is zero, part of a terminal emulator or video driver routine that calculates row/column positions for cursor placement or scrolling, branching to handle the zero case (e.g., top of screen).
0x0000B926 [R2] This `xor ch,ch` clears the high byte of CX to use CL (loaded from [0x989]) as a count for a `rep stosb` operation that fills video memory with spaces (0x20) and attribute bytes, part of the terminal emulator's screen clearing or line-erasing routine.
0x0000664C [R2] This `cmp word [0x130],byte +0x3` checks a configuration or mode flag at address 0x130 against the value 3, part of the terminal emulator's backspace/character-deletion handler that determines whether to use a special deletion mode (e.g., for VT-style escape sequences) before processing the backspace character.
0x0000A56E [R2] This instruction reads a byte from the ES segment at DI-1, which is part of a loop that waits for a video memory attribute byte to have bit 7 set (test al,0x80), used in the terminal emulator's character rendering or cursor update logic to synchronize with the display refresh.
0x00006124 [R2] This INT 21h with AH=0x2 prints a space character (DL=0x20) to the console, part of a diagnostic routine that displays register values (here printing spaces between hex values) during a crash or error dump in the firmware kernel.
0x0000BC79 [R2] This increments DI to advance to the next character position in video memory, part of the terminal emulator's screen output routine that writes characters and attributes to the display buffer, specifically after calling a subroutine (0xBCC5) to output a character with a given attribute.
0x00009BCF [R2] This RETF 0x2 returns from a far call and pops 2 bytes from the stack, part of the kernel's file I/O or interrupt handler dispatch that returns a success/failure status (carry flag set by previous instruction) to the caller, likely for an INT 21h function.
0x0000B38A [R2] This conditional jump (JNZ) checks if two pointers at [ES:0x914] and [ES:0x978] are not equal, part of the terminal emulator's interrupt handler that compares cursor position or buffer pointers to determine whether to update the display or clear a character attribute at the cursor location.
0x00006BC7 [R2] This instruction checks the carry flag after an INT 21h LSEEK call (AX=0x4200, SEEK_SET) to reposition the file pointer to the beginning of a file, part of the XMODEM download routine (DOWNLOAD.EXE) that reads a file from the embedded filesystem for firmware transfer.
0x00009B1D [R2] This instruction pops SI from the stack as part of a subroutine epilogue that restores registers (SI, DX, CX, BX) before returning via `retf 0x2`, used in the kernel's interrupt handler or system call return path (e.g., after an INT 21h file I/O operation).
0x00006787 [R2] This `test al,al` checks if the first byte of a memory block (pointed to by ES:DI) is zero or 0xFF, which is part of a memory test or initialization routine that determines whether a segment is free or allocated (likely in the kernel’s memory arena/PSP management during executable loading or self-test).
0x0000B2B4 [R2] This `mov es,di` sets ES to 0x0000 (since DI was zeroed) to access the interrupt vector table at the bottom of memory, part of a routine that installs a custom interrupt handler (storing the signature 0x5647 and the current CS at vector 0x10, i.e., INT 0x10 offset 0x40) for the terminal emulator’s video BIOS wrapper or system call layer.
0x000071CB [R2] This `push dx` is part of a sequence that is almost certainly data (the surrounding bytes include `dec sp`, `and ax,0x3e4c`, and other nonsensical instructions), so this is a misdecoded data table (likely a font bitmap or character generator table) — answer DATA.
0x00009D95 [R2] This `mov es,ax` sets the extra segment to the segment value loaded from [SI+0x2] (a memory control block field), part of the kernel’s memory management routine that swaps or relinks MCB entries (adjusting the “previous” and “next” pointers and updating the size field) during a memory block free or coalescing operation.
0x000076ED [R2] This `jmp short 0x76b5` is a branch back to the top of a VT-style escape sequence parser dispatch loop, used after clearing a flag (e.g., resetting a bit in [0x7f5] or [0x7f6]) to continue processing the next character in the input stream for terminal emulation.
0x000079CB [R2] This `mov ds,bx` sets the data segment to 0x40 (BIOS data area) to access the BIOS timer tick or keyboard flag area, part of an interrupt handler that checks or updates a terminal emulator status byte at offset 0x98 in a data structure.
0x0000798E [R2] This `mov bx,0x40` loads the segment for the BIOS data area (0x40) to access a pointer at [0x914], part of an interrupt handler that stores a byte (likely a keyboard or serial status flag) into a data structure at offset 0x97 with bit 7 set.
0x00009FD0 [R3] This `call 0x74e5` is part of a serial port initialization or handshake routine that writes a byte to UART port 0x342 (likely the transmit buffer or line control register) and then calls a delay subroutine to allow the UART to stabilize before setting modem control bits (RTS/DTR) at port 0x341.
0x0000DAFE [R3] This `cmp byte [bx+si],0x44` is part of a font bitmap or character generator table (the surrounding bytes form a 8x8 pixel glyph pattern for a character like 'D'), so this instruction is misdecoded data — answer DATA.
0x0000DAF8 [R3] This `sbb al,0x0` is part of a font bitmap or character generator table (the surrounding bytes form a 8x8 pixel glyph pattern for a character like 'C'), so this instruction is misdecoded data — answer DATA.
0x0000B46C [R3] This `mov di,0xde00` sets the destination index to segment offset 0xDE00 for a `rep movsb` operation that copies 16 bytes of font bitmap data from source SI=0x524A to video memory or a glyph buffer, part of the terminal emulator's character generator or font rendering routine that loads character glyphs for display.
0x0000A580 [R3] This `ret` instruction returns from a subroutine that writes a character with attribute 0x40 (green on black) to the video display buffer and waits for bit 7 of the attribute byte to be set (indicating display refresh synchronization), part of the terminal emulator's character output routine that handles cursor positioning and screen updates.
0x0000AF99 [R3] This instruction compares AL to 0x0C as part of a cascading range check that maps a character or attribute value (likely a font height or scan line count) to a corresponding AH value (1-8), which is then stored at [0x971] for use in the terminal emulator’s character rendering or cursor positioning logic.
0x000065E4 [R3] This instruction increments a counter at [0x53b] that cycles through 0-9 (modulo 10), which is used to select one of ten font or character generator rows by adding the counter to a base address (0xC000) and outputting the result to I/O port 0xFF88 — part of the terminal emulator’s font/glyph table selection or CRTC register programming for character row addressing.
0x0000DAFC [R3] This instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000B734 [R3] This instruction compares a byte at [0x98a] to 0x10, determining whether to program the CRT controller with a different set of register values (e.g., 0x2D/0xAF vs. 0x2C/0x8E) based on a configuration or mode flag — part of the terminal emulator’s video CRTC initialization or mode-set routine that adjusts horizontal or vertical timing parameters.
0x0000A894 [R3] This IRET instruction returns from an interrupt handler (likely INT 10h or a custom video BIOS interrupt) after loading a value from a data structure at [bx+0xA9] into DX, which is part of a dispatch or lookup routine that retrieves a character attribute or status byte for the terminal emulator’s video service layer.
0x00007086 [R3] This `stosw` writes the value 0x11DC (a character/attribute word) into the video display buffer at offset 0x100, part of the terminal emulator's screen initialization routine that fills the video memory with default character cells (likely spaces with a specific attribute) to clear or set up the display.
0x00006F90 [R3] This `mov es,dx` sets the extra segment to the next 0x1000-byte block (starting at 0x1000, then 0x2000, etc.) during a memory scan routine that searches for a specific byte pattern (in BX) across the first 64 KB of memory, part of a RAM test or memory detection algorithm in the manufacturing self-test (MFGTEST.EXE).
0x00006B68 [R3] This `pop ds` restores the data segment register after a subroutine call that computed a checksum or summed bytes from segment 0xC000 (the font or code region), part of a ROM integrity verification routine in the self-test that checks the firmware or font table contents.
0x0000AF36 [R3] This `out dx,al` writes 0xE001 to I/O port 0xFF46, which is part of the 80186 on-chip interrupt controller's interrupt vector register (IVR) or interrupt control register, programming the base vector for interrupt requests during the terminal emulator's initialization or interrupt setup sequence.
0x00006E25 [R3] This `xor bx,bx` clears BX to zero before setting ES to 0x0000 (the interrupt vector table), part of a routine that installs a custom interrupt handler by writing a far pointer (segment:offset from DX:DS) into the IVT at the vector number specified by AL, used by the kernel's system call or interrupt dispatch layer.
0x00006021 [R3] This instruction loads a word from [0x7BA] into AX, which is part of a crash/error dump routine that retrieves a saved register value (likely the stack pointer or a saved segment) to display it via the hex-printing subroutine at 0x6139, after previously clearing a bit in the NVM or UART control register at port 0x343.
0x0000AFE3 [R3] This compares AL to 0x10 (XON character), and if not equal, jumps back to 0xAFB6 to continue the VT-style escape sequence parser’s dispatch loop, which checks for specific control characters (e.g., XON, XOFF, DC1, DC2) to set a flag at [0x7F4] for flow control or terminal mode handling.
0x0000A84D [R3] This sets AH=0x0 for INT 0x10 (set video mode) to initialize or reset the display mode based on a byte loaded from a data structure at [BX+0x99], part of the terminal emulator’s routine that restores or applies saved video and NVM configuration parameters (cursor position, mode, attributes) from a context block.
0x0000A52C [R3] This pops CX from the stack as part of a subroutine epilogue that restores registers (CX, BX, ES, SI, DS) before returning via RETF 0x2, used in the kernel’s memory block verification or copy routine that checks if a segment offset falls within a valid range (e.g., below 0x8000) before proceeding with a memory test or font loading operation.
0x00006E9E [R3] This conditional jump (JC) loops back to 0x6E8A if BX (the current pointer into a 0xB8-byte entry data structure) is less than 0x245E, continuing a search routine that iterates through entries looking for a matching AX value at offset [BX+0x4] and a non-zero byte at [BX+0xA7], likely used in the terminal emulator’s escape sequence or attribute table lookup.
0x00009CA5 [R3] This instruction decrements the segment value in AX to access the Memory Control Block (MCB) header immediately preceding an allocated memory block, part of the kernel's DOS-compatible memory management routine that validates MCB signatures (0x434C) and checks allocation type during memory free or validation operations.
0x0000AC16 [R3] This instruction sets ES to 0x40 (BIOS Data Area segment) to copy a 12-byte data structure (6 `movsw` operations) from the terminal emulator's internal state at [0x953] into the BIOS Data Area, likely updating BIOS keyboard or timer flags as part of an INT 10h or INT 16h video/keyboard service handler.
0x0000714E [R3] This instruction sets DI=0x64 (100 decimal) as a loop counter for a parallel printer port test subroutine in MFGTEST.EXE, which sends a data pattern (0x55, 0xE8) to I/O port 0x342 and reads back status from port 0x340 to verify printer port operation.
0x0000BAF3 [R3] This instruction adds DX (a cursor column offset or buffer index) to BX (loaded from [0x9B8], the base address of the video display buffer) to compute the destination address for storing a character byte (AL) into the terminal emulator's screen buffer, part of the character output routine that writes to video memory.
0x000064FF [R3] This instruction loads DX with the address of a string (0x6FB) that will be printed via INT 21h AH=9, as part of a routine that toggles and displays a printer port status message (likely "Printer On" or "Printer Off") in the manufacturing self-test (MFGTEST.EXE) based on the flag at [0x7AF].
0x0000B15D [R3] This instruction subtracts the base address 0x285E from SI to compute an index into a font or glyph bitmap table, which is then multiplied by 8 (via three `add bx,bx` instructions) to calculate the byte offset for a character row in the terminal emulator's character generator rendering routine.
0x00007491 [R3] This instruction sets DS to 0x40 (the BIOS Data Area segment) to access the BIOS timer tick or keyboard flag area, part of a subroutine that writes a byte to I/O port 0x343 (likely a UART or chip-select control register) to enable or disable serial handshaking signals (RTS/DTR) based on the AL parameter.
0x0000FE18 [R3] This instruction loads AX with 0x800A and outputs it to I/O port 0xFFA2 (the LMCS chip-select register), configuring the lower memory chip-select for the 80186 to map the ROM at physical address 0x80000 with a 10-byte wait state or address range, part of the boot initialization sequence that programs the memory controller.
0x00006B2B [R3] This instruction pushes DX onto the stack to save the segment value (from AX) before setting ES to that segment, as part of a routine that reads or writes to the NVM (non-volatile memory) via I/O port 0x343, constructing a control byte (AL) based on flags in DX (bits 0x1000 and 0x2000) to select NVM bank or mode.
0x0000A643 [R3] This instruction pushes DS to save the data segment register as part of a memory block relocation or size adjustment routine in the kernel's memory management layer, where it manipulates MCB (Memory Control Block) fields (subtracting from and adding to offsets 0x4 and 0x6) to adjust block sizes during allocation or deallocation.
0x0000786C [R3] This conditional jump (JNZ to 0x78A7) checks if the lower 2 bits of AL (tested with 0x03) are set, part of a serial port or keyboard status byte decoding routine in the terminal emulator's input handler that determines the type of received character or event (e.g., break, parity error, or framing error) before dispatching to the appropriate processing path.
0x0000A3B0 [R3] This `push cx` at 0xA3B0 is the start of a subroutine prologue that saves registers (CX, DX, SI, DI, DS, ES) before calling subroutines at 0xA1B8 and 0xA1F8, part of the kernel's memory management or file I/O layer that validates and processes a memory control block (MCB) or file handle structure.
0x00009E07 [R3] This `pop dx` at 0x9E07 is part of a subroutine epilogue that restores registers (DX, CX, BX, AX) and returns with carry clear via `retf 0x2`, used in the kernel's memory arena management routine (likely after successfully linking or freeing a memory control block) to return success status to the caller.
0x0000B640 [R3] This `mov cx,0x8` at 0xB640 sets the count for a `rep movsw` operation that copies 8 words (16 bytes) of font bitmap data from the source (DS:SI) to the destination (ES:DI), part of the terminal emulator's character rendering routine that transfers a single character row's glyph data for display.
0x00006CC8 [R3] This instruction checks if the high byte (AH) of a character/attribute word is less than 0x80, part of a character rendering routine that replaces non-printable or high-bit-set characters with a dot (0x2E) for display in the terminal emulator's screen output.
0x0000B567 [R3] This instruction saves the DS register before switching to the BIOS Data Area segment (0x40) to check keyboard buffer pointers, part of the terminal emulator's interrupt handler that determines whether to update the display or process keyboard input.
0x00006963 [R3] This instruction masks DX to keep only the lower 12 bits (0xFFF), part of a routine that constructs a segment address by adding 0xC000 to the masked value, used to set ES for accessing a specific memory region (likely font or code segment) during a ROM verification or checksum operation.
0x0000B7D8 [R3] This instruction combines the CH register (which holds a bitmask for video mode or CRTC control bits) with the current value from [0x7b9] (masked to clear bits 3-5), then writes the result to I/O port 0x343 (a CRTC or video control register) — part of the terminal emulator's video mode initialization or CRTC register programming sequence that sets display timing or attribute control bits.
0x0000AB40 [R3] This instruction tests bit 4 of the value read from the 80186 timer 1 status register (I/O port 0xFF5A), checking the timer's output or status flag to determine whether to set bit 4 in AL (which constructs a return status byte) — part of a serial port or timer status query handler that returns modem or timer status bits for a system call or interrupt handler.
0x0000999E [R3] This instruction pops BX (the previously pushed memory size or segment value) from the stack, then compares it with AX (the result of a memory allocation or arena traversal routine) to check if the available memory block is large enough — part of the kernel's DOS-compatible memory management (arena allocation) that validates block sizes during memory allocation or executable loading.
0x00007048 [R4] This instruction writes 0x44 to AL, which is then stored at [0x7b9] and output to I/O port 0x343 (a CRTC or video control register), part of the terminal emulator's video mode initialization sequence that sets a specific control byte (likely enabling a video mode or attribute setting) after copying and clearing a display buffer region.
0x0000AD07 [R4] This `test ax,0x1000` checks bit 12 of the value read from the 80186 interrupt controller's in-service register (I/O port 0xFF36), polling for a specific interrupt request (likely timer or serial interrupt) to be acknowledged before proceeding with the interrupt controller initialization or mode-setting sequence.
0x0000E64D [R4] This `adc bh,al` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000E796 [R4] This `push word 0x941e` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00006E0F [R4] This `pop bx` restores the BX register as part of the epilogue of an INT 10h video service handler (which just called INT 0x10 with AH=0x0E to output a character via BIOS teletype), returning from the interrupt via the subsequent `iret` instruction.
0x00009EE1 [R4] This `mov dx,0x878` loads the address of a string (likely an error or status message) for printing via INT 21h AH=9, part of the kernel's memory management or MCB validation routine that displays diagnostic information about a memory control block (e.g., block size or allocation type) during a memory dump or error reporting function.
0x0000BBD1 [R4] This `pop ds` restores the data segment register after a `rep movsw` operation that copied a row of video memory (character/attribute words) within the terminal emulator's screen scrolling or line-copy routine, which shifts display buffer contents during scroll operations.
0x0000E4AD [R4] This `sbb dl,ah` is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000C8FB [R4] This `or [bx+si],cl` is part of a font bitmap or character generator table (the surrounding bytes form a 8x8 pixel glyph pattern for a character like 'H' or a similar letter), so this is DATA.
0x0000A16C [R4] This `mov ax,0x2` sets the return value to 2 (likely an error code indicating "file not found" or "invalid handle") for a kernel memory management or file I/O routine, preparing to return failure status via `stc` and `retf 0x2` after a validation call at 0xA1F8 failed (JZ at 0xA16A not taken).
0x00006056 [R4] This `mov dl,al` loads the character byte from the string being printed into DL for INT 21h AH=0x2 output, part of the crash/error dump routine that displays register values character-by-character to the console.
0x00006BC4 [R4] This `int 0x21` with AX=0x4200 (SEEK_SET) repositions the file pointer to the beginning of the file, part of the XMODEM download routine (DOWNLOAD.EXE) that reads a file from the embedded filesystem for firmware transfer.
0x00006F0B [R4] This `mov dl,al` stores the result of a division (computing a screen position or cursor coordinate) into DL, part of a terminal emulator or video driver routine that calculates row/column positions for cursor placement or scrolling.
0x0000A853 [R4] This `int 0x10` with AH=0x0 (set video mode) initializes or resets the display mode based on a byte loaded from a data structure at [BX+0x99], part of the terminal emulator’s routine that restores or applies saved video and NVM configuration parameters from a context block.
0x00006DA1 [R4] This `push cs` instruction is part of a sequence of repeated `push cs` / `pop es` operations that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x000061A4 [R4] This `jz 0x61ad` checks if AL is 0x00 (null character) to branch to a dispatch table lookup at 0x61AD, part of the terminal emulator's VT-style escape sequence or control character handler that maps character codes to jump targets via a table at CS:0x156.
0x0000AC52 [R4] This `push ds` at 0xAC52 is the prologue of a subroutine that sets DS=0x40 (BIOS Data Area), clears byte [0x7A8], and calls 0xAC66 to update or query a terminal emulator status flag (likely related to keyboard or serial input handling), then returns via `retf 0x2`.
0x00009CC0 [R4] This `mov dx,[bx+0x6]` loads the size field from a Memory Control Block (MCB) header at ES:BX+0x6, part of the kernel's DOS-compatible memory arena coalescing routine that merges adjacent free blocks by adding the size of the next block (DX+1) to the current block's size field.
0x0000B859 [R4] This `shl bh,byte 0x5` shifts the high byte of BX left by 5 bits to combine with the low 5 bits of a byte from [0x999], constructing a composite attribute/control byte that is stored into video memory at offsets 0xCC00 and 0xCD00 relative to ES — part of the terminal emulator's cursor position or attribute encoding routine that packs row/column coordinates into a display memory format.
0x0000A2FB [R4] This `pop cx` restores the loop counter after an INT 0x10 teletype output loop (AH=0xE) that printed a string of characters from DS:DX, part of the kernel's system call or interrupt handler that provides a character-string display service (similar to INT 21h AH=9) using BIOS video services.
0x0000B14F [R4] This `loop 0xb149` decrements CL and repeats a bit-test loop that checks each bit of a byte (using AH as a shifting bitmask) to find the position of a set bit in a pixel comparison result — part of the terminal emulator's character rendering routine that determines the horizontal offset of a differing pixel between two font glyphs for cursor or highlight positioning.
0x0000B296 [R4] This `mov dx,0x343` loads the I/O port address for a CRTC or video control register (0x343), which is then written with a value (0x28 ORed with a byte from [0x7b9]) to configure display timing or attribute control bits — part of the terminal emulator's video mode initialization or CRTC register programming sequence.
0x0000B55C [R4] This `mov cx,0x10` sets the count for a `rep movsb` operation that copies 16 bytes of font bitmap data from a source pointer (DS:SI) to a destination (ES:DI), part of the terminal emulator's character generator routine that transfers glyph data for rendering a character row to the display buffer.
0x00006B2B [R4] This `push dx` saves the segment value (from AX) before setting ES to that segment, as part of a routine that reads or writes to the NVM (non-volatile memory) via I/O port 0x343, constructing a control byte (AL) based on flags in DX (bits 0x1000 and 0x2000) to select NVM bank or mode.
0x0000B035 [R4] This `mov [0x2862],ax` stores the result of a subroutine call (0xB1D8) that generates or computes a random number or seed value, part of a sequence that fills a table at 0x285E-0x286A with 16-bit values for use in the terminal emulator's random number generation or initialization routine.
0x00006CEE [R4] This `xor si,si` initializes the source index to zero before a `lodsw`/`loop` checksum computation over 0x8000 bytes (32KB) of memory, part of a ROM integrity verification routine in the self-test that calculates a 16-bit sum of the firmware or font table contents.
0x0000A920 [R4] This `mov di,[bx+0x6]` loads a pointer from a data structure at [0x914] (offset 0x6) into DI, part of the terminal emulator's interrupt handler that checks if a keyboard or serial input buffer entry exists (test di,di) before clearing status bytes at [di+0xA5] and [di+0xA6] during input processing or buffer management.
0x0000A860 [R4] This `int 0x10` with AH=0x1 sets the cursor shape (using CX from the pop) as part of the terminal emulator's routine that restores saved video configuration parameters (cursor position, mode, attributes) from a context block during initialization or mode switching.
0x0000DFCA [R4] This `jc 0xdfd6` is part of a font bitmap or character generator table (the surrounding bytes form repeated 8x8 or 8x16 pixel glyph patterns), so this instruction is misdecoded data — answer DATA.
0x00007371 [R4] This `mov es,[es:0x7d5]` loads the extra segment from a pointer stored in the BIOS Data Area (segment 0x40) at offset 0x7D5, part of an interrupt handler that retrieves a saved segment value to access a data structure for terminal emulator state or interrupt vector management.
0x0000AB9B [R4] This `push dx` saves the DX register (containing a character or cursor position parameter) at the start of a subroutine that checks a configuration flag at [0x7A8] and manipulates a circular buffer pointer at [0x94A] for character input or keyboard buffer handling in the terminal emulator.
0x00006395 [R4] This `jz 0x638c` checks if the character in AL is a carriage return (0x0D), and if so, jumps back to continue reading characters in a loop that filters out line-feed (0x0A) and carriage-return characters, part of a diagnostic or input routine that processes console input while ignoring newline characters.
0x0000B475 [R4] This `add si,cx` advances the source pointer by 16 bytes (CX=0x10) to skip a font glyph row when the high bit of AH is clear, part of the terminal emulator's character rendering routine that conditionally selects alternate glyph data for double-height or double-width character modes.
0x0000B3B8 [R4] This `cmp bx,[0x978]` compares the current cursor position pointer (from BIOS data area offset 0x914) with a saved pointer at [0x978], part of the terminal emulator's interrupt handler that determines whether the cursor has moved and needs to trigger a display update or attribute clear at the old cursor location.
0x0000E056 [R4] This `add [bx+di],ah` is part of a data table (likely a jump table or offset table for escape sequence handlers), as the surrounding bytes show patterns of word-sized offsets (e.g., 0x0D9E, 0x0DBA) that are misdecoded as instructions — answer DATA.
0x0000BC05 [R4] This `mov ah,[bx+0x9b]` loads the attribute byte from a terminal emulator data structure at offset 0x9B (relative to the cursor position pointer at [0x914]), part of a routine that retrieves the current character attribute for cursor positioning or display update operations.
0x00006D0D [R4] This `jz 0x6d27` checks if the keyboard buffer is empty (after INT 16h AH=1), part of a font/glyph table browsing or self-test routine that waits for keyboard input to adjust a font row pointer (SI) by ±0x100 or ±0x200 based on '+'/'-' key presses, with ESC to exit.
0x0000B898 [R4] This instruction stores the low byte of DX (DL) into the cursor attribute or status field at [BX+0x9F], part of the terminal emulator's cursor position update routine that saves the column or attribute byte for the current cursor location before comparing pointers to determine if a display refresh is needed.
0x0000E5B5 [R4] This `ficom` instruction is a misdecoded data byte sequence (part of a font bitmap or character generator table), as the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data — answer DATA.
0x00006FE5 [R4] This `out dx,al` writes 0xC003 to I/O port 0xFF36 (the 80186 interrupt controller's interrupt mask register or control register), part of a routine that enables or configures interrupt sources (likely timer or serial interrupts) during a self-test or initialization sequence that also programs the NVM and timer registers.
0x0000BC83 [R4] This `int 0x16` with AH=0x1 checks the keyboard buffer status (zero flag set if no key available), part of the terminal emulator's screen output or input polling routine that waits for a keypress before continuing to process character output or update the display.
0x0000E6DA [R4] This `adc di,dx` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000B881 [R4] This `xor bx,bx` clears BX to zero before storing a byte at [0x994] and writing it to video memory at [ES:bx-0x3900], part of the terminal emulator's interrupt handler that updates video mode or attribute control registers during display configuration.
0x0000A3EE [R4] This `pop cx` is part of a subroutine epilogue that restores registers (CX, BX, AX) before returning via `retf 0x2` with carry clear, used in the kernel's memory management or file I/O layer to return success status to the caller.
0x0000BBBC [R4] This `mov cl,[0x988]` loads the character width or column count from a configuration variable, part of the terminal emulator's screen scrolling or line-copy routine that uses CX as a count for a `rep movsw` operation to shift rows in the video display buffer.
0x000078D4 [R5] This conditional jump (JZ to 0x78FC) checks if bit 2 of AL is clear, part of the terminal emulator's serial port status byte decoding routine that determines the type of received character or event (e.g., break, parity error, or framing error) before dispatching to the appropriate processing path.
0x0000AA85 [R5] This instruction compares AL to 0x1, checking if a configuration flag at [0x7A8] equals 1 (indicating a specific terminal emulator mode or state), part of a routine that reads timer 1 status from I/O port 0xFF5A and processes modem or timer status bits for serial port handshaking or status reporting.
0x00006846 [R5] This instruction masks SI with DX (AND SI,DX), computing a 16-bit offset within a 0x1000-byte boundary as part of a memory block copy or checksum routine that processes data in 0x1000-byte chunks, likely in the XMODEM download or ROM verification code.
0x00009BBE [R5] This `retf 0x2` instruction returns from a far subroutine with a 2-byte stack pop (removing a return parameter), returning with carry set (STC at 0x9BBD) to indicate an error or failure status in the kernel's memory management or file I/O layer.
0x0000AC62 [R5] This `pop bx` at 0xAC62 restores the BX register as part of the epilogue of a subroutine that clears byte [0x7A8] and calls 0xAC66 to update or query a terminal emulator status flag (likely related to keyboard or serial input handling), returning via `retf 0x2`.
0x00006E91 [R5] This `cmp ax,[bx+0x4]` at 0x6E91 compares the current cursor position or terminal identifier (AX) with a saved value in a data structure at [BX+0x4], part of the terminal emulator's routine that searches through a table of terminal context blocks (each 0xB8 bytes) to find a matching entry for cursor position or display context management.
0x0000BC85 [R5] This `jz 0xbcae` at 0xBC85 checks if the keyboard buffer is empty (after INT 16h AH=1), part of the terminal emulator's screen output or input polling routine that waits for a keypress before continuing to process character output or update the display.
0x0000AF6D [R5] This instruction sets AH=0x12 as part of a cascading comparison chain that selects a divisor or timing constant (AH) based on the value at [0x7b8], which likely encodes a baud rate or refresh rate index for the terminal emulator's CRTC or serial port configuration.
0x0000757D [R5] This `pop ax` restores the AX register after an INT 0x40 call (AH=0), part of the terminal emulator's input buffer management routine that retrieves a character from a circular buffer at [bx+0x90] and checks for special control codes (AH=0x40) before processing the received data.
0x00006107 [R5] This `mov ax,ss` loads the stack segment register into AX as part of a crash/error dump routine that displays register values (SS, then SI) character-by-character to the console via INT 21h AH=2, following a system failure or exception.
0x00006BC4 [R5] This `int 0x21` with AX=0x4200 (SEEK_SET) repositions the file pointer to the beginning of the file, part of the XMODEM download routine (DOWNLOAD.EXE) that reads a file from the embedded filesystem for firmware transfer.
0x0000A669 [R5] This `xor di,di` initializes the destination index to zero before a `call 0xa564` that performs a memory copy or block transfer operation (likely using `rep movsw`), part of the kernel's memory management routine that copies 0x8000-byte segments during a memory block relocation or size adjustment operation.
0x0000998C [R5] This `jmp short 0x999b` is part of the kernel's DOS-compatible memory arena allocation routine, skipping an addition of 0x10 to AX when a flag at [0x872] is non-zero, to adjust the segment value before pushing it for a memory allocation call.
0x00006204 [R5] This `and di,byte +0xf` masks the destination index to keep only the low 4 bits, part of a character output routine that wraps the pointer within a 16-byte buffer segment (ES:DI at 0xC000) for storing a character byte via `stosb` in the terminal emulator's display buffer.
0x00007607 [R5] This `push ds` saves the data segment register before calling a subroutine at 0x760D, part of a routine that loads a byte from [0x7B1] and uses it as an index into a table at ES:0x800 to look up a font or configuration pointer in the terminal emulator's initialization or character mapping code.
0x000079E5 [R5] This instruction sets AL to 0xFF (true) to indicate that the terminal emulator's cursor position has changed or a display update is needed, based on comparing the current cursor position pointer at [bx+0x90] with the saved position at [bx+0x8e] and checking the flag at [bx+0x98] — part of the terminal emulator's interrupt handler that determines whether to trigger a screen refresh.
0x0000E4BC [R5] This instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00007650 [R5] This instruction pops SI to restore the source index register after a subroutine call that processes escape sequence or control character comparisons (comparing AL with AH and calling subroutines at 0x7629 or 0x760D) — part of the terminal emulator's VT-style escape sequence parser that matches character sequences for display attribute or cursor control.
0x0000ACD6 [R5] This instruction outputs the value in AL to I/O port 0xFF34 (the 80186 interrupt controller's interrupt request register), as part of a routine that acknowledges or clears a specific interrupt request (likely a timer or serial interrupt) during interrupt controller initialization or mode-setting sequence.
0x0000B1D8 [R5] This instruction loads DX with I/O port 0xFF5E (the timer 1 control register or timer 1 count register), part of a subroutine that programs the 80186 timer 1 by manipulating its control bits (clearing and setting bit 1) and reading its status register at port 0xFF5A — used for generating timing delays or baud rate clock signals in the terminal emulator's serial communication or display timing routines.
0x000099D9 [R5] This instruction adds 0x10 (16 bytes) to AX, which holds a memory block size or segment value, as part of the kernel's DOS-compatible memory arena allocation routine that adjusts the block size to include the MCB (Memory Control Block) header overhead before storing it at [0x810] and adding it to the total memory counters at [0x824] and [0x82c].
0x0000AEBB [R5] This `test al,0xa7` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x000074CF [R5] This instruction sets CX to 6 as the loop counter for a table search routine that compares BX against a 6-entry word table at [0x7BA] (likely a lookup table for baud rates or configuration parameters), part of the terminal emulator's setup or configuration parsing function that returns carry clear on match.
0x0000772B [R5] This instruction clears bit 3 (0xF7 mask) of the status byte at [0x7F6], part of the terminal emulator's VT-style escape sequence or control character handler that manages serial port handshaking or flow control flags (likely clearing a "break" or "framing error" status bit) before testing for other conditions.
0x0000AE42 [R5] This `ss xchg ax,sp` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000B1CB [R5] This `push bx` saves the BX register before calling a subroutine at 0x768C that uses AL (loaded from [0x973]) as a parameter, part of the terminal emulator's cursor or attribute update routine that adjusts display parameters based on comparison results from [0x970] and [0x971].
0x00006865 [R5] This `add bx,byte +0x8` advances a pointer by 8 bytes within a loop that processes font or glyph bitmap data, part of the terminal emulator's character generator routine that iterates through glyph rows during display rendering or font table traversal.
0x0000BD1C [R5] This `out dx,al` writes the modified value (with bit 3 cleared) to I/O port 0xFF1C (the 80186 interrupt controller's control register), part of a routine that disables or configures a specific interrupt source (likely the timer or serial interrupt) during initialization or mode switching.
0x00009989 [R5] This `jz 0x998e` checks if a byte at [0x872] is zero to conditionally add 0x10 to AX (for segment alignment), part of the kernel's DOS-compatible memory arena allocation routine that adjusts block sizes during memory allocation or executable loading.
0x0000C7F7 [R5] This `or [bx+si],cl` is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 pixel glyph data for characters like 'H' or similar letters), so this is DATA.
0x0000B35A [R5] This instruction compares the byte at [0x97A] (a cursor movement or display update flag) with AL (which is zero from the preceding `xor ax,ax`), part of the terminal emulator's display refresh logic that checks whether the cursor has moved or a screen update is needed before setting attribute/control bits for the video output.
0x0000B84C [R5] This instruction copies BL (the row or column coordinate) into BH, part of the terminal emulator's cursor position encoding routine that converts row/column coordinates into a composite attribute byte stored in video memory at offsets 0xCC00 and 0xCD00, used for cursor positioning and display updates.
0x00006B51 [R5] This instruction pops DX (the saved segment or address value) from the stack, part of the NVM (non-volatile memory) read/write routine that constructs a segment address by masking DX to 12 bits and adding 0xC000, then sets DS to access a specific memory region for ROM verification or checksum operations.
0x0000E74F [R5] This instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000E6C2 [R5] This instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000B7D2 [R5] This `cli` instruction disables interrupts before modifying the CRTC/video control register at I/O port 0x343, part of the terminal emulator's video mode initialization sequence that updates display timing or attribute control bits (combining CH with the masked value from [0x7b9]) to configure the video mode.
0x0000A0F8 [R5] This `jz 0xa12c` checks if the byte at ES:DI (a memory control block or executable header field) is zero, part of the kernel's memory management or executable loading routine that validates a segment or block descriptor before processing allocation or relocation data.
0x00006018 [R5] This instruction loads the current video control register value from memory location [0x7b9] into AL, which is then masked (AND with 0xBF) and output to I/O port 0x343 (CRTC/video control register) — part of the terminal emulator's routine that disables a specific video attribute or mode bit (likely clearing bit 6) during a display mode change or initialization sequence.
0x0000719E [R5] This instruction (`dec sp`) is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000A1BF [R5] This instruction sets DI to 0x18CB, which is used as a destination pointer for comparing characters from a string (loaded via LODSB) against delimiters (colon, backslash, space) — part of the kernel's file path parsing or command-line argument processing routine that validates or tokenizes a path string by skipping delimiter characters.
0x0000687B [R5] This instruction outputs the value in AL (which was masked with the complement of 0xFC from [0x7b9]) to I/O port 0x343 (CRTC/video control register) — part of the terminal emulator's routine that disables video display or blanks the screen (by clearing the lower 2 bits) during a mode switch or initialization sequence, with interrupts disabled (CLI) before the write and re-enabled (STI) after.
0x00006075 [R5] This instruction calls subroutine 0x6139 with AX containing a value from [bp-0x8] (likely a register value or status word) — part of the crash/error dump routine that displays register contents character-by-character to the console, where 0x6139 is a hex-to-ASCII conversion and print function that outputs the 16-bit value in AX as hexadecimal digits.
0x00006A22 [R6] This `lodsw` instruction loads a 16-bit word from the ROM/font region at DS:SI (DS=0xC000) into AX, as part of the ROM integrity verification routine that computes a 16-bit checksum over 0x8000 bytes of firmware or font table contents by subtracting each word from BX in a loop.
0x0000A208 [R6] This `pop si` instruction restores the source index register after pushing it at 0xA202, part of the kernel's executable loading or memory management routine that searches through a table of segment descriptors (starting at 0x8B1, each 8 bytes) to find a free or matching memory block for allocation or relocation.
0x000078D2 [R6] This `test al,0x4` instruction checks bit 2 of AL (which holds a serial port status byte), part of the terminal emulator's serial port status decoding routine that determines the type of received character or event (e.g., break, parity error, or framing error) before dispatching to the appropriate processing path.
0x0000FE7A [R6] This `mov ax,0x8c` instruction loads the value 0x8C into AX to be written to I/O port 0xFFB2, part of the boot initialization sequence that programs external peripheral chip-select registers (likely configuring a UART or CRTC control register) during the early hardware setup before jumping to the resident firmware kernel.
0x0000B7A2 [R6] This `add al,0x3` adjusts the computed column position (derived from row/column arithmetic) by adding 3, part of the terminal emulator's cursor positioning routine that calculates the hardware cursor address for the CRTC, where the final value is output via `call 0xb7fc` to set the cursor location register.
0x00007972 [R6] This `cli` disables interrupts before updating the saved cursor position pointer (DI) and segment (ES) stored in the terminal emulator's data structure at [BX+0x92] and [BX+0x94], part of the interrupt handler that saves the current cursor context during a task switch or context save operation, ensuring atomic update of the pointer pair.
0x0000ADA3 [R6] This `dec bp` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000E7E3 [R6] This `adc sp,dx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000A7E5 [R6] This `mov [bx+0x2],sp` stores the current stack pointer (SP) into the terminal emulator's context block at offset 0x2, part of the interrupt handler that saves the stack pointer for the current terminal context (identified by BX pointing to a 0xB8-byte context block) before switching to another context or processing input/output.
0x0000708D [R6] This `stosw` stores the value 0x0DF2 (from AX) into the destination at ES:DI=0x84, part of a routine that initializes a table of word-sized constants (likely font glyph offsets or configuration parameters) in the terminal emulator's data area during startup or mode initialization.
0x00006466 [R6] This `lea si,[bx+0xab]` loads the address of a 12-byte string field (at offset 0xAB in a data structure pointed to by BX) into SI, part of the crash/error dump routine that displays a device name or identification string character-by-character via INT 21h AH=2 after printing register values.
0x0000A81A [R6] This `mov sp,[bx+0x2]` loads the stack pointer from offset 0x2 of a terminal context block (pointed to by BX), part of the terminal emulator's task-switching or context-switch routine that restores the stack segment and stack pointer to switch between multiple terminal sessions or display contexts.
0x0000DE21 [R6] This `pop es` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000636C [R6] This `cmp al,0x20` checks if the input character is a space or control character (values below 0x20), part of a console input routine that filters printable characters for storage in a buffer at ES:DI (starting at 0x167) while echoing them via INT 10h AH=0Eh, with control characters triggering buffer termination.
0x00006AF9 [R6] This `pop ds` restores the data segment after a series of calls to 0x6B88 that compute checksums over 0x8000-byte blocks of memory (0x8000, 0x9000, 0xA000, 0xB000), accumulating the result in DX as part of the ROM integrity verification routine in the self-test.
0x00006FEB [R6] This `mov al,0x44` writes the value 0x44 to I/O port 0x343 (the CRTC/video control register), part of the terminal emulator's video mode initialization or blanking routine that sets a specific display control byte (likely enabling or configuring video output timing).
0x0000E810 [R6] This `add [bx+si],ax` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00009A3D [R6] This `mov es,[0x80c]` loads the extra segment from a saved segment value at [0x80C] (part of the kernel's memory arena data structure), before zeroing 0x80 words at ES:DI and writing an MCB (Memory Control Block) header with signature 0x20CD and size fields — part of the DOS-compatible memory allocation routine.
0x00009C54 [R6] This `mov [si+0x6],ax` stores the current block size (AX) into the memory control block's size field at offset 0x6, as part of the kernel's memory arena splitting routine that divides a free block into allocated and remaining portions during memory allocation.
0x00006B30 [R6] This `test dx,0x1000` checks bit 12 of DX (which holds a video attribute or mode parameter) to conditionally set bit 0 of AL (via `or al,0x1`), part of the terminal emulator's routine that constructs a composite video control byte from attribute flags before writing it to the CRTC control port 0x343 at [0x7b9].
0x00006515 [R6] This `mov byte [0x985],0x1` sets a flag at [0x985] to 1, part of the terminal emulator's configuration or mode-switching routine that toggles a display attribute or video mode setting (likely related to cursor or screen update behavior) after printing a status string via INT 21h.
0x0000CA38 [R6] This `clc` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated 0xF8 and 0xFF patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000B4F8 [R6] This `push bp` saves the base pointer (BP) containing a pointer to a lookup table or data structure, part of the terminal emulator's character rendering or escape sequence handler that uses a table-driven dispatch (comparing AL with CL and calling 0xB4B1) to process control characters or display attributes.
0x0000E7F1 [R6] This `push es` instruction is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000ACBE [R6] This `xchg ax,[0x964]` atomically exchanges AX with a semaphore or flag at [0x964] (with interrupts disabled via CLI), part of a critical-section or mutual-exclusion mechanism in the terminal emulator's interrupt handler or serial port status processing routine.
0x0000B6C5 [R6] This `xchg al,ah` swaps the high and low bytes of AX (which holds the value 0x0040 from DI), converting a little-endian column/row coordinate into a big-endian format before storing it into video memory at ES:BX, part of the terminal emulator's cursor position or attribute encoding routine for display updates.
0x00009A08 [R6] This `jns 0x9a1f` checks the sign flag after `dec dx` (which sets DX to 0xFFFF), part of the kernel's DOS-compatible memory arena allocation or block management routine that conditionally processes a memory control block or segment descriptor based on whether DX underflowed (indicating a boundary condition or end-of-list marker).
0x00006627 [R6] This `mov [0x7b9],al` stores the modified video control register value (with bits 0-2 manipulated to select a refresh rate or display mode) into the configuration variable at [0x7b9], part of the terminal emulator's routine that changes the CRTC/video refresh rate (60 Hz vs 72 Hz) and displays the corresponding mode string via INT 21h.
0x0000E317 [R6] This `mov [0x8ff],dl` stores a byte (likely a font glyph row or attribute value) into a data table at offset 0x8FF, part of the terminal emulator's character generator or font bitmap processing routine that constructs or modifies glyph data for display rendering.
0x00006BB1 [R6] This `mov ax,0x4202` sets up an INT 21h function 0x42 (LSEEK) with method 2 (seek from end of file) to determine the file size, part of the kernel's file I/O or executable loading routine that calculates the total size of a file before reading or allocating memory for it.
0x000064D2 [R6] This `jmp 0x642e` is an unconditional jump back to the start of a loop that prints a string of spaces (via INT 10h) followed by a prompt string (via INT 21h AH=9), part of a diagnostic or self-test routine that clears a line of text on the screen and prompts for user input.
0x000098EB [R6] This `mov di,0x40` sets DI to segment 0x40 (the BIOS Data Area) before loading DS and ES with that segment, part of the kernel's executable loading routine that accesses BIOS data area variables (such as the equipment list or video mode) during initialization or memory configuration.
0x0000B828 [R6] This `cmp ah,0x20` checks if the high byte of the cursor position or attribute value equals 0x20 (space character), part of the terminal emulator's cursor positioning or display update routine that determines whether the current character is a space to handle special cursor movement or attribute logic.
0x0000B5E7 [R6] This `jz 0xb5fd` checks if the byte in DL equals 0xFF, which is a terminator or sentinel value in a run-length encoded font glyph or character width table, part of the terminal emulator's character rendering routine that decodes compressed glyph data for display output.
0x0000BC10 [R6] This `pop ds` restores the data segment register from the stack as part of the terminal emulator's interrupt handler epilogue, returning from an interrupt service routine (via `iret`) that processes cursor position or attribute updates for the video display.
0x0000AD0A [R6] This `jz 0xad06` creates a polling loop that repeatedly reads the 80186 interrupt controller's interrupt mask register at I/O port 0xFF36, waiting for bit 12 (0x1000) to become set, part of a routine that synchronizes with a timer or serial interrupt before configuring the interrupt controller with a new mask value (0xD007) and enabling interrupts.
0x0000AFF9 [R6] This `add word [0x968],byte +0x1` increments a 32-bit counter at [0x968-0x96A] by 1, part of the terminal emulator's timer tick or serial port interrupt handler that tracks elapsed time or character counts for baud rate timing or display refresh scheduling.
0x00006BB9 [R6] This `xor dx,dx` clears DX to zero before setting up a file seek operation (INT 21h AH=0x4200 with CX:DX=0), part of the kernel's file I/O routine that seeks to the beginning of a file after determining its size (via AH=0x4202), in preparation for reading the entire file into memory for executable loading or configuration processing.
0x0000A918 [R6] This `cmp bx,[0x916]` compares the current cursor position pointer (loaded from [0x914]) with a saved pointer at [0x916], part of the terminal emulator's interrupt handler that checks if the cursor has moved to determine whether a display update or attribute clear is needed.
0x00006534 [R6] This `int 0x21` with AH=0x9 prints a $-terminated string at address DX (0x281) to the console, part of the terminal emulator's setup or configuration routine that displays a menu or status message.
0x00009A7F [R6] This `mov al,0xcb` writes the opcode for a `retf` instruction (0xCB) into the interrupt vector table entry being constructed at ES:DI=0x50 (INT 0x14), as part of the kernel's initialization that sets up a dummy interrupt handler for serial port interrupts.
0x000078B1 [R6] This `jz 0x78bb` checks if the lower 2 bits of AL are zero (test al,0x3), part of the terminal emulator's serial port status byte decoding routine that selects a baud rate divisor table entry (BX pointing to 0x124A or 0x144A) based on the received character's status bits.
0x0000AF27 [R6] This `stosw` stores AX (containing 0x4FEE and CS) into the interrupt vector table at ES:DI=0x4C (INT 0x13), as part of the kernel's initialization that installs a custom disk interrupt handler for the terminal emulator's file system access.
0x000068AA [R6] This `cmp di,0x782` checks if the destination index has reached the end of a 0x782-byte input buffer (starting at 0xC000), part of a keyboard input routine that limits character entry to the buffer size before wrapping or terminating the input line.
0x0000BB3B [R7] This `jmp short 0xbb7a` is part of the terminal emulator's display output routine that handles line wrapping or scrolling when the cursor column (DX) reaches the maximum column count stored at [0x989], jumping to the code that processes the newline/carriage return and updates the cursor position stored at [0x9ba].
0x00006179 [R7] This `add al,0x96` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000AA11 [R7] This `out dx,al` writes the value 0x80 to I/O port 0xFF02 (the 80186 interrupt controller's interrupt request register), part of the timer interrupt handler that acknowledges and clears a timer interrupt by writing to the end-of-interrupt (EOI) register before returning via IRET.
0x0000B464 [R7] This `and al,0xf0` masks the lower nibble of the byte at [0x995] (a video attribute or mode control register), part of the terminal emulator's font selection routine that combines the masked value with a bit from AH (shifted right) to select between two character generator font tables (at 0x524A or 0x524A+0x10) for display rendering.
0x00009A7F [R7] This `mov al,0xcb` loads the opcode for a `retf` instruction (0xCB) into AL, which is then stored into the interrupt vector table entry at ES:DI=0x50 (INT 0x14), as part of the kernel's initialization that sets up a dummy interrupt handler for serial port interrupts.
0x0000A58E [R7] This `push si` saves the source index register at the start of a memory block copy routine (part of the kernel's executable loading or memory management) that copies 0x1000-byte segments from one location to another using `rep movsw`, handling large allocations by iterating through 0x8000-byte chunks.
0x0000C779 [R7] This `inc dx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00007722 [R7] This `jmp short 0x76b5` is an unconditional jump back to the main dispatch loop of the terminal emulator's escape sequence parser, after processing a control sequence that clears specific bits in the attribute flags at [0x7f6] and [0x7f5] and calls subroutine 0x7935 (likely a display update or attribute application routine).
0x0000B4FE [R7] This `jnz 0xb502` checks if the current character (AL) matches the target character (CL) during a table-driven dispatch in the terminal emulator's character rendering or escape sequence handler, where a match sets CH to 1 to indicate a successful comparison before calling subroutine 0xB4B1 to process the matched entry.
0x00007117 [R7] This `int 0x21` with AH=0x9 prints a $-terminated string at address DS:DX to the console, part of the terminal emulator's initialization or configuration routine that displays a status message or prompt after setting a flag at [0x986] to 1.
0x0000BD9C [R7] This `push si` saves the source index register before a block copy operation (rep movsw) that copies data from [0x9AD] to video memory at 0xD000, part of the terminal emulator's display buffer update routine that transfers font or glyph data for screen rendering.
0x0000A435 [R7] This `jc 0xa48e` (jump if carry) checks if the loaded character (AL) is a control character (below 0x20), part of the kernel's file path parsing routine that skips or terminates on control characters while processing command-line arguments or path strings.
0x00007959 [R7] This `call 0x7862` invokes a subroutine that processes serial port status or received data, part of the terminal emulator's interrupt handler (likely INT 0x14 or serial interrupt) that decodes incoming serial characters and updates terminal state.
0x00009977 [R7] This `rcr ax,1` performs a rotate-right-through-carry as part of a 32-bit division by 16 (shifting DX:AX right by 4 bits), part of the kernel's memory arena allocation routine that converts a byte count into a paragraph count for memory block size calculations.
0x0000B23E [R7] This sequence of repeated `pusha` and `push si` instructions (with no meaningful preceding setup) is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00006D23 [R7] This `add si,0x100` adjusts the font table pointer by 0x100 bytes (256 bytes) when the '+' key is pressed, part of the terminal emulator's character generator or font selection routine that scrolls through font tables or glyph sets in response to keyboard input.
0x0000FED8 [R7] This `mov dx,0xff18` selects the 80186 interrupt controller's interrupt request register (IRR) at I/O port 0xFF18, part of the boot initialization sequence that programs the interrupt controller's priority and mask registers before jumping to the resident firmware kernel.
0x0000BA7A [R7] This `mov [0x9a5],ax` stores the doubled column coordinate (from CL) into a video timing or cursor position variable at [0x9A5], part of the terminal emulator's CRTC cursor positioning or display timing calculation routine that computes hardware cursor addresses for the video controller.
0x0000E67B [R7] This `adc [bx+si],dh` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00007833 [R7] This `inc bx` advances the buffer pointer by 2 bytes (after storing DX and AX) within a circular buffer management routine, part of the terminal emulator's serial port interrupt handler that stores received character data and status words into a ring buffer for later processing.
0x0000749D [R7] This `and al,[0x7b9]` masks AL (0x7F) with the current video control register value at [0x7b9], part of the terminal emulator's CRTC/video control routine that clears bit 7 (disable video or blank screen) while preserving other bits before writing to I/O port 0x343.
0x00009C13 [R7] This `mov bx,[si+0x6]` loads the block size from a memory control block (MCB) field at offset 0x6, part of the kernel's DOS-compatible memory arena allocation routine that retrieves the size of a free block during memory splitting or allocation validation.
0x00006B63 [R7] This `xor di,di` zeroes the destination index register before calling subroutine 0xA564, part of the ROM integrity verification routine that computes a checksum over a 0x8000-byte memory block (starting at DS:0) to validate firmware or font table contents.
0x000099FF [R7] This `jc 0x99a9` checks the carry flag after calling 0x9B2B (a memory allocation or block validation subroutine), part of the kernel's executable loading routine that detects allocation failure and jumps to error handling if the carry flag is set.
0x0000B069 [R7] This `jnz 0xafec` checks if the byte read from the serial port (compared against [0x7f4]) does not match the expected configuration byte, part of the terminal emulator's serial port initialization or handshake routine that retries or aborts if the received configuration value is incorrect.
0x00006366 [R7] This `mov di,0x167` sets the destination index to offset 0x167, which is the start of a console input buffer, as part of the terminal emulator's keyboard input routine that stores printable characters (filtered by the `cmp al,0x20` check at 0x636C) into the buffer for command-line or configuration entry.
0x0000B011 [R7] This `inc byte [0x972]` increments a counter or flag at memory location 0x972, part of the terminal emulator's timer tick or serial port interrupt handler that tracks elapsed time or character counts for baud rate timing or display refresh scheduling.
0x000063E4 [R7] This `pop di` restores the destination index register from the stack, part of the terminal emulator's console input buffer management routine that fills the remaining buffer space (from DI to the end) with 0x1A (Ctrl-Z) characters after a line has been entered or terminated.
0x0000BB0B [R7] This instruction loads a word from the extra segment at offset 0x30 (the CRTC cursor address register value) into BX, part of the terminal emulator's display update routine that retrieves the hardware cursor position for calculating where to write a character attribute byte in video memory.
0x0000607B [R7] This `int 0x21` with AH=0x9 prints a $-terminated string at address DX (0xDB) to the console, part of the crash/error dump routine that displays a label or separator string (likely "DS=" or similar) between register value outputs during the diagnostic register display sequence.
0x00006102 [R7] This `int 0x21` with AH=0x9 prints a $-terminated string at address DX (0x12C) to the console, part of the crash/error dump routine that displays a label string (likely "SS:SP=" or similar) before outputting the stack segment and pointer register values during the diagnostic register dump.
0x000077B5 [R7] This conditional jump checks if bit 2 of the byte at [0x7F5] is set (testing for a specific serial port status or modem control flag), part of the terminal emulator's serial port interrupt or status processing routine that conditionally skips setting or clearing control bits for the UART's modem control register.
0x000079F4 [R7] This `mov bx,[0x914]` loads the current terminal context block pointer from memory location [0x914] (a saved cursor/context pointer), part of the terminal emulator's keyboard interrupt handler that checks a flag at [bx+0x98] to determine whether to process a pending keyboard input or return from interrupt.
0x0000A833 [R7] This `mov [0x978],ax` stores the new terminal context block address (from AX) into the global context pointer at [0x978], part of the terminal emulator's context-switching routine that switches between multiple terminal sessions by updating the active context pointer and restoring cursor position/attributes via INT 10h.
0x00006DCB [R7] This `inc bx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000BAE1 [R7] This `jz 0xbb5f` checks if the input character (AL) is a backspace (0x08), part of the terminal emulator's keyboard input processing routine that dispatches control characters (backspace, carriage return, bell, linefeed) to their respective handlers before storing printable characters into the display buffer at ES:DI.
0x00006E6B [R7] This `iret` instruction returns from an interrupt service routine, part of the terminal emulator's interrupt handler that fetches an interrupt vector from the IVT (using AL as the interrupt number) and performs a far jump to the handler, with this specific path being the return from a dummy or default interrupt handler.
0x000098A8 [R7] This `push di` saves the destination index register as part of the kernel's DOS-compatible memory arena allocation or executable loading routine, preserving context before calling subroutine 0xA6E6 to process memory control blocks or segment descriptors.
0x0000B539 [R7] This `pop si` restores the source index register after processing run-length encoded font glyph or character width data, part of the terminal emulator's character rendering routine that decodes compressed glyph bitmaps for display output.
0x0000A047 [R7] This `cli` disables interrupts before writing a modified value (with bits 2 and 3 set) to I/O port 0x341 (a CRTC or video control register) and updating the shadow register at [0x89A], part of the terminal emulator's routine that configures video timing or display attributes during a mode change or initialization sequence.
0x0000F200 [R7] This instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000AEAA [R7] This instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x000065F7 [R7] This `mov ax,0xc000` loads a base segment address (0xC000) which is then added to a byte from [0x53b] (a font row or character index) and output to I/O port 0xFF88 (the CRTC character generator or font data register), part of the terminal emulator's font/glyph selection routine that selects a specific character row or font page for display rendering.
0x0000A03B [R7] This `or bh,0x80` sets bit 7 of BH (a flags or attribute byte) based on testing AL for bit 5 (0x20) being set, part of the terminal emulator's keyboard or serial input decoding routine that maps received character status bits (from a call to 0xF600:0x4B49) into display attributes or control flags for the video output.
0x0000705F [R7] This `loop 0x7058` decrements CX (initialized to 0x100 = 256) and repeats the `stosw` pair that writes interrupt vector entries (segment:offset = CS:0x0004) into the interrupt vector table at ES:DI=0x0000, part of the kernel's initialization routine that installs a default interrupt handler (pointing to address 0x0004 within the current code segment) for all 256 interrupt vectors.
0x0000E5D6 [R7] This `adc ax,0x15a8` is part of a sequence of bytes that form a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x15, 0xA8, 0xAC, 0x0D, 0x0A), so this is DATA.
0x00009DB0 [R7] This `add dx,byte +0x2` adds 2 to DX (which holds the total size of a memory block being merged), as part of the kernel's memory arena coalescing routine that adjusts the combined block size to account for the 2-byte memory control block header when merging adjacent free blocks during memory deallocation or arena compaction.
0x0000ADAD [R8] This `pop di` instruction is part of the terminal emulator's interrupt handler epilogue, restoring the destination index register from the stack before returning from an interrupt service routine via `iret`, likely after processing serial port or timer interrupt context.
0x00007555 [R8] This `cmp al,0x1` instruction checks if the lower 2 bits of the serial port status byte (masked with 0x3) equal 1, part of the terminal emulator's serial port status decoding routine that selects between different baud rate divisor tables or UART configuration values based on the received character's status bits.
0x0000B7FB [R8] This `iret` instruction returns from an interrupt service routine, part of the terminal emulator's interrupt handler that processes cursor position or display updates, restoring the saved registers (DS, BX, AX, SI, CX, DX, DI, ES) from the stack before returning to the interrupted code.
0x00006717 [R8] This `ret` instruction returns from a subroutine that reads 16 bytes from I/O port 0x3A8 (likely a UART or CRTC register) and displays them via INT 21h AH=0x9, part of the terminal emulator's diagnostic or self-test routine that dumps hardware register contents for debugging.
0x00006A91 [R8] This `call 0x67c8` instruction invokes a subroutine that performs a ROM checksum or integrity verification over a memory block, part of the manufacturing self-test (MFGTEST) routine that validates firmware contents after prompting the user for confirmation (Y/N response at 0x6A76-0x6A78).
0x0000B3FA [R8] This `cmp al,0x1` checks if the keyboard input character equals 0x01 (Ctrl-A), part of the terminal emulator's keyboard input dispatch routine that selects between setting or clearing a flag at [bx+0xa4] (likely a terminal attribute or mode control flag) based on the received control character.
0x00006887 [R8] This `push ds` saves the data segment register before setting ES=DS and calling INT 21h AH=0x9 to print a $-terminated string at address 0x67B, part of the terminal emulator's setup or configuration routine that displays a prompt and waits for keyboard input via INT 16h.
0x00009D5F [R8] This `xor si,si` zeroes the source index register before comparing a word at [si] against 0x434C ("CL"), part of the kernel's memory arena scanning routine that searches for a memory control block (MCB) signature to validate or traverse the DOS-compatible memory allocation chain.
0x00006C7E [R8] This converts a nibble (0-15) to its ASCII hexadecimal character by adding 0x30 ('0'), with an additional 0x7 added for values above 9 (A-F), as part of the crash/error dump routine's hex digit conversion for displaying register values.
0x0000BDB4 [R8] This stores AX (the last word loaded from the source buffer) into the video memory destination at ES:DI, completing the transfer of font/glyph data from the character generator table at [0x9AD] to video memory at segment 0xD000 for display rendering.
0x0000BB75 [R8] This calls INT 0x40 with AH=0x2, CX=0x3E8 (1000), and DX=0x7D (125), which is a delay or timing function (likely a millisecond delay or timer wait) used in the terminal emulator's keyboard input processing routine before retrying a character read.
0x00006A2E [R8] This tests bit 0 of AH (which was toggled by the preceding `xor ah,0x1`) to check if the current checksum calculation pass is the first or second iteration, part of the ROM integrity verification routine that computes a 16-bit checksum over the firmware using alternating XOR patterns.
0x0000A180 [R8] This `xor ax,ax` clears AX to zero as part of a loop that searches through a table of terminal context blocks (starting at 0x164A, with 0xA-byte entries up to 0x18CA) for a free slot where the context pointer at offset +8 is zero, part of the terminal emulator's context-switching or session management routine that allocates a new terminal context block.
0x00006D0F [R8] This `mov ah,0x0` followed by `int 0x16` reads a key from the keyboard buffer (BIOS keyboard function 0), part of the terminal emulator's font selection or character generator scrolling routine that responds to '+' and '-' key presses to adjust the font table pointer (SI) by ±0x100 or ±0x200 bytes for scrolling through font tables.
0x0000BC5E [R8] This `mov ah,0x2` sets up BIOS INT 10h function 02h (set cursor position) with the cursor row (DH=0x18=24) and column from [0x9BA], part of the terminal emulator's display update routine that positions the hardware cursor to the bottom row of the screen before writing attribute or character data.
0x000068AA [R8] This `cmp di,0x782` checks if the destination index has reached the end of a 0x782-byte input buffer (starting at 0xC000), part of a keyboard input routine that limits character entry to the buffer size before wrapping or terminating the input line.
0x00007107 [R8] This `call 0xa69d` invokes a subroutine that compares a string at DS:DX (pointing to 0x7DB) against a stored string or configuration value, part of the terminal emulator's initialization or configuration routine that checks for a specific host or terminal identification string before setting a flag at [0x986] and printing a status message via INT 21h AH=9.
0x00009EDE [R8] This `call 0x6c0e` is part of the kernel's memory arena diagnostic routine that prints the size of a memory control block (loaded from `[es:bx+0x6]`) as a hexadecimal value, used during memory allocation validation or error reporting.
0x00006917 [R8] This `mov si,0x771` sets up the source pointer to copy 18 bytes (0x12) of configuration or identification data (likely a terminal name or version string) into a buffer at DI=0x12, part of the terminal emulator's initialization routine that populates setup or status fields.
0x0000B843 [R8] This `jmp short 0xb881` is an unconditional branch in the terminal emulator's cursor positioning or attribute calculation routine, taken when the high byte of the cursor position (AH) equals 0x20 (space), bypassing the normal attribute adjustment logic to directly update the video control register at [0x994].
0x0000674C [R8] This `sub ax,cx` calculates the number of remaining characters to display (0x39 minus the loop counter CX) for a progress indicator or countdown display, part of the terminal emulator's diagnostic or self-test routine that outputs a numeric value via INT 10h function 0x0E (teletype output).
0x00009E92 [R8] This `cmp bx,[es:si+0x8]` compares a block handle or segment value (BX) against the owner field of a memory control block at offset 0x8, part of the kernel's memory arena deallocation routine that searches for the MCB matching the block being freed before calling INT 21h AH=0x49 (release memory).
0x000066DC [R8] This `call 0x62e4` is part of a delay or synchronization routine within the self-test sequence that reads and modifies the parallel printer port (I/O 0x350) control register, applying a mask (0x3F) to clear specific bits before calling the delay subroutine again to allow the port to stabilize before reading back the data register values for comparison.
0x0000725F [R8] This `jnc 0x726a` checks the carry flag after calling subroutine 0x71EA (which attempts to load or allocate a memory segment from the kernel’s DOS-compatible memory arena), and if no error occurred (carry clear), proceeds to load the segment value from [0x7D5] into ES for further processing of the loaded executable’s header or relocation table.
0x0000A8B1 [R8] This `mov dx,bx` sets DX to the base address (BX = 0x18DE) of a terminal context block that was found to have a non-zero flag at [bx+0xA7] and a matching byte at [bx+0xA9] with DL, as part of the terminal emulator’s context-switching or session lookup routine that identifies the active terminal session by scanning a table of context blocks.
0x0000BB97 [R8] This `rep stosw` fills a 0x28-word (80-byte) buffer at ES:DI (pointing to the screen row buffer at [0x9B8]) with the value 0x2020 (two space characters), part of the terminal emulator’s display update routine that clears a line of text in the local display buffer before copying it to video memory at 0xD000 for screen rendering.
0x0000740F [R8] This `mov [0x7af],al` stores the byte at [ES:SI+0x15] (a video timing or CRTC register parameter from a configuration table) into memory location [0x7AF], part of the terminal emulator’s CRTC initialization routine that loads a set of horizontal/vertical timing parameters from a table into global variables for programming the video controller registers.
0x00007013 [R8] This `mov bh,bl` copies the low byte of BX (the current character or index) into BH, part of the terminal emulator's font/glyph rendering routine that constructs a character attribute or font pointer by combining the character code with a base address for display output.
0x0000AB8B [R8] This `retf` returns from a far call with carry flag set (stc at 0xAB8A), part of the terminal emulator's circular buffer or queue management routine that signals a full or empty condition when the buffer pointer at [0x946] matches the current pointer in BX, indicating no data available.
0x0000988F [R8] This `pop cx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x59, 0x00, 0x19, 0x00, 0x00, 0x15, 0xCC, 0x79), so this is DATA.
0x00006E18 [R8] This `jz 0x6e20` checks if the loaded character (AL) equals 0x24 (the '$' string terminator), part of the terminal emulator's INT 10h-based string output routine that prints characters via teletype output (AH=0x0E) until the dollar sign terminator is encountered.
0x000099A1 [R8] This `jnc 0x99ac` checks if the allocated memory block size (AX) is greater than or equal to the requested size (BX), part of the kernel's DOS-compatible memory arena allocation routine that validates whether a free memory block is large enough to satisfy the allocation request, jumping to success handling if sufficient.
0x0000B4EB [R8] This instruction loads a word from a table at [bx+si+0x2] into BX, part of the terminal emulator's character rendering or escape sequence dispatch routine that retrieves a pointer to a run-length encoded font glyph or character width entry from a compressed data table for display output.
0x00006D97 [R8] This `push cs` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000693C [R8] This `mov ax,dx` transfers the accumulated result (from calls with AL=4 through 7) into AX before calling subroutine 0x6C0E, part of the ROM integrity verification or self-test routine that computes a checksum over memory blocks (likely the font tables or firmware regions) to validate ROM contents.
0x000068BC [R8] This `cmp di,0x783` checks if the destination index has reached the end of a 0x783-byte input buffer (starting at 0xC000), part of a keyboard input routine that limits character entry to the buffer size before wrapping or terminating the input line.
0x00009C1E [R8] This `mov ax,0x8` loads the value 8 into AX as an error code or return value, part of the kernel's DOS-compatible memory arena allocation routine that sets up for a return with carry flag set (via `stc`) to indicate an allocation failure or invalid memory control block.
0x0000FEF3 [R8] This `out dx,al` writes the value 0x0E to I/O port 0xFF12 (the 80186 interrupt controller's interrupt mask register), part of the boot initialization sequence that programs the interrupt controller's priority and mask registers before jumping to the resident firmware kernel at 0xF600:0x0000.
0x0000BCD6 [R8] This `stosb` stores the ASCII character in AL (converted from a nibble value via the hex-to-ASCII conversion routine at 0xBCC8) into the destination buffer at ES:DI, part of the terminal emulator's display or debug routine that converts binary values to hexadecimal ASCII strings for screen output.
0x0000AC4E [R8] This `pop bx` restores the BX register from the stack as part of the epilogue of a subroutine that handles serial port or terminal configuration, returning via `retf 0x2` after processing a configuration command that sets byte [0x7A8] to 0 and calls subroutine 0xAB6D.
0x0000B368 [R8] This `or al,ah` combines the video attribute/mode byte from [0x98f] (masked to 0x7F) with the high byte from AH, part of the terminal emulator's video attribute update routine that sets or clears bit 7 (blink/intensity flag) before writing the attribute to video memory at 0xD000.
0x0000AA3A [R8] This `jnz 0xaa48` checks if the byte read from I/O port 0xFF68 (timer 2 count register) does not match either the expected value at [0x7b6] or [0x7b5], part of the terminal emulator's timer-based baud rate or display refresh synchronization routine that detects timing mismatches and falls through to store the value in a ring buffer at [0x946].
0x0000A28E [R8] This `mov ax,[es:di+0xc]` loads the upper 16 bits of a memory block's size from a memory control block (MCB) structure at ES:DI offset 0xC, part of the kernel's DOS-compatible memory arena allocation routine that retrieves the total block size for comparison during memory splitting or coalescing operations.
0x0000B918 [R9] This `mov ax,[es:di]` loads a word from the video display buffer (segment ES, likely 0xD000) at the calculated cursor position offset (DI), part of the terminal emulator's display update routine that retrieves the current character and attribute at the cursor location before clearing the line and writing spaces for a new row of text.
0x00009D4F [R9] This `pop dx` restores the DX register from the stack as part of the kernel's DOS-compatible memory arena deallocation or error-handling routine, popping saved registers before setting the carry flag and returning with an error code (0x9) when the ES segment is zero (indicating an invalid memory control block or allocation failure).
0x0000E75E [R9] This `rcr byte [di],byte 0xc6` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1D, 0xC6, 0x03, 0xCE), so this is DATA.
0x0000E67F [R9] This `adc [bx+si],dx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x10, 0x11, 0x11, 0x11, 0x64, 0x11, 0xDE, 0x12), so this is DATA.
0x0000A71B [R9] This `rep stosb` fills the remaining bytes of a 12-byte buffer at ES:DI (pointing to [bx+0xAB]) with zeros (AL=0 from the preceding `test al,al`/`jz` path), part of the terminal emulator's context block initialization routine that pads the terminal name or identification string buffer with null terminators after copying a string from the DS segment.
0x00009F72 [R9] This `inc ax` counts the number of non-zero bytes copied from a string or data buffer at [si] during a loop (controlled by CX), part of the kernel's string length calculation or memory scanning routine that determines the length of a null-terminated or zero-terminated string for subsequent processing.
0x0000B1D4 [R9] This `pop bx` restores the BX register after a call to subroutine 0x768C (which likely processes a timer or serial port timing value), part of the terminal emulator's timer tick or baud rate synchronization routine that compares accumulated time values at [0x970]-[0x972] and updates a timing flag at [0x975] before returning to the main timing loop.
0x00006D1D [R9] This `jnz 0x6d27` checks if the keyboard input character is neither '+' (0x2B) nor '-' (0x2D), part of the terminal emulator's font table scrolling routine that adjusts the font pointer (SI) by ±0x200 or +0x100 bytes in response to '+' and '-' key presses for browsing through character generator font tables.
0x000061CC [R9] This `mov ax,0xe5e` loads the value 0x0E5E (which encodes a cursor row/column position or video page) for INT 10h function 0x0E (teletype output), part of the terminal emulator's diagnostic or error display routine that outputs a character (AL+0x40) to the screen after incrementing a counter at [0x130] for tracking repeated character sequences.
0x0000E55C [R9] This `sbb bh,[bp+si+0x16]` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1A, 0x7A, 0x16, 0x6C, 0x16, 0xDC, 0x16, 0xB8, 0x18), so this is DATA.
0x0000AD06 [R9] This instruction reads back the interrupt controller's in-service register (IRR) at I/O 0xFF36 to poll for a specific interrupt bit (bit 12, the timer 2 interrupt), part of the terminal emulator's timer synchronization or baud-rate timing routine that waits for a timer event before enabling interrupts and returning.
0x0000E38A [R9] This instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000672C [R9] This conditional jump (carry) loops back to process the next entry in a table of CRTC or UART register values (starting at 0x8B1, with 8-byte entries up to 0x8F1), part of the terminal emulator's diagnostic or self-test routine that dumps hardware register contents for debugging.
0x00009FF9 [R9] This `pop bx` restores the BX register from the stack as part of the epilogue of an interrupt service routine (followed by `iret`), part of the terminal emulator's serial port or keyboard interrupt handler that reconstructs the register state before returning from the interrupt.
0x0000B440 [R9] This `push cx` saves the CX register on the stack as part of the prologue of a keyboard or serial input interrupt handler that prepares to update video memory at segment 0xD000, part of the terminal emulator's character display routine that writes the received character's attribute byte to the video buffer.
0x0000B171 [R9] This `jnz 0xb179` checks a flag at [si-0x1] to decide whether to clear or set a byte at [di-0x1], part of the terminal emulator's character attribute or display update routine that conditionally applies video attributes (such as underline, reverse video, or blink) to a character position in the screen buffer based on a stored attribute flag.
0x000073E8 [R9] This `mov al,[es:si+0x10]` loads a CRTC timing parameter (the 17th byte of a video configuration table) and stores it at [0x7b6], part of the terminal emulator's CRTC initialization routine that copies a set of horizontal/vertical timing and register parameters from a configuration table into global variables for programming the video controller.
0x000099C8 [R9] This `mov [0x80c],ax` stores the allocated memory block's segment address (AX) into the kernel's memory arena control structure at [0x80c], part of the DOS-compatible memory allocation routine that records the base segment of a newly allocated block after successful arena scanning and size validation.
0x0000A697 [R9] This `mov ds,dx` sets the data segment register to the value in DX (a segment address from a previously saved context), part of the terminal emulator's string comparison or configuration lookup routine that restores the data segment after a loop that compares two strings byte-by-byte across segment boundaries.
0x0000B6C7 [R9] This `mov [es:bx],ax` stores a word (the swapped cursor position or attribute value from AX) into video memory at ES:BX, part of the terminal emulator's screen initialization or display clearing routine that writes cursor position data and space characters (0x20) to the video buffer at segment 0xDE00 for a 25-row by 80-column text display.
0x00009FB6 [R9] This `ja 0x9ffb` checks if AH (the printer function number) is greater than 1, branching to an error or unsupported function handler in the terminal emulator's parallel printer port interrupt handler (INT 17h) that validates the requested operation (0=print, 1=initialize, 2=status) before proceeding with printer I/O at ports 0x340-0x342.
0x0000A0FC [R9] This `jz 0xa12c` checks if the byte at [ES:DI+0x10E] (a terminal context block's active session flag) was 0xFF after incrementing it, part of the terminal emulator's context-switching or session management routine that detects when all session slots are full (wrapping from 255 to 0) and jumps to an error path that returns a failure code.
0x0000A4E8 [R9] This `xor ah,ah` clears the high byte of AX (setting error code to 0x02) before setting the carry flag and jumping to the return path, part of the kernel's DOS-compatible memory arena allocation routine that signals an "arena corrupted" or "invalid MCB" error when no matching memory control block is found during the search loop.
0x000072E8 [R9] This `xor di,di` zeroes the destination index register before loading a word from the extra segment at offset 0 (the interrupt vector table entry for INT 0), part of the terminal emulator's executable loading or initialization routine that retrieves the original INT 0 handler address from the IVT to restore or chain to it after setting up custom interrupt handlers.
0x0000DAEA [R9] This `inc dx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x40, 0x42, 0x7E, 0x42, 0x42, 0x42, 0x00, 0x00), so this is DATA.
0x0000995D [R9] This `push ax` saves the computed memory block size (in DX:AX) onto the stack before calling subroutine 0x9B2B, as part of the kernel's DOS-compatible memory arena allocation routine that calculates the adjusted segment address and size for allocating a memory block from the free list.
0x00006AC9 [R9] This `jmp short 0x6aac` creates a loop back to continue processing remaining data when the current chunk size (DI) is smaller than the remaining count (CX), part of the manufacturing self-test (MFGTEST) routine's ROM checksum or memory verification algorithm that processes data in fixed-size chunks across multiple memory regions.
0x0000B0B8 [R9] This `in al,dx` reads the current value of I/O port 0xFF5E (timer 2 control register), part of the terminal emulator's timer synchronization routine that clears bit 1 of the timer control register to stop or reset timer 2 before comparing a stored string at 0x285E against a reference pattern for timing calibration.
0x0000FE22 [R9] This `out dx,al` writes the value 0x01 to I/O port 0xFF80 (PCS0 chip-select base address register), part of the boot initialization sequence that programs the external peripheral chip-select registers to configure the memory-mapped I/O addresses for the UART, CRTC, and other peripherals on the PCS bus.
0x00006575 [R9] This `or ax,0x240a` is part of a sequence of bytes that form a string table of diagnostic messages (the surrounding bytes show patterns like "1200", "2400", "4800", "9600", "14400", "19200", "28800" with CR/LF terminators), so this is DATA representing baud rate display strings for the terminal emulator's setup menu.
0x0000A21F [R9] This instruction sets SI to 0x18CB, the start of a terminal context block table, as part of the terminal emulator's context-switching or session lookup routine that scans 0x12-byte entries for a matching signature or identifier.
0x00009BF0 [R9] This instruction checks if the byte at offset +0xA in the current memory control block (MCB) is zero, part of the kernel's DOS-compatible memory arena traversal routine that determines whether the block is free (0x00) or allocated (non-zero) during memory allocation or deallocation operations.
0x0000B577 [R9] This `pop ds` restores the data segment register before returning from interrupt via `iret`, part of the terminal emulator's keyboard or serial interrupt handler epilogue that checks if the current context pointer at [0x978] matches the saved context at [0x914] before deciding whether to process the interrupt or return immediately.
0x000078FE [R9] This `mov al,ch` loads the character code from CH into AL for translation via the XLAT table at CS:0x187F, part of the terminal emulator's keyboard scan-code to ASCII conversion routine that maps raw keyboard positions to character values before accumulating a numeric parameter (multiplying the existing value at [0x7FC] by 10 and adding the translated digit) for escape sequence or setup-menu numeric input processing.
0x0000E877 [R9] This `add al,0xd` is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x04, 0x0D, 0xE8, 0x1A, 0xF6), so this is DATA.
0x000064CA [R9] This `mov dx,0x1f8` loads the address of a $-terminated string (likely a status message or prompt) for printing via INT 21h AH=0x9, part of the terminal emulator's diagnostic or self-test routine that displays a progress indicator or result string after outputting a series of characters via INT 10h teletype output in the preceding loop.
0x0000E0CF [R9] This `test [bx],dl` is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x17, 0x84, 0x56, 0x18, 0xF8), so this is DATA.
0x0000B03A [R9] This `call 0xb1d8` invokes a subroutine that reads 16 bits (CX=0x10) from a hardware register or memory-mapped I/O location (likely the NVM or configuration storage), part of the terminal emulator's initialization or configuration loading routine that sequentially reads a series of 16-bit values from non-volatile memory and stores them into a parameter table at addresses 0x285E-0x286A for terminal setup attributes.
0x0000B526 [R9] This `jnz 0xb519` loops back to copy another 16-byte glyph row from a run-length encoded font table, part of the terminal emulator's character rendering routine that decompresses compressed font bitmap data for display output.
0x0000A15E [R9] This `push cx` saves the CX register as part of the terminal emulator's context-switching or session management routine that allocates a new terminal context block by scanning a table of context blocks for a free slot.
0x0000AB31 [R9] This `jz 0xab37` checks if bit 1 of the byte at [0x7b2] is clear (testing a timer or serial port status flag), part of the terminal emulator's timer-based baud rate or display refresh synchronization routine that conditionally reads timer 2 count register at I/O port 0xFF5A to detect timing mismatches.
0x0000E7EB [R9] This `push es` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x06, 0xF6, 0x05, 0x24, 0x13, 0x04), so this is DATA.
0x00006C21 [R9] This `add al,0x7` converts a hex digit value (10-15) to its ASCII letter ('A'-'F') by adding 7 to the base '0' offset, part of the crash/error dump routine's hex digit conversion for displaying register values.
0x0000B368 [R9] This `or al,ah` combines the video attribute byte from [0x98f] (masked to 0x7F) with the high byte from AH to set or clear bit 7 (blink/intensity flag), part of the terminal emulator's video attribute update routine that writes the combined attribute to video memory at 0xD000 for display rendering.
0x00006300 [R9] This `jz 0x62e1` checks if the keyboard scan code in AH equals 0xBB (likely a function key or special key code), part of the terminal emulator's keyboard input processing routine that dispatches special key sequences to a subroutine before falling through to output the character via INT 10h teletype output.
0x0000AF04 [R9] This `sti` enables interrupts after disabling the timer 2 output compare or interrupt flag at I/O port 0xFF5E, part of the terminal emulator's interrupt handler initialization routine that sets up interrupt vectors for INT 0x70 (0x5211 handler) and INT 0x20 (0x5207 handler) in the IVT.
0x00006A29 [R9] This `xor si,si` zeroes the source index register before XORing AH with 0x1 and testing bit 0, part of the ROM integrity verification routine that performs a two-pass checksum calculation over the firmware at segment 0xC000, alternating XOR patterns to validate ROM contents.
0x000065F7 [R10] This `mov ax,0xc000` loads the base segment address for the character generator font table, which is then added to the current font page offset from [0x53b] and written to I/O port 0xFF88 (the CRTC font/character generator address register), part of the terminal emulator's font page selection routine that switches between multiple font tables for display rendering.
0x00009A75 [R10] This `mov ax,cs` stores the code segment value into the memory arena control block being constructed at ES:DI, part of the kernel's DOS-compatible memory arena initialization routine that builds the initial memory control block (MCB) chain with signature bytes (0x4D = 'M', 0x5A = 'Z') and segment addresses for the resident firmware's memory management system.
0x0000E511 [R10] This `sbb al,0x38` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1C, 0x38, 0x1C, 0x46, 0x1C, 0xB6), so this is DATA.
0x0000A4DF [R10] This `jns 0xa4d7` creates a loop that decrements DI by 0x12 (the size of a terminal context block) and continues scanning backward through the context block table at ES:0x10E for a free slot (where the word at [ES:DI] equals 0xFFFF), part of the terminal emulator's session management routine that allocates a new terminal context block by finding an unused entry in the table.
0x00007557 [R10] This `jc 0x757e` checks if the keyboard controller status bits (masked to 0x3) are less than 1 (i.e., zero), branching to return a default value from [0x7f5] when no keyboard data is available, part of the terminal emulator's keyboard input interrupt handler (INT 9h or INT 16h) that reads the keyboard status byte from the BIOS data area at segment 0x40 to determine if a key has been pressed.
0x000060D1 [R10] This `mov ax,[bp-0x12]` loads a 16-bit value from the stack frame (likely a register or memory address saved during a crash/error dump routine) for conversion to hexadecimal ASCII via subroutine 0x6139, part of the terminal emulator's diagnostic or error display routine that outputs register contents as hex strings using INT 21h AH=0x9 for $-terminated string printing.
0x00007027 [R10] This `mov si,0x0` sets the source index to zero to begin copying the resident firmware kernel from the ROM segment (CS-based calculation) to the destination segment at 0x0040 (the interrupt vector table or low memory area), part of the boot initialization sequence that relocates the kernel code and data from the ROM image into RAM for execution.
0x0000BB94 [R10] This `mov cx,0x28` sets the count to 40 (0x28) for a `rep stosw` loop that fills a 40-word (80-byte) buffer with 0x2020 (space characters), part of the terminal emulator's screen buffer clearing routine that writes spaces to a line in the video display buffer at segment 0xD000 for a new row of text.
0x00006651 [R10] This `jnz 0x6658` checks if the counter at [0x130] is not equal to 3, part of the terminal emulator's diagnostic or error display routine that tracks repeated character sequences and conditionally suppresses output when a specific character (AH=0x23, '#') is repeated three times, falling through to print a space and backspace for cursor positioning.
0x0000A5B7 [R10] This `mov cx,ax` transfers the remaining paragraph count (from AX) into CX for a `rep movsw` loop that copies 8 words per paragraph (shifted left by 3 bits), part of the kernel's DOS-compatible memory arena allocation or block relocation routine that moves memory blocks in 0x1000-byte chunks between segments during memory management operations.
0x00009B85 [R10] This `pop ds` at 0x9B85 is part of the kernel’s DOS-compatible file read routine (INT 21h AH=0x3F) that restores the data segment after a failed read operation (carry set), returning with carry flag set to indicate an error.
0x0000A150 [R10] This `mov ah,0x48` / `int 0x21` at 0xA150 is part of the terminal emulator’s memory allocation routine that requests a 0x200-byte block from the kernel’s DOS-compatible memory manager, storing the resulting segment at [0x89C] and the actual allocated size at [0x89E].
0x000067AB [R10] This `mov ax,[es:di+0xc]` at 0x67AB loads a 16-bit value (likely a hardware register dump or diagnostic parameter) from a table of CRTC/UART register entries during the manufacturing self-test (MFGTEST) routine that displays hardware register contents for debugging.
0x00009EC7 [R10] This `add ax,[es:si+0x6]` at 0x9EC7 accumulates the size field (offset +6) of a memory control block (MCB) during the kernel’s DOS-compatible memory arena traversal, summing the sizes of allocated blocks that match a specific owner ID (byte at +0xA equals 1) and segment (BX).
0x0000B0BC [R10] This `sti` re-enables interrupts after disabling timer 2 by clearing bit 1 of its control register at I/O port 0xFF5E, part of the terminal emulator's timer synchronization routine that stops timer 2 before comparing a stored configuration string at 0x285E against a reference pattern for timing calibration.
0x0000E6D2 [R10] This `sbb bh,ah` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1A, 0xFC, 0x15, 0x01, 0x03, 0x16, 0x13, 0xF6), so this is DATA.
0x0000A138 [R10] This `jc 0xa0ee` checks if the table pointer SI (scanning 8-byte entries) has reached the end of a context block table at address 0x8F1, part of the terminal emulator's session management routine that searches for a free slot in the terminal context block table to allocate a new session.
0x0000645A [R10] This `mov ax,[bx+0x4]` loads a 16-bit value from offset +4 of a memory control block or data structure (likely a segment address or size field), part of the kernel's memory arena traversal or diagnostic dump routine that displays memory block details (such as size or owner) during a memory inspection or self-test operation.
0x0000AE1B [R10] This `scasb` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0xAE, 0x3A, 0xB0, 0x83, 0x82, 0x6E, 0x6F, 0x30, 0x2F), so this is DATA.
0x0000765F [R10] This `jmp short 0x7686` skips the font glyph rendering loop when the loaded byte (AL) is zero, indicating an end-of-row or null character in the terminal emulator's character generator table processing routine that copies 8x8 or 8x16 font bitmaps from a compressed table at CS:0xA4A into video memory at segment 0xD000 for display output.
0x0000B92A [R10] This `mov al,0x20` loads the ASCII space character (0x20) to fill the remainder of a cleared video line with spaces, part of the terminal emulator's display update routine that clears a row of text at the cursor position in the video buffer (segment ES, likely 0xD000) after retrieving the current character and attribute from the cursor location.
0x0000B61B [R10] This `cmp al,ah` compares the character code (AL) with the attribute byte (AH) to check if they match, part of the terminal emulator's character rendering routine that determines whether to use a run-length encoded font glyph or a direct bitmap copy when writing characters to video memory at segment 0xD000 for display output.
0x0000B13E [R10] This `xor al,[di-1]` XORs the current byte from the source font bitmap (at [si-1]) with the corresponding byte from the destination video buffer (at [di-1]), part of the terminal emulator's character attribute or display update routine that computes a difference mask for applying video attributes (such as underline, reverse video, or blink) to a character position in the screen buffer.
0x00006B4A [R10] This `or al,ah` combines the computed CRTC or UART configuration byte (AL, derived from testing DX bits 12 and 13) with the masked value from [0x7b9] (AH, masked with 0xFC), part of the manufacturing self-test (MFGTEST) routine's parallel printer port initialization that programs the printer control register at I/O port 0x343 with the appropriate mode and status bits for printer testing.
0x0000B1A3 [R10] This `mov bx,0x4d12` selects a default font table offset when the character height value at [0x7f4] does not match any of the specific heights (0x12, 0x13, 0x10, or 0x06), part of the terminal emulator's character rendering routine that uses the XLAT instruction to map character codes to font glyph pointers for display output.
0x0000A246 [R10] This `cmp bx,0x18ca` checks if the context block pointer (BX) is within the valid range of terminal context block entries (0x164A to 0x18CA), part of the terminal emulator's context-switching or session management routine that validates a context block address before comparing its saved segment value at [bx+0x8] against the current active context at [0x914].
0x0000689B [R10] This `int 0x16` reads a keyboard character (AH=0x00, blocking read) after polling for key availability with AH=0x01, part of the terminal emulator's keyboard input routine that echoes the character via INT 10h teletype output (AH=0x0E) and stores it in a buffer at ES:DI (starting at 0x773) until a carriage return or backspace is entered.
0x0000BB77 [R10] This `pop cx` restores CX after an INT 0x40 call (with AH=2, CX=0x3E8, DX=0x7D) that likely performs a timer delay or synchronization operation, part of the terminal emulator's timing or baud-rate synchronization routine that uses the 80186 timer subsystem to measure or wait for a specific interval before continuing a loop.
0x00006092 [R10] This `mov dx,0xeb` loads the offset of a $-terminated diagnostic or status string (at file offset 0xEB) for printing via INT 21h AH=0x9, part of the manufacturing self-test (MFGTEST) routine that displays test results or progress messages after converting values to ASCII via subroutine 0x6139.
0x0000712C [R10] This `call 0x714e` invokes a subroutine that processes a block of 0x880 bytes (CX=0x880), part of the terminal emulator's font or glyph table initialization routine that copies or manipulates character generator bitmap data for display rendering, following similar calls with different sizes (0x240, 0x2020, 0x8008) to handle multiple font tables or video buffer regions.
0x00009A59 [R10] This `mov al,0x9a` stores the byte value 0x9A (which encodes a memory control block type or signature, likely indicating the last block in the arena) into the kernel's DOS-compatible memory arena allocation routine, writing it as part of the MCB header structure at ES:DI during memory block initialization or deallocation.
0x0000B19B [R10] This `mov bx,0x4e7a` loads the offset of a font translation table (at CS:0x4E7A) for use with the XLAT instruction, part of the terminal emulator's character rendering routine that selects a run-length encoded font bitmap table based on the character attribute byte at [0x7F4] (comparing against values 0x12, 0x13, 0x10, 0x06) before translating the character code via `cs xlatb` for display output.
0x00007381 [R10] This `add sp,byte +0x2` adjusts the stack pointer to discard a saved word before executing `iret`, part of the terminal emulator's interrupt handler epilogue that cleans up the stack after processing a hardware interrupt (likely from the timer or serial port) and returns to the interrupted code.
0x00007868 [R10] This `jnz 0x78ac` checks if bit 3 of AL is set (testing a serial port status or modem control register flag), part of the terminal emulator's serial port interrupt handler that dispatches to different service routines based on the type of serial event (receive, transmit, line status, or modem status) indicated by the UART's interrupt identification register.
0x00009BC8 [R10] This `clc` clears the carry flag before returning via `retf 0x2`, part of the kernel's DOS-compatible memory arena allocation routine that signals a successful operation (no error) when returning from a memory management function, such as after successfully allocating or deallocating a memory control block.
0x00006AA9 [R10] This `mov cx,[bx+0x2]` loads the size (in bytes) of the current memory region from a table entry at [bx+0x2], part of the manufacturing self-test (MFGTEST) routine's ROM checksum or memory verification algorithm that iterates through a table of memory regions (starting at 0x8B1) to compute checksums over firmware blocks for integrity validation.
0x00006250 [R10] This `mov ah,0x2` / `int 0x21` outputs the tens digit of a two-digit number (from the preceding division by 10) as a character via DOS function 2, part of the terminal emulator's numeric-to-ASCII conversion routine for displaying diagnostic or status values.
0x0000B10E [R10] This `pop dx` restores the DX register as part of the epilogue of an interrupt service routine (likely timer or system tick), returning from the interrupt via `iret` after decrementing timing counters at [0x96e] and [0x96c] and incrementing a 32-bit tick counter at [0x6c]-[0x6e].
0x00006C32 [R10] This `test ax,ax` checks if the quotient from dividing a value by 0x2710 (10000) is zero, part of the crash/error dump routine's multi-digit decimal display function that suppresses leading zeros when printing register values or memory contents.
0x0000B0C9 [R10] This `jnz 0xb116` checks if a 15-byte string comparison (via `repe cmpsb`) between the stored configuration at [0x285e] and a reference pattern at [0x286d] failed, part of the terminal emulator's timer synchronization routine that detects when the stored timing calibration data does not match the expected pattern, branching to recalibrate.
0x0000E7BA [R10] This `sub al,0x1f` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1E, 0x1F, 0x2C, 0x1F, 0x4A, 0x06, 0x3A, 0x1F), so this is DATA.
0x0000BCB2 [R10] This `mov ah,0x4` / `int 0x40` calls a custom INT 40h handler (likely the terminal emulator's display or cursor service) to set the cursor type or shape, part of the character output routine that follows teletype output via INT 10h.
0x0000998E [R10] This `add ax,0x10` rounds up the computed memory block size to the next 16-byte paragraph boundary, part of the kernel's DOS-compatible memory arena allocation routine that aligns allocation sizes before scanning the MCB chain.
0x0000B442 [R10] This `push di` saves the DI register as part of the prologue of a keyboard or serial input interrupt handler that prepares to write the received character's attribute byte to video memory at segment 0xD000 for display rendering.
0x0000A738 [R11] This instruction initializes a byte in the terminal context block (at offset +0x9F from BX) to 0x00, part of the terminal emulator's session initialization routine that sets default values for various context fields (such as cursor position, scroll region, or attribute flags) when creating or resetting a terminal session.
0x0000DB05 [R11] This byte (0x44) is part of a character generator font bitmap table (the surrounding bytes show typical 8x8 or 8x16 glyph patterns with values like 0x08, 0x1C, 0x40, 0x80, 0x38, 0x44, 0x82, 0x28, 0xEE), so this is DATA representing pixel data for a glyph in the terminal emulator's display font.
0x0000664B [R11] This `ret` instruction returns from a subroutine that displays diagnostic or status messages (using INT 21h AH=0x9 for $-terminated strings and INT 10h AH=0xE for teletype output), part of the terminal emulator's diagnostic or error display routine that outputs a progress indicator or result string after converting values to ASCII.
0x0000FE14 [R11] This `out dx,al` writes the value 0x00 to I/O port 0xFFA0 (UMCS register), part of the boot initialization sequence that programs the 80186 chip-select registers to configure the upper memory chip-select (UMCS) for the ROM at address 0xF0000-0xFFFFF, setting the base address and control bits for memory access.
0x0000BBB1 [R11] This `test di,di` checks if the DI register is zero (indicating whether a previous cursor position was saved at [0x98B]), part of the terminal emulator's display update routine that conditionally clears the underline attribute (by ANDing with 0xF7) at the previous cursor location in the video buffer at segment 0xD000 before moving the cursor to a new position.
0x0000B538 [R11] This `pop di` restores the destination index register as part of the epilogue of the terminal emulator's run-length encoded font decompression routine, which copies glyph rows from a compressed font table into video memory for character rendering.
0x0000B90C [R11] This `mov dl,ch` transfers the character's row offset or attribute byte from CH into DL, part of the terminal emulator's display update routine that calculates the destination address in the video buffer (segment 0xD000) for writing a character glyph during screen rendering.
0x0000797E [R11] This `cli` disables interrupts before storing a default interrupt handler address (0x1961) and code segment into the terminal context block at [bx+0x92]/[bx+0x94], part of the terminal emulator's context initialization routine that sets up a fallback interrupt vector when no custom handler is provided.
0x0000B7FA [R11] This `pop ds` restores the data segment register before returning from interrupt via `iret`, part of the terminal emulator's keyboard or serial interrupt handler epilogue that checks if the current context pointer at [0x978] matches the saved context at [0x914] before deciding whether to process the interrupt or return immediately.
0x0000673C [R11] This `int 0x21` with AH=0x9 prints a $-terminated diagnostic or status string at DX=0x1FB, part of the manufacturing self-test (MFGTEST) routine that displays test results or progress messages after iterating through a table of CRTC/UART register entries.
0x00009CA8 [R11] This `cmp word [es:bx],0x434c` checks if the first word of a memory control block (MCB) matches the signature "CL" (0x434C), part of the kernel's DOS-compatible memory arena traversal routine that validates MCB headers during memory allocation or deallocation operations.
0x00006FBD [R11] This `mov bx,0x2f` loads a diagnostic result code (0x2F) indicating a specific test outcome, part of the manufacturing self-test (MFGTEST) routine's parallel printer port testing that branches to display different error or status messages based on the test results.
0x0000B1CF [R11] This `xor ah,ah` zeroes the high byte of AX before calling subroutine 0x768c, part of the terminal emulator's character rendering routine that uses the value at [0x973] (a character code or attribute) as an 8-bit parameter for font glyph lookup or display output.
0x000062BD [R11] This `pop es` restores the ES segment register (previously saved by `push ds`) to set up the environment for an INT 21h AH=0x4B00 (EXEC) call that loads and executes an external program (likely DOWNLOAD.EXE, EMULATOR.EXE, or MFGTEST.EXE) from the ROM filesystem.
0x0000A642 [R11] This `push di` saves the DI register as part of the prologue of a memory block splitting routine in the kernel's DOS-compatible memory arena manager, which divides an allocated memory control block (MCB) into two parts when the requested size is smaller than the available block.
0x0000FEA6 [R11] This `mov dx,0xff54` loads the I/O port address of Timer 0's count register (part of the 80186 on-chip timer subsystem at PCB base 0xFF00), programming it with value 0x3F (63) as part of the boot initialization sequence that configures timer 0 for system timing operations.
0x0000EA12 [R11] This `add [bx+si],al` instruction is part of a sequence of zero bytes (0x0000) that serve as padding or alignment between the ASCII filename strings (MFGTEST.EXE, CONFIG.SYS, etc.) and the following character-generator font/bitmap data, so this is DATA.
0x0000A1E8 [R11] This `jnz 0xa1ec` checks if the character at [es:di-1] is not a period (0x2E), part of the terminal emulator's filename or path parsing routine that strips the file extension by decrementing DI when a period is found, before padding the remaining buffer with spaces.
0x0000BD81 [R11] This `stosb` stores the byte from AL (loaded from [0x9B3]) into the video buffer at ES:DI (pointing to segment 0xC300), part of the terminal emulator's display update routine that writes a character to the screen buffer at a calculated position for rendering text output.
0x0000A344 [R11] This `mov dx,cx` transfers the high word of a 64-bit value (from CX) into DX, part of the kernel's DOS-compatible memory arena allocation or deallocation routine that updates the size fields (at [bx+0x4] and [bx+0x6]) of a memory control block during block splitting or merging operations.
0x0000A39F [R11] This `jnc 0xa3a6` checks if the context block pointer BX is within the valid range (0x164A to 0x18CA) for terminal context blocks, part of the terminal emulator's session management routine that validates a context block address before proceeding with context-switching operations.
0x0000B7FF [R11] This `add di,0xff` advances the video buffer pointer by 255 bytes (one full row of 80 characters × 2 bytes each plus attribute space), part of the terminal emulator's character rendering routine that positions the write pointer to the next screen row in the video buffer at segment 0xD000.
0x00009C7D [R11] This `mov byte [si+0xa],0x1` sets the owner ID byte of a memory control block (MCB) to 1 (indicating an allocated block), part of the kernel's DOS-compatible memory arena allocation routine that marks a newly allocated memory block as belonging to the current process during memory management operations.
0x0000ACB3 [R11] This `mov [0x964],ax` stores the value 0x3FFF (likely a timer reload or maximum count value) into a timing control variable, part of the terminal emulator's timer synchronization routine that sets the maximum baud rate or refresh timing parameter for the serial port or display synchronization.
0x0000BD44 [R11] This `mov [0x9af],bx` stores the video buffer offset of a character position (with swapped byte order) for later attribute manipulation, part of the terminal emulator's display update routine that saves the location of a character to apply video attributes (such as underline or reverse video) in the screen buffer at segment 0xD000.
0x0000AAEB [R11] This `cmp ax,[0x94a]` compares the current configuration pointer (loaded from [0x94c]) against a saved or expected value at [0x94a], part of the terminal emulator's configuration or NVM parameter validation routine that checks whether the configuration data has been modified or needs reloading.
0x0000A5DE [R11] This `mov ah,0x50` writes the value 0x50 to a memory-mapped I/O location (likely a CRTC or video controller register) as part of a hardware detection or initialization sequence that tests for the presence and responsiveness of the video hardware by writing and reading back test patterns.
0x00006946 [R11] This `int 0x21` with AH=0x9 prints a $-terminated string at DX=0x2E6 (likely a diagnostic message or test result), part of the manufacturing self-test (MFGTEST) routine that displays the results of hardware register tests or configuration checks after calling subroutine 0x6C0E for hex conversion.
0x0000AC81 [R11] This `shr bx,1` divides the CX register value (likely a timer count or baud rate divisor) by 2, part of the terminal emulator's baud rate or timing calibration routine that computes an average or adjusted value for display refresh or serial port synchronization.
0x00009D88 [R11] This `cmp ax,0x3dbb` is part of a jump table or dispatch table (the surrounding bytes show table entries like 0x3D8F, 0x3DBB, 0x3DF7) used by the kernel's DOS-compatible memory management routine to select different memory block operations (such as allocate, free, or resize) based on the function code in AX, so this is DATA representing jump table offsets.
0x0000A699 [R11] This `pop bp` restores the base pointer register as part of the epilogue of the kernel's DOS-compatible memory arena deallocation or block relocation routine, returning from a function that manipulates memory control block (MCB) entries after scanning or modifying the arena chain.
0x00006C3B [R11] This `mov ax,dx` loads the remainder from a previous division by 0x2710 (10000) into AX for further decimal digit extraction, part of the crash/error dump routine's multi-digit decimal display function that converts register values or memory contents to ASCII for output.
0x000062D4 [R11] This `mov dx,0x2e6` loads the offset of a $-terminated success or completion string (likely "Program loaded" or similar) for printing via INT 21h AH=0x9, part of the terminal emulator's program loading routine that displays a status message after successfully executing an EXEC (INT 21h AH=0x4B) call to load an embedded executable.
0x000079D8 [R11] This `mov ax,[bx+0x90]` loads a 16-bit value (likely a ring buffer head pointer or character count) from the current terminal context block at offset 0x90, part of the terminal emulator's keyboard or serial input interrupt handler that checks whether the input buffer is empty by comparing the head and tail pointers at offsets 0x90 and 0x8E before returning a status byte.
0x0000A14D [R11] This `mov bx,0x200` sets the requested allocation size to 0x200 bytes (512 bytes) for the subsequent INT 21h AH=0x48 memory allocation call, part of the terminal emulator's session management routine that allocates a new terminal context block for a terminal session.
0x0000A30C [R11] This `mov ds,ax` sets the data segment register to 0x40 (the BIOS data area segment), part of the terminal emulator's context-switching or session validation routine that accesses the BIOS data area to retrieve or compare the current active terminal context segment stored at [0x914] against the context block pointer in BX.
0x0000BDBC [R11] This `and byte [es:di],0xfc` clears the two least significant bits of the attribute byte at the cursor position in the video buffer (segment ES, likely 0xD000), part of the terminal emulator's display update routine that modifies video attributes (such as disabling underline or blink) during cursor positioning or screen refresh operations.
0x00009F2A [R11] This `jnz 0x9edb` creates a loop that continues scanning through memory control blocks (MCBs) in the kernel's DOS-compatible memory arena traversal routine, checking if the current block's size field (at [es:bx+0x2]) is non-zero to determine whether to proceed to the next block or return.
0x0000CFCA [R11] This `add [bp+si-0x7c],bh` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x7A, 0x84, 0x84, 0x84, 0x7C, 0x04, 0x84, 0x78), so this is DATA.
0x00006DBB [R11] This `push cs` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x0E, 0xD2, 0x3B, 0x3E, 0x3D, 0x95, 0x3C), so this is DATA.
0x00006FEB [R11] This `mov al,0x44` / `out dx,al` writes the value 0x44 to I/O port 0x343 (the parallel printer port control register), part of the manufacturing self-test (MFGTEST) routine's parallel printer port initialization that programs the printer control register with specific mode and status bits for printer testing.
0x0000719E [R11] This `dec sp` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x4C, 0x7B, 0x53, 0x1E, 0x44, 0x4A, 0x3F, 0xF2), so this is DATA.
0x0000A81E [R12] This `xor al,al` clears AL before subtracting the byte at [bx+0xa8] (a context block field indicating whether a terminal session is active or has pending data), part of the terminal emulator's context-switching routine that computes a return status or flag value when restoring a saved terminal context after a task switch or interrupt return.
0x0000677D [R12] This `int 0x21` with AH=0x9 prints a $-terminated diagnostic string at DX=0x1F8 (likely a test result or status message such as "PASS" or "FAIL"), part of the manufacturing self-test (MFGTEST) routine's CRTC/UART register verification loop that displays the outcome after iterating through a table of hardware register entries.
0x0000B9B1 [R12] This `mov al,bh` loads the high byte of BX (which contains the character's attribute or color byte) into AL for use as the fill byte in a `rep stosb` loop that writes attribute values across a row of the video buffer, part of the terminal emulator's display update routine that clears or initializes a line of screen memory at segment 0xD000.
0x0000706B [R12] This `mov di,0xbc` sets the destination index to offset 0xBC within the interrupt vector table (segment 0x0000), preparing to write the address 0x11DB (the handler for INT 0x2F or a custom software interrupt) into the vector table, part of the manufacturing self-test (MFGTEST) routine's interrupt vector initialization that installs custom handlers for hardware testing.
0x00009DD0 [R12] This `add dx,byte +0x1` increments the size field (at [si+0x6]) of a memory control block (MCB) by 1, part of the kernel's DOS-compatible memory arena deallocation routine that adjusts the block size when merging adjacent free blocks during memory release operations.
0x000066AB [R12] This `add al,0x30` converts a hexadecimal nibble (0-15) to its ASCII character representation ('0'-'9' or 'A'-'F') by adding 0x30, and if the nibble was >= 0x0A, an additional 0x07 was already added at 0x66A9; this is part of the terminal emulator's numeric-to-ASCII conversion routine for displaying diagnostic or status values via INT 10h teletype output.
0x0000796A [R12] This `or bx,di` checks if both BX (the ES segment) and DI (the offset) are zero, indicating whether a valid interrupt handler address was provided; part of the terminal emulator's context initialization routine that decides whether to store a custom interrupt handler address or use a default fallback handler (0x1961) in the terminal context block.
0x000061CB [R12] This `push ax` saves the current character code (in AL) before calling INT 10h to output a prompt or status string at offset 0xE5E, part of the manufacturing self-test (MFGTEST) routine's keyboard input or configuration menu handler that displays a character prompt and then outputs the user's keypress with an offset of 0x40 added to the character code.
0x0000FEBB [R12] This `mov dx,0x343` loads the I/O port address of the parallel printer control register (at 0x343), then writes 0x00 to it via `out dx,al`; part of the boot initialization sequence that initializes the external parallel printer port by clearing its control register before setting up the stack pointer and programming the interrupt controller registers.
0x000068E8 [R12] This `int 0x16` with AH=0x01 polls the keyboard for a character (checking the Zero Flag) in a loop, part of the manufacturing self-test (MFGTEST) routine's interactive prompt that waits for a Y/N response after displaying a diagnostic string, before reading the actual keypress with AH=0x00.
0x0000E83F [R12] This `xchg ax,dx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1D, 0x1C, 0x0A, 0x09, 0x18, 0x34, 0x4E, 0x5C, 0xD4, 0x03, 0x6A, 0x78, 0xFE, 0x24, 0x05, 0x28, 0x04, 0x86, 0x36, 0x04, 0x52, 0x04, 0x94), so this is DATA.
0x0000A074 [R12] This `xor di,di` initializes DI to zero before writing a test pattern (0x90) to video memory at segment 0xA000 and reading it back to detect the presence of a specific video controller (checking for values 0x9589 or 0x9489), part of the terminal emulator's hardware detection routine that identifies the video hardware type for proper initialization.
0x0000AF93 [R12] This `cmp al,0x9` checks if the AL value (a character code or attribute) is less than or equal to 9, part of the terminal emulator's escape sequence or attribute parsing routine that maps character values to display attribute bytes (AH values 0x2 through 0x9) for setting video attributes like underline, reverse video, or blink.
0x00007702 [R12] This `call 0x7935` invokes a subroutine that likely handles a specific terminal escape sequence or control function (such as cursor positioning or screen clearing), part of the terminal emulator's VT-style escape sequence parser that dispatches to handler routines based on the current escape sequence state and character received.
0x00009FBE [R12] This `mov bl,al` stores the character to be output (AL) for writing to the parallel printer data port at 0x342, part of the terminal emulator's printer output routine that checks printer status via port 0x340 before sending data.
0x00006EFD [R12] This `mov cl,al` captures the low byte of a computed value (likely a pixel position or color component) during a division sequence that converts coordinates or timing values, part of the terminal emulator's display or timing calculation routine that decomposes a 32-bit value into hours/minutes/seconds or pixel rows/columns for screen rendering.
0x0000AFCF [R12] This `cmp al,0x12` checks if the character height value matches 0x12 (18 pixels), part of the terminal emulator's font selection routine that compares the current height against known values (0x12, 0x13, 0x41, 0xC1, 0x06, 0x10) to select the appropriate font table for character rendering.
0x0000E709 [R12] This `add ax,0x6801` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x05, 0x01, 0x68, 0x17, 0x03, 0x02, 0x76, 0x17), so this is DATA.
0x0000BD04 [R12] This `xor di,di` zeroes the destination index register to set up writing the interrupt vector table entry for INT 0x38 (serial port or timer interrupt), part of the terminal emulator's interrupt handler installation routine that stores the handler address (0x5D1F) and code segment into the IVT at 0x0000:0x0038.
0x00006388 [R12] This `call 0x63a7` invokes a subroutine that reads a character from the keyboard or serial input buffer (likely via INT 16h or direct port I/O), part of the terminal emulator's line-editing input routine that processes typed characters, handles backspace/CR, and stores them in a buffer at ES:DI for command or data entry.
0x0000A836 [R12] This `mov [0x914],ax` stores the new terminal context block segment address (from AX) into the global active context pointer at [0x914], part of the terminal emulator's context-switching routine that activates a different terminal session by updating the current context and restoring its associated video and cursor parameters.
0x0000B8A0 [R12] This `jnz 0xb8df` checks if the current context block pointer (BX from [0x914]) matches the active display context at [0x978]; if not equal, it skips the cursor-position update and attribute-clearing logic, part of the terminal emulator's display update routine that only modifies the on-screen cursor when the context being updated is the currently visible session.
0x0000A2EA [R12] This `pop bx` retrieves a count value from the stack (previously pushed as part of a loop counter or parameter), part of the terminal emulator's character output routine that prints a string of CX bytes via INT 10h teletype output, handling a bounded repeat loop for displaying diagnostic or status messages.
0x0000B30E [R12] This `xchg al,[0x97b]` atomically exchanges the current byte (likely a cursor blink or attribute toggle flag) with a new value from AL, part of the terminal emulator's cursor blink timer routine that toggles the cursor visibility state and resets the blink counters at [0x97c] and [0x97d] when the blink period expires.
0x0000BD20 [R12] This `cld` instruction sets the direction flag to forward (increment) before a string operation, part of the terminal emulator's interrupt handler prologue that prepares to copy data to the video buffer at segment 0xD000 after saving registers and setting up segment registers for display output.
0x0000A239 [R12] This `pop bx` restores the BX register after a subroutine return, part of the terminal emulator's context-switching or session management routine that validates a context block address (checking if BX is between 0x164A and 0x18CA) before comparing its saved segment value against the active context at [0x914].
0x0000AAF1 [R12] This `ret` instruction returns from a subroutine that compares the current configuration pointer at [0x94c] against a saved or expected value at [0x94a], part of the terminal emulator's configuration or NVM parameter validation routine that checks whether the configuration data has been modified or needs reloading.
0x0000E8A4 [R12] This `push es` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1B, 0x1C, 0x4A, 0x06, 0x0E, 0x1C, 0x2A, 0xBE, 0x0C, 0x38, 0x46, 0x54, 0xC8, 0x70, 0x7E, 0x8C), so this is DATA.
0x0000AC85 [R12] This `adc dx,byte +0x0` adds any carry from a previous addition (AX += BX) into DX, part of the terminal emulator's baud rate or timing calibration routine that computes a 32-bit accumulated value (at [0x960]/[0x962]) for averaging or adjusting display refresh or serial port synchronization parameters.
0x000069A8 [R12] This `in al,dx` reads the parallel printer status port at I/O 0x340, part of the manufacturing self-test (MFGTEST) routine's parallel printer port initialization that checks the printer status register bits (testing bits 0 and 1 for error conditions) before programming the printer control register at 0x343.
0x0000E5F9 [R12] This `add [si],cx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1C, 0x12, 0x5E, 0x01, 0xC2, 0x19, 0x60, 0x12), so this is DATA.
0x00006D7B [R12] This `push cs` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x0E, 0x07, 0x0E, 0x07, 0x0E, 0x07, 0x24, 0x0E), so this is DATA.
0x0000E519 [R12] This `sbb al,0x62` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1C, 0x4A, 0x06, 0xA8, 0x1C, 0x0E, 0x1C, 0x1C), so this is DATA.
0x0000B577 [R12] This `pop ds` restores the data segment register as part of the epilogue of the terminal emulator's keyboard or serial interrupt handler, which checks if the current context pointer at [0x978] matches the saved context at [0x914] before deciding to return from interrupt immediately via `iret` without processing.
0x00006C87 [R12] This `int 0x40` with AH=0x4 calls the terminal emulator's display or cursor service (likely to set cursor type/shape or perform a video operation), part of the crash/error dump routine's display function that outputs register values or memory contents as hexadecimal digits after converting them via the preceding nibble-to-ASCII conversion subroutine.
0x0000E6EF [R12] This `push es` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x06, 0x02, 0x12, 0x06, 0x10, 0x01, 0x01, 0xBC), so this is DATA.
0x0000A479 [R12] This `jnz 0xa49a` checks if the result from subroutine 0xa21b (which likely validates a memory control block or configuration signature) is non-zero, part of the terminal emulator's session initialization routine that scans through context block entries starting at 0x8B1 to find an available or valid terminal session context.
0x0000DAC8 [R12] This `adc [bx+si],ch` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x00, 0x40, 0x40, 0x80, 0x7E, 0x22, 0x20, 0x3C), so this is DATA.
0x0000A0F6 [R12] This `test al,al` checks if the byte at [es:0x10E] (likely a memory control block type or signature byte) is zero, part of the kernel's DOS-compatible memory arena initialization routine that validates MCB headers during the memory block scanning or allocation process before proceeding to check for the end-of-chain marker.
0x0000AA06 [R12] This `call 0xaa75` is part of the terminal emulator's timer synchronization or baud-rate calibration routine, invoked when testing the timer status register at I/O port 0xFF66 reveals that bit 6 (0x40) is set, indicating a specific timer event or overflow condition that requires recalibration or adjustment of timing parameters.
0x0000DEA3 [R12] This `add al,0x52` instruction is part of a character generator font bitmap or glyph data table (the surrounding bytes show a regular progression of values typical of 8x16 font patterns with incremental offsets), so this is DATA.
0x00007A0A [R12] This `iret` instruction is part of the terminal emulator's keyboard or serial input interrupt handler epilogue that returns from interrupt after checking whether a flag at [bx+0x98] (likely a keyboard buffer or input pending flag) is zero, and if so, branching to read a character via INT 16h AH=0x00 before returning.
0x0000B26D [R12] This `adc [bp+di],al` instruction is part of a character generator font bitmap or glyph data table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x02, 0x19, 0x50, 0x10, 0x03, 0x19, 0x50, 0x10, 0x07, 0x19, 0x50, 0x10, 0x27, 0x1B, 0x50, 0x0E, 0x30, 0x1E, 0x50, 0x0E, 0x32, 0x19, 0x84, 0x10, 0x34, 0x1B, 0x84, 0x0E, 0x37, 0x1E, 0x84, 0x0E, 0x1E, 0x07, 0xBF, 0x7C, 0x28), so this is DATA.
0x0000BDC8 [R12] This `xor al,al` clears the AL register to zero as part of the terminal emulator's display update routine, resetting a flag at [0x9b5] that tracks underline or cursor attribute state after modifying video attribute bytes at the cursor positions stored in [0x9b1] and [0x9af].
0x0000BC66 [R12] This `int 0x10` with AH=0x2 calls the BIOS video interrupt to set the cursor position to the coordinates specified by DH (row 0x18) and DL (column from [0x9ba]), part of the terminal emulator's display output routine that positions the hardware cursor after rendering character glyphs to the video buffer.
0x000069C1 [R12] This `mov es,ax` sets the extra segment register to 0xC000 (pointing to the video ROM or expansion ROM area), part of the manufacturing self-test (MFGTEST) routine's hardware detection sequence that compares strings at [0x763] and [0x783] against data in the video BIOS area to identify the installed video controller type.
0x0000B32D [R12] This `jz 0xb345` checks if the byte at [0x7ad] (a configuration or timing parameter) is zero, part of the terminal emulator's keyboard or serial input timing routine that decrements countdown timers at [0x97d] and [0x97c] to implement a repeat-delay or typematic rate for key input processing.
0x0000786E [R12] This `cmp ch,0x47` compares the high byte of CX (likely a UART line status or modem control register value) against 0x47 (ASCII 'G'), part of the terminal emulator's serial port interrupt handler that dispatches to different service routines based on the type of serial event indicated by the UART's interrupt identification register and character values.
0x00009DC1 [R13] This `mov es,ax` loads the segment address of a memory control block (MCB) from AX into ES, part of the kernel's DOS-compatible memory arena deallocation routine that swaps owner fields between adjacent MCBs when merging free blocks during memory release operations.
0x0000A398 [R13] This `clc` clears the carry flag to indicate a successful operation (no error), part of the terminal emulator's context-switching or session validation routine that returns from a far call with a success status after validating a context block address range (0x164A-0x18CA) or a small handle value (<5).
0x00006072 [R13] This `mov ax,[bp-0x8]` loads a 16-bit value (likely a register dump or error code) from the stack frame for conversion to hexadecimal ASCII via subroutine 0x6139, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs multiple diagnostic values with $-terminated label strings.
0x0000701A [R13] This `add ax,0xfe8e` computes a segment address (CS + 0xFE8E - 0xF600 = CS + 0x88E) to point to the ROM's resident firmware data area, part of the manufacturing self-test (MFGTEST) routine's initialization that copies 0x9CA bytes of configuration or context data from ROM to RAM at segment 0x40 (BIOS data area) for runtime use.
0x0000FE9B [R13] This `mov dx,0xff74` selects the 80186 Timer 2 control register (at PCB offset 0x74) to program the timer's mode/control byte, part of the boot initialization sequence that configures the on-chip timers after setting up chip-select registers and the interrupt controller.
0x0000FEC6 [R13] This `mov sp,0xfffe` sets the stack pointer to the top of the 64KB ROM address space (0xFFFFE physical, just below the reset vector), part of the boot initialization sequence that establishes a temporary stack before programming the interrupt controller registers and jumping to the resident firmware kernel.
0x00009CFC [R13] This `sub dx,cx` computes the remaining size of a memory control block (MCB) after subtracting the requested allocation size (CX) from the block's total size field at [es:bx+0x6], part of the kernel's DOS-compatible memory arena allocation routine that checks if the block is large enough to split.
0x0000A09A [R13] This `inc di` increments the destination index to advance through video memory at segment ES (0xA000) during a hardware detection loop, part of the terminal emulator's video controller identification routine that writes test patterns and reads them back to detect specific video hardware types.
0x00006005 [R13] This `push es` saves the extra segment register at the start of a subroutine that initializes the parallel printer port control register at 0x343, part of the boot initialization sequence that programs external peripheral chip-selects and configures the printer hardware.
0x0000B2E5 [R13] This `jz 0xb2ec` checks if the result from subroutine 0xa69d (which likely validates a configuration or NVM parameter) is zero, part of the terminal emulator's configuration loading routine that determines whether to load the interrupt vector table entry from [cs:0x8] or use a default value based on the validation result.
0x000065B3 [R13] This `and ax,0x8005` masks the AX register to isolate specific bits (likely bit 15 and bits 2/0) from a value read from I/O port 0xFF8C (a chip-select or configuration register), part of the terminal emulator's hardware initialization routine that tests or configures the video memory mapping by checking the state of address line or memory decode bits.
0x00006B2E [R13] This `mov al,0x4` sets bit 2 of the parallel printer control register value (port 0x343) to enable the printer's auto-linefeed or initialize the control register, part of the manufacturing self-test (MFGTEST) routine's parallel printer port initialization that constructs the control byte by testing DX bits 0x1000 and 0x2000 and combining with a saved mask at [0x7b9].
0x000067A1 [R13] This `loop 0x679c` decrements CX and repeats the character output loop (loading a byte from ES:SI and displaying it via INT 10h AH=0x0E) for 12 iterations, part of the manufacturing self-test (MFGTEST) routine's CRTC/UART register verification that displays the contents of a hardware register table entry as ASCII characters on screen.
0x0000AABD [R13] This `mov bx,[0x94c]` loads the current configuration data pointer from the global variable at [0x94c], part of the terminal emulator's NVM configuration or baud-rate calibration routine that compares it against the saved pointer at [0x94a] to determine whether to output a new byte from the configuration table to the timer watchdog register at port 0xFF6A.
0x0000611D [R13] This `mov cx,0x8` sets the loop counter to 8 for printing 8 words (16 bytes) of stack data as hexadecimal values via INT 21h AH=0x02, part of the crash/error dump routine's stack dump display that outputs the contents of the stack segment (SS) in a formatted hex dump after printing the stack segment address and a colon separator.
0x0000B134 [R13] This `or dh,ah` accumulates a bitmask in DH by OR-ing the current character's attribute byte (AH) when a character is found to have at least one bit set that is also set in the previous character, part of the terminal emulator's font or glyph comparison routine that computes a mask of differing bits between two character bitmaps for determining character uniqueness or collision detection.
0x000078D2 [R13] This `test al,0x4` checks bit 2 of the UART line status register (AL loaded from CH, which holds the LSR value) to determine if a break signal has been received, part of the terminal emulator's serial port interrupt handler that dispatches to break handling or character processing routines based on UART status bits.
0x0000ABC8 [R13] This `retf` instruction returns far from the terminal emulator's configuration or NVM parameter update routine, returning with carry clear to indicate successful completion after storing a new configuration pointer at [0x94a] and calling the timer synchronization subroutine at 0xaa6b.
0x0000AD61 [R13] This `popa` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x1f, 0x10, 0x09, 0x67, 0x3c, 0x5d, 0x4c, 0x2d), so this is DATA.
0x000064FF [R13] This `mov dx,0x6fb` loads the offset of a $-terminated diagnostic string (likely "Printer port test" or a status message) for printing via INT 21h AH=0x9, part of the manufacturing self-test (MFGTEST) routine's parallel printer port test that displays the test result based on the flag at [0x7af].
0x0000718B [R13] This `mov al,[0x7f0]` loads the accumulated parallel printer status error bits (from the loop that tested printer data/status registers) into AL for combining with the inverted CL mask, part of the manufacturing self-test (MFGTEST) routine's parallel printer port verification that computes the final error status byte stored at [0x7f2].
0x0000A517 [R13] This `jnz 0xa535` checks if the word at [bp-0xa] is non-zero, controlling whether to bypass a memory copy operation; part of the terminal emulator's session initialization routine that conditionally copies a context block or configuration data from a source buffer (pointed to by LDS SI) to the ES:DI destination, based on a size or flag parameter.
0x0000B7F3 [R13] This `pop es` restores the extra segment register as part of the epilogue of an interrupt handler (ending with `iret`), likely the terminal emulator's timer or cursor blink interrupt service routine that updates cursor visibility flags at [0x97b] and [0x97a] before returning from interrupt.
0x0000B3A2 [R13] This `add sp,byte +0x2` adjusts the stack pointer to discard a previously pushed word (likely a return address or saved register) before executing `iret`, part of the terminal emulator's context-switching or interrupt handler that checks whether the current terminal context matches the active display context and conditionally cleans up the stack before returning from interrupt.
0x0000BD0F [R13] This `stosw` writes the value 0x5D1F (the handler address for INT 0x38) into the interrupt vector table at 0x0000:0x0038, part of the terminal emulator's interrupt handler installation routine that sets up a custom serial port or timer interrupt handler for terminal operation.
0x00007067 [R13] This `mov ax,0xfffe` loads the value 0xFFFE (the relocation register address for the 80186 peripheral control block) to be stored into the interrupt vector table entry for INT 0x03 (at offset 0x0C), part of the manufacturing self-test (MFGTEST) routine's interrupt vector initialization that installs a custom handler (0x11DB) for hardware testing purposes.
0x00009FEA [R13] This `mov dx,0x340` loads the I/O port address of the parallel printer status register, part of the terminal emulator's printer output routine that reads the printer status (checking bits for error conditions) before sending data to the printer data port at 0x342.
0x00007685 [R13] This `movsw` instruction copies a 16-bit word from the source (font bitmap data) to the destination (video buffer at segment 0xD000), part of the terminal emulator's character rendering routine that draws glyphs by copying font patterns to screen memory with interleaved row spacing (0x1FE byte gaps between rows).
0x00006E7E [R13] This `iret` instruction returns from an interrupt handler that retrieves the active terminal context block segment from [0x914] and loads the saved BX register value from offset [bx+0x4] before restoring DS and adjusting the stack, part of the terminal emulator's context-switching or interrupt return mechanism.
0x0000A48A [R13] This `mov al,0xff` sets AL to 0xFF (indicating an error or invalid session status) when the byte at [0x8a0] is non-zero (indicating a configuration or initialization flag is set), part of the terminal emulator's session initialization routine that scans context block entries to find an available terminal session context.
0x0000BB08 [R13] This `mov bx,0x30` loads the offset 0x30 (the address of the INT 0x0C vector in the IVT) to retrieve the current video controller's interrupt vector, part of the terminal emulator's display update routine that writes a character to the video buffer at the cursor position, using the video controller's base address to compute the correct offset in video memory.
0x0000E4A7 [R13] This `inc word [bp+di]` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0xFF, 0x03, 0x00, 0x41, 0x3E, 0x5C, 0x1A, 0xD4, 0x03, 0x6A, 0x1A, 0x78, 0x1A, 0xFE, 0x03, 0x24, 0x05, 0x28, 0x04, 0x86, 0x1A, 0x36, 0x04, 0x52, 0x04, 0x94, 0x1A, 0x6E, 0x04), so this is DATA.
0x00009C49 [R13] This `mov word [es:si],0x0` zeroes the owner field (first word) of a newly created memory control block (MCB) at the extra segment, part of the kernel's DOS-compatible memory arena splitting routine that initializes the header of a free block when dividing a larger allocated block into two separate MCB entries during memory allocation operations.
0x0000AF65 [R13] This `jna 0xafb1` (jump if not above, i.e., if AL <= 1) checks if the character height value (from [0x7b8]) is 0 or 1, part of the terminal emulator's font selection routine that maps character height values (0 through 5) to corresponding attribute bytes (AH values 0x37, 0x1C, 0x12, 0x0E, 0x0B) for selecting the appropriate font table for character rendering.
0x0000A1F0 [R13] This `rep stosb` fills the remaining bytes of a string buffer with spaces (0x20) to pad or clear the field, part of the terminal emulator's filename or path parsing routine that normalizes a file name by removing a trailing period and then padding the buffer with spaces to a fixed length for subsequent filesystem operations.
0x0000B42B [R13] This `mov bx,0x40` sets the data segment to the BIOS data area (segment 0x40) to access the active terminal context pointer at [0x914], part of the terminal emulator's keyboard or serial input interrupt handler that updates the context block's input flag and conditionally refreshes the display.
0x00006D76 [R13] This `and al,0xe` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `push cs`/`pop es` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000E89E [R13] This `sbb si,dx` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `sbb` and `push es`/`push cs` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000B2CC [R13] This `mov ax,[0x914]` loads the active terminal context segment address from the global pointer at [0x914] and stores it to [0x978] (the current display context), part of the terminal emulator's context-switching routine that activates a new terminal session by updating the display context and resetting the video mode and cursor position.
0x00009F9F [R13] This `add [fs:bx+si],al` instruction is part of a character-generator font or bitmap data table (the surrounding bytes show string data like "Text1" and "Text2" followed by code), so this is DATA.
0x00006E85 [R13] This `mov ax,bx` copies the context block segment address (from BX) into AX as part of the terminal emulator's context-switching routine that searches through terminal context blocks (starting at 0x18DE) to find a matching session by comparing the segment at [bx+0x4] against the active context pointer.
0x000078CB [R13] This `add ax,0xff06` instruction is part of a jump table or dispatch table (the surrounding bytes show table entries like 0x0506, 0xFF01, 0x0203, 0x00A8) used by the terminal emulator's serial port interrupt handler to select different service routines based on the UART interrupt identification register value, so this is DATA.
0x0000ACB9 [R13] This `jmp short 0xaca2` jumps to the instruction that stores AX (value 0x1) into [0x966] and then executes an IRET, part of the terminal emulator's timer synchronization or baud-rate calibration routine that sets a flag or counter value to indicate a successful calibration or timing adjustment after processing a timer event.
0x000072D7 [R13] This `mov di,[es:bx]` loads a 16-bit value from the extra segment (pointing to a font or glyph table) at offset BX into DI, part of the terminal emulator's font bitmap or character generator table copy routine that copies glyph data from source (DS:SI) to destination (ES:DI) using `rep movsb` for rendering characters to the video buffer.
0x00006B1B [R14] This `mov ah,0x9` followed by `int 0x21` prints the $-terminated string at DX=0x4CC, which is part of the manufacturing self-test (MFGTEST) routine's success path that displays a completion message after parallel printer port verification passes, before entering an infinite loop at 0x6B1F.
0x00007381 [R14] This `add sp,byte +0x2` adjusts the stack pointer to discard a pushed word before executing `iret`, part of the terminal emulator's interrupt handler epilogue that handles font or glyph table lookups by returning from interrupt after cleaning up the stack when the character index (AL) is zero.
0x0000B4FE [R14] This `jnz 0xb502` checks if the current character byte (AL) does not match the comparison character (CL), part of the terminal emulator's escape sequence or control character parsing routine that sets CH=1 as a match flag when characters are equal during VT-style parameter scanning.
0x000073E5 [R14] This `mov [0x7b5],al` stores a font or character generator parameter byte (from offset +0xF of a configuration table at ES:SI) into the global variable at [0x7b5], part of the terminal emulator's font initialization routine that copies glyph dimension or spacing parameters from a ROM configuration table to RAM workspace.
0x00006616 [R14] This `ret` instruction returns from a subroutine that displays a test pattern character (0xE30 + offset) via INT 10h AH=0x0E and prints a status string, part of the manufacturing self-test (MFGTEST) routine's video or CRTC register verification that outputs test characters to the screen after programming video hardware registers.
0x0000B4C5 [R14] This `mov dl,[0x9c0]` loads a byte from the global variable at [0x9c0] (likely a column or cursor position parameter) as part of the terminal emulator's character rendering or cursor positioning routine that uses the pointer at [0x9bc] to access a font or glyph table for determining character placement in the video buffer.
0x0000732C [R14] This `pop bx` restores the previously saved BX register (which held a segment or pointer value) as part of the terminal emulator's font bitmap or character generator table copy routine that calculates glyph offsets by multiplying CX (character height or row count) by 4 before comparing against a size limit in DX for rendering characters to the video buffer.
0x0000E468 [R14] This `call 0x8c15:0xa815` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `adc`, `add`, and `jz`/`jo` instructions typical of 8x8 or 8x16 glyph data with values like 0x12, 0x13, 0x14, 0x15, 0x16, 0x17, 0x18), so this is DATA.
0x0000C7DB [R14] This `ja 0xc7ba` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `stosb`/`push sp` sequences and `ja`/`or` instructions typical of 8x8 or 8x16 glyph data with values like 0xAA, 0x54, 0xDD, 0x77, 0x08), so this is DATA.
0x00006078 [R14] This `mov dx,0xdb` loads the offset of a $-terminated label string (likely "CR=" or similar diagnostic label) for display via INT 21h AH=0x09, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs multiple register values or error codes as hexadecimal digits after converting them via subroutine 0x6139.
0x000060D1 [R14] This loads a 16-bit value from the stack frame (offset -0x12 from BP) for conversion to hexadecimal ASCII via subroutine 0x6139, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs multiple diagnostic register values with $-terminated label strings.
0x0000A19B [R14] This stores the destination index (DI) into a terminal context block entry at [bx], part of the terminal emulator's session initialization routine that sets up a new terminal session context by saving the far pointer (segment:offset) of the context data structure during context block allocation.
0x0000662A [R14] This outputs the constructed control byte to the parallel printer control register at I/O port 0x343, part of the manufacturing self-test (MFGTEST) routine's parallel printer port initialization that toggles control register bits (specifically testing bit 2 for auto-linefeed) and displays the resulting status.
0x0000A238 [R14] This returns from a subroutine that validates a memory control block or configuration signature by comparing 6 words at ES:DI against a reference pattern at CS:0x18CB, part of the terminal emulator's session initialization routine that scans through context block entries to find an available or valid terminal session context.
0x0000A299 [R14] This `sbb dx,[bx+0x6]` performs a 32-bit subtraction (with borrow) of the memory block's starting segment:offset from the current allocation pointer, part of the kernel's DOS-compatible memory arena allocation routine that calculates the difference between a candidate MCB's base address and the requested allocation address to determine if the block is large enough to satisfy the request.
0x0000B26A [R14] This `add bl,[bx+di]` instruction is part of a character generator font bitmap or glyph data table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x02, 0x19, 0x50, 0x10, 0x03, 0x19, 0x50, 0x10), so this is DATA.
0x0000B36D [R14] This `push es` saves the extra segment register before setting ES to 0xD000 (video buffer segment) to write a character byte (AL) to the video buffer at offset 0xC200, part of the terminal emulator's cursor blink or display update routine that directly writes a character to screen memory when the cursor visibility flag at [0x97a] is cleared.
0x0000BAED [R14] This `jz 0xbb3b` checks if the character being processed is a line feed (0x0A), part of the terminal emulator's display output routine that dispatches special control characters (backspace, carriage return, bell, line feed) to their respective handlers before writing printable characters to the video buffer at the cursor position.
0x0000AD4F [R14] This `push word 0xe41` instruction is part of a character generator font bitmap or glyph data table (the surrounding bytes show repeated patterns typical of 8x8 or 8x16 glyph data with values like 0x68, 0x41, 0x0E, 0x4B, 0x2F, 0x20, 0x11, 0x07), so this is DATA.
0x0000B9A4 [R14] This `stosb` writes a null byte (0x00) to clear the first byte of a character cell in the video buffer, part of the terminal emulator's character rendering routine that clears a row before drawing glyphs by storing a null byte followed by spaces (0x20) and then attribute bytes (BH) to the video memory at segment 0x8000.
0x0000A4AA [R14] This `mov [0x18d7],ax` stores a 16-bit value (likely a segment or pointer) into the global variable at offset 0x18D7, part of the terminal emulator's session initialization routine that saves the base address of a context block or memory arena after validating and scanning through context block entries starting at 0x8B1.
0x0000BA28 [R14] This `cmp cl,dl` compares the column register (CL) against the display width parameter (DL) to determine if the cursor position is within the visible screen area, part of the terminal emulator's display update routine that calculates scroll margins and cursor positioning parameters stored at [0x9a9], [0x9ab], and [0x9ad].
0x0000A3D2 [R14] This `pop ds` restores the data segment register after pushing ES earlier, part of the terminal emulator's session initialization routine that copies a filename or command-line string from a source buffer (pointed to by SI) to the destination at ES:DI (offset 0x80 in the context block), filtering out control characters below 0x20.
0x0000A2A2 [R14] This `mov cx,ax` updates the loop counter CX with the smaller of two size values (AX from the comparison at 0xA29E vs the original CX), part of the kernel's DOS-compatible memory arena allocation routine that determines the actual block size to allocate when the requested size (CX) exceeds the available space in the current memory control block.
0x00009C95 [R14] This `pop bx` is part of the kernel's DOS-compatible memory arena deallocation routine, restoring the BX register after saving it earlier, as part of the epilogue that merges adjacent free memory control blocks (MCBs) during memory release operations.
0x000070EE [R14] This `mov [0x800],di` stores the destination index (DI) value into the global variable at offset 0x800, part of the terminal emulator's configuration or NVM parameter loading routine that saves a pointer or handle after validating a configuration block address via subroutine 0xa69d.
0x0000B9A5 [R14] This `mov al,0x20` loads the ASCII space character (0x20) into AL to fill a video buffer row with spaces, part of the terminal emulator's display clearing or line-erase routine that uses `rep stosb` to clear a row of character cells in the video buffer before writing new glyph data.
0x0000B2F4 [R14] This `mov ax,[es:di]` loads a 16-bit value from the extra segment (pointing to a font or glyph table header) at offset DI, part of the terminal emulator's font selection routine that reads the font table's entry count or offset field to compute the address of a specific character glyph for rendering.
0x0000A645 [R14] This `push ax` saves the allocation size parameter before calling subroutine 0xa564 to copy a memory control block (MCB) header, part of the kernel's DOS-compatible memory arena splitting routine that divides a larger allocated block into two separate MCB entries during memory allocation operations.
0x00009D52 [R14] This `add sp,byte +0x2` adjusts the stack pointer to discard a previously pushed word (likely a return address or saved register) before setting the carry flag and returning with an error status, part of the kernel's DOS-compatible memory arena deallocation routine that handles the case where the extra segment (ES) is zero, indicating an invalid memory control block.
0x0000BDAD [R14] This `mov ds,di` sets the data segment to 0xD000 (the video buffer segment) to prepare for copying font or glyph data from the source buffer to video memory, part of the terminal emulator's display update routine that renders character glyphs to the video buffer using `rep movsw` to transfer 16-bit words.
0x000067DD [R14] This `mov [es:di],al` writes the value 0x50 (a test pattern byte) to the video memory at segment ES (pointing to the video controller's memory-mapped I/O or CRTC register), part of the manufacturing self-test (MFGTEST) routine's CRTC/UART register verification that writes test patterns and reads them back to verify hardware register functionality.
0x0000B649 [R14] This `push ds` saves the data segment register at the start of a dispatch routine that uses a jump table at [cs:bx+0x522a] to handle different terminal emulator commands based on the value in AH, part of the terminal emulator's command dispatch mechanism that processes VT-style escape sequences or control functions.
0x00006B89 [R14] This `mov ds,bx` sets the data segment to 0xC000 (video ROM area) to compute a checksum over the video BIOS region, part of the manufacturing self-test (MFGTEST) routine's video controller identification that validates the video BIOS by summing bytes from the video ROM segment.
0x000065D3 [R14] This `mov ax,0xe30` loads the value 0xE30 (a video mode or attribute parameter) and adds a configuration byte from [0x525] before calling INT 10h, part of the terminal emulator's video mode initialization routine that sets the display mode or attribute based on a hardware detection flag.
0x0000658B [R14] This `xor [di],cl` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of numeric strings like "3400", "3800", "3900" with 0x0D, 0x0A, 0x24 terminators typical of displayable data tables), so this is DATA.
0x00006CBE [R14] This `mov cx,0x8` sets the loop counter to 8 for processing 8 words (16 bytes) of data from a source buffer, part of the manufacturing self-test (MFGTEST) routine's display formatting function that converts raw bytes to ASCII characters (replacing non-printable bytes with '.' and checking both high and low bytes) for a hex dump or register display.
0x0000B0BC [R14] This `sti` re-enables interrupts after clearing bit 1 of the Timer 0 control register at I/O port 0xFF5E (disabling a timer function), part of the terminal emulator's timer synchronization or baud-rate calibration routine that temporarily disables a timer before comparing a 15-byte configuration string at [0x285E] against a reference pattern at [0x286D] to validate timing parameters.
0x00009A84 [R14] This `push ds` saves the data segment register before loading a pointer from [0x804] (likely the current PSP or program segment prefix) to copy the default file control block (FCB) at offset 0x5C into the kernel's internal data area, part of the DOS-compatible program loading or EXEC function that initializes FCB fields for the new process.
0x0000B4AA [R14] This `add di,0x800` advances the video memory destination pointer by 0x800 bytes (one full character row in a 80x25 text mode at 0xA000 segment) after rendering a glyph, part of the terminal emulator's character display routine that iterates through a row of characters and writes their font bitmaps to the video buffer with proper row spacing.
0x00009C06 [R14] This `mov ds,ax` sets the data segment to the segment address loaded from [0x876] (the current memory control block or arena segment), part of the kernel's DOS-compatible memory allocation routine that traverses the MCB chain to find a free block large enough to satisfy the requested allocation size.
0x00006EEF [R14] This `mov cx,0x18` sets the divisor to 24 (0x18) for converting a tick count or timer value into hours during a time-of-day calculation, part of a routine that converts a raw timer count (likely from the 80186 timer or system tick) into hours, minutes, and seconds for display or timestamp purposes.
0x00006052 [R14] This `test al,al` checks if the byte loaded from the string at [0xab + 0x914] (a context-specific label or identifier string) is zero (null terminator), part of the crash/error dump routine's display function that outputs a $-terminated label string character by character via INT 21h AH=0x02 until reaching the null byte.
0x0000AA4F [R14] This increments the pointer in SI by 2 to advance past the just-stored 16-bit value (AX) in the circular buffer at [0x946], part of the terminal emulator's timer synchronization or baud-rate calibration routine that stores timing calibration data into a circular buffer before checking for wrap-around at address 0x275E.
0x000077D2 [R14] This calls subroutine 0x7862 to process a received serial character through the terminal emulator's serial port interrupt handler, part of the routine that handles incoming data from the UART by calling the character processing function after checking the cursor blink flag at [0x97a] and the serial status byte at [0x7f5].
0x000060AF [R14] This sets AH=0x9 to prepare for an INT 21h DOS-compatible system call that will print a $-terminated string at DX=0xFA, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs formatted diagnostic values with label strings after converting register values to hexadecimal ASCII via subroutine 0x6139.
0x00006CB4 [R14] This `loop 0x6cad` decrements CX and repeats the loop that loads a word from DS:SI (pointing to video ROM at segment 0xC000), converts it to ASCII via subroutine 0x6c69, and stores a space character followed by the converted value, part of the manufacturing self-test (MFGTEST) routine's video controller identification that reads and displays the video BIOS identification string from the expansion ROM area.
0x0000B705 [R14] This calls subroutine 0xB7FC with AL=0x00 to write a byte to the video controller's CRTC register, part of the terminal emulator's display initialization routine that programs the CRT controller registers (setting horizontal/vertical timing parameters) by writing a sequence of register index values and data bytes to configure the video display mode.
0x00009DD7 [R15] This `jmp short 0x9df7` is part of the kernel's DOS-compatible memory arena deallocation routine that merges adjacent free memory control blocks (MCBs) by updating the size fields and linking pointers, jumping to the common epilogue that zeroes the owner field and marks the block as free.
0x000061E7 [R15] This `jmp short 0x61cb` is part of the manufacturing self-test (MFGTEST) routine's keyboard or serial input handler that sets a flag at [0x130] to 1 (indicating a key or character was received) and then jumps to display the character via INT 10h AH=0x0E teletype output, used for echoing input during the self-test.
0x0000AC62 [R15] This `pop bx` is part of the terminal emulator's context-switching or interrupt return routine that restores the BX register (which held a segment or pointer value) before executing a far return with 2-byte stack cleanup, returning from a subroutine that cleared the cursor blink flag at [0x7a8] during display update operations.
0x0000A560 [R15] This `mov al,0x4` loads the value 4 into AL as part of the terminal emulator's font bitmap or character generator table copy routine that selects a specific glyph rendering mode or attribute (likely indicating a 4-pixel-wide character or a particular font width parameter) before jumping to the common glyph-drawing loop at 0xA4E8.
0x00009C99 [R15] This `push es` saves the extra segment register at the start of the kernel's DOS-compatible memory arena deallocation routine, preserving the segment of the memory control block (MCB) being freed before checking its signature (0x434C "CL") and owner field to validate the block during memory release operations.
0x00009C7D [R15] This instruction sets the owner field of a memory control block (MCB) to 0x01 (indicating it belongs to the kernel/DOS arena), part of the kernel's DOS-compatible memory arena splitting routine that initializes a newly created free block header during memory allocation operations.
0x000060E7 [R15] This sets AH=0x9 to prepare for an INT 21h DOS-compatible system call that will print a $-terminated string at DX=0x119, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs formatted diagnostic values with label strings after converting register values to hexadecimal ASCII.
0x0000AF11 [R15] This stores the current code segment (CS) into the interrupt vector table at offset 0x72 (the high word of the INT 0x1C vector), part of the terminal emulator's timer or system initialization routine that installs interrupt handlers by writing segment:offset pairs to the IVT.
0x00006EF7 [R15] This exchanges AX and BX after dividing by 0x18 (24), part of a time-of-day calculation routine that converts a raw timer count into hours, minutes, and seconds by performing successive divisions (by 60 for minutes/seconds, by 24 for hours) using the 80186's DIV instruction.
0x0000A021 [R15] This calls a far subroutine at 0xF600:0x4B49 to read the keyboard controller status or scan code, part of the terminal emulator's keyboard input routine that checks for key press events and updates the status byte (BH) with modifier flags (shift, control, alt) based on the returned value.
0x00006075 [R15] This `call 0x6139` converts the 16-bit value in AX (loaded from [bp-0x8], likely a register or error code) into hexadecimal ASCII digits for display, part of the manufacturing self-test (MFGTEST) crash/error dump routine that outputs multiple register values with label strings.
0x0000E417 [R15] This `pop ss` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `adc`, `push ss`, `sbb`, and `xor` instructions typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000A793 [R15] This `mov bx,[0x914]` loads the active terminal context segment address from the global pointer to deactivate the current session by clearing byte [bx+0xa7] and calling subroutine 0x9e84, part of the terminal emulator's context-switching routine that restores a previous terminal session.
0x000079D6 [R15] This `jnz 0x79e2` checks if the byte at [bx+0x98] (a serial input or status flag) is non-zero, part of the terminal emulator's serial port interrupt handler that determines whether to return an error status (AL=0xFF) or compare cursor position values at [bx+0x90] and [bx+0x8e] to check for display update conditions.
0x0000730E [R15] This `mov ah,0x6` sets the function code for INT 0x40 (a custom system call or firmware service) to perform a font or glyph table operation, part of the terminal emulator's font bitmap copy routine that uses parameters in SI (source pointer), BX (offset), CX (count), and DX (size) to render character glyphs to the video buffer.
0x000072E2 [R15] This `add bx,bp` instruction advances the source pointer (BX) by the row stride (BP) to skip to the next character row in the font bitmap table, part of the terminal emulator's character rendering routine that copies glyph data from a ROM font table to the video buffer with interleaved row spacing.
0x000077F4 [R15] This `call far [di+0x92]` instruction performs an indirect far call through a function pointer stored in a context block or dispatch table at offset 0x92 from DI, part of the terminal emulator's serial port interrupt handler that dispatches to a specific character processing routine based on the current terminal context.
0x00006D5F [R15] This `push cs` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated `push cs`/`pop es` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x00006145 [R15] This `push ax` instruction saves the current value of AX on the stack before extracting the low nibble for hexadecimal digit conversion, part of the manufacturing self-test (MFGTEST) routine's hex-to-ASCII conversion subroutine (0x6139) that converts a 16-bit value into four hexadecimal digits for display via INT 21h AH=0x02.
0x000073C8 [R15] This `mov al,[es:si+0x9]` instruction loads a font configuration parameter (specifically the character width or spacing value at offset 0x9 of a font table header) from the extra segment into AL, part of the terminal emulator's font initialization routine that copies glyph dimension parameters from a ROM configuration table to RAM workspace variables at [0x7a8].
0x00006CE7 [R15] This `mov di,ax` sets the destination index to the incremented value from `[es:bx]` (after byte-swap), used in the MFGTEST routine's hex dump display formatter to position the output pointer for writing two space characters (0x2020) as separators before computing a 32-bit checksum over the video ROM region (0x8000 bytes at segment 0xC000).
0x0000B5B4 [R15] This `mov al,[0x9c0]` loads the current cursor column position from the global variable at [0x9c0], part of the terminal emulator's font selection routine that uses the column value along with the pointer at [0x9bc] to look up a font table entry for determining character rendering parameters.
0x00006E47 [R15] This `mov bx,0xfe8e` loads segment 0xFE8E (pointing into the ROM area) to set up the extra segment for accessing a font bitmap or character generator table, part of the terminal emulator's interrupt handler that restores a saved context or font pointer before executing an IRET.
0x000067AF [R15] This `mov dx,[es:di+0xe]` loads a 16-bit value (likely a CRTC register data word or hardware configuration parameter) from offset +0xE of a CRTC register table entry, part of the MFGTEST routine's CRTC/UART register verification that reads back test patterns written to hardware registers and displays them via INT 21h.
0x0000B00A [R15] This `mov bx,[0x914]` loads the active terminal context segment address from the global pointer at [0x914], part of the terminal emulator's timer interrupt handler that increments a counter at [bx+0x8] and updates timing flags (incrementing [0x972], decrementing [0x96d]) for cursor blink or display refresh synchronization.
0x0000665E [R15] This `mov ah,0xe` sets up the teletype output function (INT 10h AH=0Eh) to output a space character (0x20) as part of a display routine that prints a space followed by a backspace to clear or position the cursor during the manufacturing self-test (MFGTEST) video/CRTC verification.
0x0000A162 [R15] This `push ds` saves the data segment register at the start of a subroutine that initializes a new terminal session context, part of the terminal emulator's session initialization routine that calls subroutines to allocate and set up context block entries.
0x0000A613 [R15] This `cmp al,0xff` checks if the byte at [si] equals 0xFF (indicating an invalid or unused entry) during the terminal emulator's session initialization routine that scans through context block entries to find an available slot, skipping entries marked with 0xFF.
0x0000B443 [R15] This `mov bx,0xd000` loads the video buffer segment address (0xD000) into BX to set up the extra segment (ES) for writing character data to video memory, part of the terminal emulator's cursor blink or display update routine that directly writes to screen memory.
0x0000BC23 [R15] This `jz 0xbc12` checks if the OR of AX and BX is zero (indicating a null far pointer), part of the terminal emulator's display update routine that tests whether the font/glyph pointer from the INT 40h call is valid before proceeding to render characters to the video buffer.
0x0000A9F1 [R15] This `push si` saves the SI register at the start of an interrupt handler (likely a timer or serial interrupt) that reads the 80186 timer 2 control/status register at I/O 0xFF66 to check for timer events (bits 0x60), part of the terminal emulator's timing or baud-rate calibration routine that uses the on-chip timer for synchronization.
0x0000756A [R15] This `cmp si,[bx+0x8e]` compares the current input buffer pointer (SI) from the context block at [bx+0x90] against the buffer end pointer at [bx+0x8e] to check if the serial input ring buffer is empty, part of the terminal emulator's serial input handler that retrieves characters from the context block's circular buffer for processing.
0x00006647 [R15] This `mov ah,0x9` followed by `int 0x21` prints a $-terminated string at DX=0x1F8 (likely a status or error message), part of the manufacturing self-test (MFGTEST) routine's video or CRTC test that displays diagnostic output after writing test characters to the screen via INT 10h.
0x0000E6D4 [R15] This `adc ax,0x301` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `adc`, `add`, and `push ss` sequences typical of 8x8 or 8x16 glyph data with values like 0x01, 0x02, 0x03, 0x04, 0x16, 0x13, 0xFA, 0x19), so this is DATA.
0x0000AC02 [R15] This `jz 0xac0b` checks if the byte at [0x7a8] equals 2 (indicating a specific timer mode or calibration state), part of the terminal emulator's timer synchronization or baud-rate calibration routine that selects between different timing calculation paths based on the configured timer mode.
0x0000AB38 [R15] This `push ax` saves the accumulated status byte (AL) before reading Timer 2’s count register at I/O port 0xFF5A, part of the terminal emulator’s baud-rate calibration or timer synchronization routine that checks the timer’s output state (bit 4) to adjust the serial port timing.
0x00006369 [R15] This `call 0x63b5` invokes a subroutine to read a single character from the keyboard or serial input, part of the manufacturing self-test (MFGTEST) routine’s interactive input loop that echoes printable characters (≥0x20) to the screen via INT 10h AH=0x0E and stores them in a buffer at offset 0x167.
0x0000787E [R15] This `mov bx,[0x7fe]` loads the pointer to the current terminal context block from the global variable at [0x7fe], part of the terminal emulator's serial port interrupt handler that checks the context block's flags (at offset +0x96) and the global flag at [0x7ab] to determine whether to process a received character through the VT-style escape sequence parser or dispatch to a different handler based on UART status bits.
0x0000B1B5 [R15] This `mov al,[0x972]` loads the current cursor row position from the global variable at [0x972], part of the terminal emulator's cursor positioning or scroll margin calculation routine that compares the cursor row against the top margin ([0x970]) or bottom margin ([0x971]) to determine if the cursor is within the scroll region before updating the cursor position or triggering a scroll operation.
0x00007354 [R15] This `test ax,ax` checks if the value loaded from a font table entry (via [bx] after adding and subtracting BP) is zero, part of the terminal emulator's font bitmap or character generator table lookup routine that determines whether a glyph entry is valid or empty before proceeding with character rendering to the video buffer.
0x0000AE90 [R15] This `daa` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `dec sp`, `adc`, `dec bx`, `dec bp`, `dec edi`, `movsb`, `popa`, `das`, `aas` typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000AF75 [R15] This `cmp al,0x4` compares the character height value (from [0x7b8]) against 4, part of the terminal emulator's font selection routine that maps character height values (0 through 8) to corresponding attribute bytes (AH values 0x37, 0x1C, 0x12, 0x0E, 0x0B, 0x09, 0x08, 0x07) for selecting the appropriate font table for character rendering.
0x0000A94F [R15] This instruction loads the active terminal context segment address from the global pointer at [0x914], part of the terminal emulator's context-switching or interrupt return mechanism that retrieves the current session's context block to access status flags at offsets +0xA5 and +0xA6 before returning from an interrupt handler.
0x0000A6E5 [R15] This `ret` instruction returns from a subroutine that initializes the terminal emulator's context block chain, having just stored the newly allocated context segment address into global pointers at [0x914], [0x916], and [0x7ba] and zeroed the owner field at [bx+0x4], part of the session initialization routine that sets up a new terminal session context.
0x0000B5D1 [R15] This `jnc 0xb625` checks if the character index (BX) is within the valid range of the font table's glyph count (loaded from [SI]), part of the terminal emulator's font selection routine that validates the character code against the font table header before computing the glyph offset for rendering characters to the video buffer.
0x0000DF79 [R15] This `punpckldq mm2,[di]` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `insb`, `db 0x0f`, `jpe`, `xor`, `push ds`, `adc`, `cmp`, `and`, `rcl`, `ficom` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000A45D [R15] This `stosb` writes a space character (0x20) to the destination buffer at ES:DI, part of the terminal emulator's filename or path parsing routine that pads the buffer with spaces to a fixed length after removing a trailing period, normalizing the file name for subsequent filesystem operations.
0x0000A704 [R16] This `int 0x4f` is a software interrupt that likely triggers a fatal error or system halt when the terminal emulator's context block scan loop fails to find an available session slot (the loop at 0xa6f4-0xa702 exhausts the context block chain without finding a free entry), part of the session initialization routine that allocates terminal context blocks.
0x0000AAC1 [R16] This `cmp bx,[0x94a]` compares the current write pointer of the circular timing calibration buffer against the buffer's end pointer to check for wrap-around, part of the terminal emulator's baud-rate calibration or timer synchronization routine that stores timing data into a circular buffer at [0x94c] before outputting values to Timer 2's control register at I/O port 0xFF6A.
0x0000E763 [R16] This `sbb ax,0x3d4` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `sbb`, `add`, `adc`, `mov`, `xchg`, `clc`, `rcr`, `inc`, `jmp`, `push`, `outsb` sequences typical of 8x8 or 8x16 glyph data with values like 0x1D, 0x04, 0x12, 0x44, 0x88, 0x96, 0xA4, 0xB2, 0xF8, 0x01, 0x56, 0x18, 0xC0, 0x03, 0xCE, 0xD4, 0x6A, 0x1A, 0xDC, 0xFE, 0xEA, 0x02, 0x03, 0xF8, 0x06, 0x1E, 0x52, 0x14, 0x1E, 0x6E, 0x28, 0x8A, 0xB0), so this is DATA.
0x00006224 [R16] This `add dl,0x30` converts the low byte of the result from INT 0x40 function 0x26 (which likely returns a firmware version or revision number in DX) into an ASCII digit by adding 0x30 ('0'), part of the manufacturing self-test (MFGTEST) routine's version display that outputs the firmware revision number as a decimal string with a period separator.
0x0000E6C0 [R16] This `add al,0x42` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `mov sp`, `sbb`, `push ss`, `cmpsb`, `sbb`, `add`, `add`, `db 0x0f`, `enter`, `add`, `cbw`, `adc`, `add`, `push ss`, `fcom`, `pushf`, `adc`, `add`, `add`, `add`, `add`, `sbb`, `adc`, `push ss`, `adc`, `adc`, `sbb` sequences typical of 8x8 or 8x16 glyph data with values like 0x10, 0x18, 0xAE, 0x5E, 0x16, 0xA6, 0x19, 0xFF, 0x05, 0x00, 0x21, 0x03, 0x0A, 0x0F, 0xC8, 0x1B, 0xD2, 0x15, 0x01, 0x02, 0x98, 0x12, 0xB0, 0x13, 0x02, 0x04, 0x42, 0x16, 0xD8, 0x10, 0x9C, 0x11, 0x96, 0x16, 0x01, 0x01, 0xAA, 0x18, 0x02, 0x03, 0x96, 0x0F, 0x16, 0x1A, 0xFC, 0x15, 0x01, 0x03, 0x16, 0x13, 0xF6, 0x13, 0xFA, 0x19, 0x01), so this is DATA.
0x000066AF [R16] This `int 0x10` with AH=0x0E outputs a single hexadecimal digit (converted from a nibble) as a teletype character, part of the MFGTEST routine's hex-to-ASCII conversion subroutine that displays register values or test results during the manufacturing self-test's diagnostic output.
0x0000E097 [R16] This `loopne 0xe0a7` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of arithmetic and data operations typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000A90F [R16] This `mov bx,0x40` loads the segment address 0x0040 (the BIOS Data Area segment) into BX to set up the data segment for accessing BIOS timer tick or keyboard flag variables, part of the terminal emulator's context-switching routine that checks the active terminal context pointer against a saved pointer before restoring a previous session's state.
0x00006557 [R16] This `mov ah,0x9` followed by `int 0x21` prints a $-terminated string at DX (pointing to a table of baud rate labels like "300", "1200", "2400") that displays the current baud rate setting, part of the terminal emulator's configuration or setup menu routine that outputs the selected communication speed from a lookup table indexed by a configuration byte.
0x00006A25 [R16] This `loop 0x6a22` decrements CX and loops back to load the next word from the video ROM segment (0xC000) and subtract it from BX, part of the MFGTEST routine's video BIOS checksum validation that sums 0x8000 words from the video ROM region to compute a checksum and verify the video BIOS integrity.
0x000076D8 [R16] This `call far [di+0x92]` dispatches to a character processing routine via a function pointer in the terminal context block, part of the terminal emulator's serial port interrupt handler that processes received characters through VT-style escape sequence parsing or UART status handling.
0x0000E3D0 [R16] This `ror byte [0xf18],cl` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `adc`, `add`, `push ss`, and `sbb` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x000060C2 [R16] This `int 0x21` with AH=0x9 prints a $-terminated string at DX=0x102, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs formatted diagnostic values with label strings after converting register values to hexadecimal ASCII via subroutine 0x6139.
0x000060B3 [R16] This `mov ax,bp` loads the base pointer value (containing a register save area offset) to prepare for converting a stored register value to hexadecimal ASCII, part of the manufacturing self-test (MFGTEST) routine's crash/error dump display that outputs formatted diagnostic values with label strings.
0x00009D9D [R16] This `mov [es:si+0x2],cx` writes the segment address of the next memory control block (MCB) into the link field of the current MCB, part of the kernel's DOS-compatible memory arena deallocation routine that merges adjacent free blocks by updating the MCB chain pointers.
0x0000BD2A [R16] This `mov ax,0xd000` sets the extra segment (ES) to 0xD000 (the video buffer segment) to prepare for writing character glyph data to video memory, part of the terminal emulator's display update routine that renders characters to the screen.
0x0000A047 [R16] This `cli` instruction disables interrupts before writing to I/O port 0x341 (the UART or video controller register) to atomically set bits 0x0C in the byte at [0x89a] (a hardware configuration shadow register), part of the terminal emulator's keyboard or serial input handler that synchronizes with the timer calibration routine at 0xA056 by measuring elapsed ticks via INT 0x40.
0x0000E385 [R16] This `bndldx bnd0,[0x1260]` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `adc`, `stosb`, `sbb`, `aam`, `insb`, `push ss` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x000062C6 [R16] This `jnc 0x62d4` checks the carry flag after INT 21h AH=0x4B (EXEC function) to determine if loading the program (e.g., DOWNLOAD.EXE or EMULATOR.EXE) from the embedded filesystem succeeded, part of the kernel's DOS-compatible program loader that displays an error message at DX=0x2CF if the load fails.
0x0000B8D5 [R16] This `mov [0x98b],bx` stores the byte-swapped font table pointer (BX from `xchg bl,bh`) into the global variable at [0x98b], part of the terminal emulator's cursor blink or display update interrupt handler that sets the attribute byte (OR 0x08) at the glyph position in video memory (segment 0xC900) for the current cursor row and column.
0x0000695F [R16] This `jz 0x6963` checks if bit 0x2000 is clear in DX (indicating a specific memory region or attribute flag), part of the manufacturing self-test (MFGTEST) routine's video controller or CRTC register programming that sets bits in AL (0x02) to configure the hardware via I/O port 0x343, using the shadow register at [0x7b9] for atomic update.
0x0000A5C3 [R16] This `pop bx` restores the saved segment pointer from the stack after copying 8 words (16 bytes) of font glyph data via `rep movsw` from DS:SI to ES:DI, part of the terminal emulator's font bitmap or character generator table copy routine that transfers glyph data between memory segments for character rendering to the video buffer.
0x0000B66E [R16] This `add si,byte +0x4` advances the pointer through a lookup table at CS:0x526A by 4 bytes per entry, part of the terminal emulator's VT-style escape sequence parsing routine that scans a table of control character sequences to match the received byte against known command codes for dispatch to handler functions.
0x0000E383 [R16] This `add dx,bp` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `adc`, `sbb`, `stosb`, `aam`, `dec`, `add`, `sub`, `xor`, `test`, `insb`, `push ss` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000ADDB [R16] This `push byte +0x48` instruction is part of a font bitmap or character generator table (the surrounding bytes show repeated patterns of `arpl`, `push cs`, `sbb`, `and`, `sar`, `sbb`, `mov`, `jna`, `inc`, `outsw`, `xor`, `pop ss`, `and`, `jnc`, `xor`, `push ss`, `pop ds`, `inc sp`, `pusha`, `jng`, `push` sequences typical of 8x8 or 8x16 glyph data), so this is DATA.
0x0000A3DA [R16] This `stosw` stores the 16-bit value from AX (loaded from [si+0xe], the file size or timestamp field of a directory entry) into the destination buffer at ES:DI, part of the terminal emulator's filesystem or program loading routine that copies fields from a directory entry into a file control block (FCB) or parameter block during file open or EXEC operations.
0x0000A6BC [R16] This stores the current code segment (CS) into the interrupt vector table at offset 0x80 (the high word of the INT 0x20 vector), part of the terminal emulator's interrupt handler installation routine that writes segment:offset pairs to the IVT for DOS-compatible system calls.
0x00007830 [R16] This increments the pointer in BX by 2 to advance past the just-stored 16-bit value (DX) in the serial input ring buffer at [di+0x8e], part of the terminal emulator's serial port interrupt handler that stores received characters into the context block's circular buffer before checking for wrap-around.
0x00006E25 [R16] This zeros BX to prepare for calculating the interrupt vector table offset (multiplying the interrupt number by 4) before storing the handler's segment:offset at the appropriate IVT entry, part of the terminal emulator's interrupt handler installation routine that sets up custom interrupt vectors.
0x0000ABDD [R16] This is part of the terminal emulator's baud-rate calibration routine that uses a jump table at `[cs:bx]` to dispatch timer configuration commands, where the `jmp short 0xabc6` at 0xABDD returns to the calibration loop after handling a timer mode selection.
0x00006337 [R16] This `mov ah,0x9` / `int 0x21` prints a $-terminated error message at DX=0x299 when an INT 21h AH=0x3D (file open) call fails (carry set), part of the manufacturing self-test (MFGTEST) routine's file-loading error handling.
0x0000A873 [R16] This `mov ds,bx` sets the data segment to 0x40 (the BIOS Data Area segment) to access the BIOS timer tick counter or keyboard flags at [0x918] and [0x7fe], part of the terminal emulator's context-switching routine that validates and stores a new terminal context segment pointer.
0x000079D1 [R16] This `cmp byte [bx+0x98],0x0` checks the serial input status flag at offset 0x98 of the current terminal context block (pointed to by [0x914]) to determine if a character is available in the serial input ring buffer, part of the terminal emulator's serial port interrupt handler that returns AL=0xFF if no character is ready.
0x0000A07B [R16] This writes test pattern byte 0x90 to video memory at segment 0xA000 (the video buffer) and then reads it back, comparing against expected values 0x9589 and 0x9489 as part of the manufacturing self-test (MFGTEST) routine's video memory or CRTC register verification test.
0x0000DAD1 [R16] This is DATA — the bytes at 0x0000DAD1 show repeated patterns of `add [bx+si+0x40],al` and surrounding glyph-like data (0x40, 0x7E, 0x82, 0x22, 0x20) typical of an 8x8 or 8x16 character generator font bitmap table.
0x0000B742 [R16] This calls subroutine 0xB7FC with AL=0xAF to write a byte to the video controller's CRTC register, part of the terminal emulator's display initialization routine that programs CRT controller registers (setting horizontal/vertical timing parameters) based on the refresh rate configuration byte at [0x98a].
0x0000A58E [R16] This pushes SI to save the source pointer at the start of a font bitmap or character generator table copy routine, part of the terminal emulator's glyph rendering subsystem that copies font data from ROM to video memory using `rep movsw` with segment registers set from [0x89c] and [si].
0x0000B509 [R16] This pops BX to restore the pointer to the font table entry or character data buffer after calling subroutine 0xB4B1 (a glyph rendering or character display routine), part of the terminal emulator's character output loop that iterates through font table entries and renders them to the video buffer.
0x000073CC [R16] This stores a font configuration parameter (character width or spacing value) from offset +0x9 of a ROM font table header into the global variable at [0x7a8], part of the terminal emulator's font initialization routine that copies glyph dimension parameters from a ROM configuration table to RAM workspace variables.
0x00009C22 [R16] This pops registers SI, DX, CX, DS, and ES as part of the epilogue of the kernel's DOS-compatible memory arena deallocation routine, restoring saved registers before returning with carry set (STC) to indicate failure after traversing the MCB chain without finding a free block large enough.
0x00009FB8 [R16] This checks if AH equals 1 (jump-if-zero to 0xA001) in the INT 17h printer service handler, part of the kernel's parallel port driver that dispatches printer operations (initialize, status check, character output) based on the function code in AH, with this path handling printer initialization when AH=1.
0x0000B3CE [R16] This compares AL against 0x3C ('<') as part of the terminal emulator's keyboard interrupt handler (INT 9h) that processes scan codes, checking for specific key values to set the flag at [0x7af] (likely a shift or modifier key state) before returning from interrupt.
