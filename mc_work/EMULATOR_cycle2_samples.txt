=== SAMPLE 1/40  (address 0x00000177) ===
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
     0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
     0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
     00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.
     00000173  8CC0              mov ax,es
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
     00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
>>>> 00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c
     ;>>>> [R2] This line sets AH=0x34 and compares AL to 0x64 to determine a serial communication parameter (likely baud rate or timeout), then calls INT 40h with the computed value.
     0000017C  B434              mov ah,0x34
     0000017E  3C64              cmp al,0x64
     00000180  B002              mov al,0x2
     00000182  7206              jc 0x18a
     00000184  B000              mov al,0x0
     00000186  7402              jz 0x18a
     ;>>>> [R2] This line sets AL to 0x1 as the final branch in the same conditional chain for INT 40h AH=0x34, selecting the parameter when AL equals 0x64 (the zero flag is set from the prior comparison).
     00000188  B001              mov al,0x1
     ;>>>> [R3] This line calls INT 40h AH=34h with AL set to 0, 1, or 2 based on comparison with 0x64, implementing a system function that returns status or performs an action based on the value in AL.

=== SAMPLE 2/40  (address 0x00000064) ===
     00000056  BD4000            mov bp,0x40
     ;>>>> [R4] This instruction loads a word from the ES:BX pointer, which contains a video memory address for clearing a character cell and its attribute byte in the dual-plane VRAM.
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
>>>> 00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).

=== SAMPLE 3/40  (address 0x000000B4) ===
     ;>>>> [R1] The line `cmp al,0x39` at 0xA4 checks if the current ASCII digit character (starting from '1') has exceeded '9', wrapping back to '0' to fill a row of digit characters in VRAM for a screen test pattern.
     000000A4  3C39              cmp al,0x39
     ;>>>> [R1] This is a column-count-dependent digit-fill loop that writes ASCII digits '1' through '9' (wrapping to '0') into video memory, using either 0x84 (132-column) or 0x50 (80-column) count based on a comparison at [0x14] with 0x30.
     000000A6  7602              jna 0xaa
     ;>>>> [R2] This resets AL to ‘0’ (0x30) when the column-number counter exceeds ‘9’, wrapping the digit for a 132-column row-number display loop that writes characters to VRAM.
     000000A8  B030              mov al,0x30
     ;>>>> [R3] The line is the `loop 0xa1` instruction that fills a row of VRAM with incrementing ASCII digits (0-9) for a 132-column or 80-column display.
     000000AA  E2F5              loop 0xa1
     000000AC  268B4714          mov ax,[es:bx+0x14]
     ;>>>> [R3] The `xchg al,ah` at 0xB0 swaps bytes of a word loaded from `[es:bx+0x14]` to convert a little-endian stored value into a big-endian offset for VRAM addressing.
     000000B0  86C4              xchg al,ah
     000000B2  8BF8              mov di,ax
>>>> 000000B4  47                inc di
     ;>>>> [R3] The `add di,byte +0xa` at 0x0B5 advances the VRAM destination pointer by 10 bytes to skip past a field boundary (e.g., column offset or attribute gap) before copying a string.
     000000B5  83C70A            add di,byte +0xa
     000000B8  BE1500            mov si,0x15
     ;>>>> [R4] This line sets up a 40-byte (0x28) block copy operation via `rep movsb` to transfer a character row or attribute data into the video buffer, likely for a 40-column display mode.
     000000BB  B92800            mov cx,0x28
     ;>>>> [R4] Executes a `rep movsb` to copy 0x28 bytes from DS:SI (0x15) to ES:DI (a character buffer offset), used to fill a display row for video output.
     000000BE  F3A4              rep movsb
     000000C0  B402              mov ah,0x2
     000000C2  BA0018            mov dx,0x1800
     000000C5  CD10              int 0x10
     ;>>>> [R3] This line checks the keyboard buffer via INT 16h AH=01h (get keyboard status) to see if a key is pending, and jumps if zero (no key) to skip processing.
     000000C7  B401              mov ah,0x1

=== SAMPLE 4/40  (address 0x0000001E) ===
     ;>>>> [R1] The line `add bx,byte +0xf` rounds the stack pointer up to the next paragraph boundary, preparing to compute the program’s memory size for an INT 21h AH=4Ah (resize memory block) call.
     00000010  83C30F            add bx,byte +0xf
     ;>>>> [R3] This line shifts BX right by 4 bits to convert the stack pointer adjustment into a paragraph value for the INT 21h AH=4Ah (resize memory block) call.
     00000013  C1EB04            shr bx,byte 0x4
     ;>>>> [R2] This line moves the stack segment (SS) into AX to calculate the memory block size for a DOS INT 21h AH=4Ah (resize memory block) call. It computes the segment of the last used stack location, subtracts the program’s ES segment, and passes the resulting paragraph count to resize the allocated memory.
     00000016  8CD0              mov ax,ss
     ;>>>> [R1] This line computes the segment base for a memory resize operation by adding the stack segment (SS) to BX, which is part of calculating the program's memory size for INT 21h AH=4Ah (resize memory block).
     00000018  03D8              add bx,ax
     ;>>>> [R1] This line moves the ES segment register into AX to compute the difference between the program's data segment and the stack-based memory top for the INT 21h AH=4Ah resize call.
     0000001A  8CC0              mov ax,es
     ;>>>> [R1] This line subtracts the ES segment from BX (`sub bx,ax`) to compute the number of paragraphs needed for the DOS memory-resize call (INT 21h AH=4Ah), adjusting the program’s allocated memory block.
     0000001C  2BD8              sub bx,ax
>>>> 0000001E  B44A              mov ah,0x4a
     00000020  CD21              int 0x21
     ;>>>> [R3] This line sets up a DOS exit call by loading AX with 0x4C00 and then executing INT 21h to terminate the current process with return code 0.
     00000022  B83200            mov ax,0x32
     00000025  A21400            mov [0x14],al
     ;>>>> [R3] This line calls INT 10h (video BIOS) to set the video mode, likely initializing the display for the terminal emulator.
     00000028  CD10              int 0x10
     ;>>>> [R1] This line is a short jump (`jmp short 0x30`) that skips over an INT 40h call (AH=0) used to check if the kernel is ready, continuing to the main initialization loop that calls INT 40h AH=4 (get host record).
     0000002A  EB04              jmp short 0x30
     ;>>>> [R1] This line calls INT 40h with AH=0 to get a character from the serial host link (Z8530 SCC), polling until one is available, as part of the main input loop.
     0000002C  B400              mov ah,0x0
     0000002E  CD40              int 0x40
     ;>>>> [R1] This line sets AH=4 and calls INT 40h, which is the kernel’s memory allocation service that returns a segment:offset in ES:BX for the terminal’s display buffer.

=== SAMPLE 5/40  (address 0x0000007B) ===
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).
     00000079  7703              ja 0x7e
>>>> 0000007B  B94F00            mov cx,0x4f
     ;>>>> [R3] This line writes the attribute byte (AH, initially 0x20 = green on black) to the attribute plane (ES:DI-0x8000) for each character position, filling the row's attribute plane during screen clear.
     0000007E  2688A50080        mov [es:di-0x8000],ah
     ;>>>> [R1] This line stores AL (a space character 0x20) into the character plane of VRAM via `stosb`, filling a row of the display with spaces during screen clearing.
     00000083  AA                stosb
     ;>>>> [R1] The line `loop 0x7e` decrements CX and jumps back to fill a row of attribute bytes in VRAM (at ES:DI-0x8000) with the value in AH, implementing a character-row clear loop for the terminal display.
     00000084  E2F8              loop 0x7e
     ;>>>> [R1] This line decrements DX, the row counter in a nested loop that paints a character row in VRAM, looping back to fill the next row until all rows are done.
     00000086  4A                dec dx
     ;>>>> [R4] This is the inner loop of a screen-clearing routine that fills the character plane with spaces (0x20) and the attribute plane with the current attribute byte, decrementing a row counter until zero.
     00000087  75D0              jnz 0x59
     ;>>>> [R2] This line pops BX from the stack, restoring the pointer to the next character pair in the host record being processed for display.
     00000089  5B                pop bx

=== SAMPLE 6/40  (address 0x0000006C) ===
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
>>>> 0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).
     00000079  7703              ja 0x7e
     0000007B  B94F00            mov cx,0x4f
     ;>>>> [R3] This line writes the attribute byte (AH, initially 0x20 = green on black) to the attribute plane (ES:DI-0x8000) for each character position, filling the row's attribute plane during screen clear.
     0000007E  2688A50080        mov [es:di-0x8000],ah
     ;>>>> [R1] This line stores AL (a space character 0x20) into the character plane of VRAM via `stosb`, filling a row of the display with spaces during screen clearing.

=== SAMPLE 7/40  (address 0x000000BB) ===
     000000A8  B030              mov al,0x30
     ;>>>> [R3] The line is the `loop 0xa1` instruction that fills a row of VRAM with incrementing ASCII digits (0-9) for a 132-column or 80-column display.
     000000AA  E2F5              loop 0xa1
     000000AC  268B4714          mov ax,[es:bx+0x14]
     ;>>>> [R3] The `xchg al,ah` at 0xB0 swaps bytes of a word loaded from `[es:bx+0x14]` to convert a little-endian stored value into a big-endian offset for VRAM addressing.
     000000B0  86C4              xchg al,ah
     000000B2  8BF8              mov di,ax
     000000B4  47                inc di
     ;>>>> [R3] The `add di,byte +0xa` at 0x0B5 advances the VRAM destination pointer by 10 bytes to skip past a field boundary (e.g., column offset or attribute gap) before copying a string.
     000000B5  83C70A            add di,byte +0xa
     000000B8  BE1500            mov si,0x15
     ;>>>> [R4] This line sets up a 40-byte (0x28) block copy operation via `rep movsb` to transfer a character row or attribute data into the video buffer, likely for a 40-column display mode.
>>>> 000000BB  B92800            mov cx,0x28
     ;>>>> [R4] Executes a `rep movsb` to copy 0x28 bytes from DS:SI (0x15) to ES:DI (a character buffer offset), used to fill a display row for video output.
     000000BE  F3A4              rep movsb
     000000C0  B402              mov ah,0x2
     000000C2  BA0018            mov dx,0x1800
     000000C5  CD10              int 0x10
     ;>>>> [R3] This line checks the keyboard buffer via INT 16h AH=01h (get keyboard status) to see if a key is pending, and jumps if zero (no key) to skip processing.
     000000C7  B401              mov ah,0x1
     ;>>>> [R2] This line calls INT 16h with AH=1 to check if a key is available in the keyboard buffer (non-blocking keyboard status check), then jumps if zero (no key pressed).
     000000C9  CD16              int 0x16
     000000CB  744C              jz 0x119
     000000CD  B400              mov ah,0x0
     ;>>>> [R1] This line calls INT 16h with AH=0 to read a key from the keyboard buffer, blocking until a key is available, then tests the character for dispatch.

=== SAMPLE 8/40  (address 0x0000003A) ===
     ;>>>> [R1] This line calls INT 40h with AH=0 to get a character from the serial host link (Z8530 SCC), polling until one is available, as part of the main input loop.
     0000002C  B400              mov ah,0x0
     0000002E  CD40              int 0x40
     ;>>>> [R1] This line sets AH=4 and calls INT 40h, which is the kernel’s memory allocation service that returns a segment:offset in ES:BX for the terminal’s display buffer.
     00000030  B404              mov ah,0x4
     00000032  CD40              int 0x40
     00000034  8CC0              mov ax,es
     ;>>>> [R4] The `or ax,bx` instruction at 0x36 tests whether the segment register ES (in AX) and the heap size BX are both zero, branching to a reset/retry path if so; this is part of a DOS INT 21h AH=4Ah (resize memory block) call sequence.
     00000036  0BC3              or ax,bx
     ;>>>> [R1] This is a conditional jump (jnz 0x3f) that checks if the ES:BX pointer returned by INT 40h AH=4 (memory allocation) is non-null; if so, it stores the segment and offset into variables at [0x10] and [0x12] for later use.
     00000038  7505              jnz 0x3f
     ;>>>> [R1] This line stores zero (`mov [0xe],ax`) into a memory variable, clearing the segment pointer used to track allocated memory, as part of the DOS memory-resize (INT 21h AH=4Ah) and initialization sequence.
>>>> 0000003A  A30E00            mov [0xe],ax
     ;>>>> [R1] The line `jmp short 0x2c` at 0x3D loops back to `mov ah,0x0; int 0x40` to repeatedly call the kernel’s serial get-character service (INT 40h, AH=0) until a key is received.
     0000003D  EBED              jmp short 0x2c
     ;>>>> [R3] The line stores the offset of a memory block (from BX) into variable [0x10], part of the same allocation tracking as S3.
     0000003F  891E1000          mov [0x10],bx
     ;>>>> [R3] The line stores the segment of a memory block (from ES) into variable [0x12], saving it for later use in a heap/arena allocation routine.
     00000043  8C061200          mov [0x12],es
     00000047  833E0E0000        cmp word [0xe],byte +0x0
     ;>>>> [R4] This line checks if word `[0x0E]` is zero; if not zero (`jnz 0xC7`), it skips initialization, indicating this is a guard to avoid reinitializing the video buffer pointer.
     0000004C  7579              jnz 0xc7
     0000004E  8C060E00          mov [0xe],es
     ;>>>> [R2] Saves BX (a pointer into the ES segment) on the stack before reading a word from [ES:BX] to compute a VRAM destination address for clearing a character cell.
     00000052  53                push bx

=== SAMPLE 9/40  (address 0x00000146) ===
     ;>>>> [R3] The `int 0x21` at 0x133 calls DOS function AH=9 (print $-terminated string at DS:DX=0x40) to display an error or status message before exiting.
     00000133  CD21              int 0x21
     ;>>>> [R1] This is a DOS exit call via INT 21h with AX=0x4C00, terminating the current process with return code 0.
     00000135  B8004C            mov ax,0x4c00
     ;>>>> [R3] This line executes INT 21h with AH=0x4C, performing a DOS program exit (terminate with return code in AL).
     00000138  CD21              int 0x21
     ;>>>> [R3] This line increments the byte at memory address [0x6F], which is likely a counter or status variable used by the terminal emulator.
     0000013A  FE066F00          inc byte [0x6f]
     0000013E  FE066F00          inc byte [0x6f]
     ;>>>> [R1] This line decrements a counter at [0x6F] that tracks the number of pending printer output bytes, used in a printer-spooling routine that sends data via INT 17h.
     00000142  FE0E6F00          dec byte [0x6f]
     ;>>>> [R1] This loads AL from memory location [0x6F] (a counter variable), then calls INT 40h with AH=0x20 to send a character to the host via the Z8530 SCC serial link.
>>>> 00000146  A06F00            mov al,[0x6f]
     00000149  B420              mov ah,0x20
     0000014B  CD40              int 0x40
     0000014D  E9DCFE            jmp 0x2c
     ;>>>> [R4] This code initializes a printer port test by setting DX=0 (LPT1) and SI=0x70 (test pattern pointer), then calling INT 17h AH=2 to send a character to the printer, followed by checking printer status via XOR/AND operations on AH.
     00000150  33D2              xor dx,dx
     00000152  BE7000            mov si,0x70
     ;>>>> [R1] This is a printer status check loop that calls INT 17h with AH=2 to read printer status, then tests bit 7 (XOR with 0x80) and checks bits 7,5,3,0 (test 0xA9) to determine if the printer is ready/online.
     00000155  EB06              jmp short 0x15d
     ;>>>> [R4] The `mov dx,0x1` at 0x157 sets DX=1 (printer number) for an INT 17h AH=2 (print character) call, sending the byte at DS:SI (0xBB) to the printer.
     00000157  BA0100            mov dx,0x1
     0000015A  BEBB00            mov si,0xbb
     0000015D  B402              mov ah,0x2

=== SAMPLE 10/40  (address 0x000000C5) ===
     000000B0  86C4              xchg al,ah
     000000B2  8BF8              mov di,ax
     000000B4  47                inc di
     ;>>>> [R3] The `add di,byte +0xa` at 0x0B5 advances the VRAM destination pointer by 10 bytes to skip past a field boundary (e.g., column offset or attribute gap) before copying a string.
     000000B5  83C70A            add di,byte +0xa
     000000B8  BE1500            mov si,0x15
     ;>>>> [R4] This line sets up a 40-byte (0x28) block copy operation via `rep movsb` to transfer a character row or attribute data into the video buffer, likely for a 40-column display mode.
     000000BB  B92800            mov cx,0x28
     ;>>>> [R4] Executes a `rep movsb` to copy 0x28 bytes from DS:SI (0x15) to ES:DI (a character buffer offset), used to fill a display row for video output.
     000000BE  F3A4              rep movsb
     000000C0  B402              mov ah,0x2
     000000C2  BA0018            mov dx,0x1800
>>>> 000000C5  CD10              int 0x10
     ;>>>> [R3] This line checks the keyboard buffer via INT 16h AH=01h (get keyboard status) to see if a key is pending, and jumps if zero (no key) to skip processing.
     000000C7  B401              mov ah,0x1
     ;>>>> [R2] This line calls INT 16h with AH=1 to check if a key is available in the keyboard buffer (non-blocking keyboard status check), then jumps if zero (no key pressed).
     000000C9  CD16              int 0x16
     000000CB  744C              jz 0x119
     000000CD  B400              mov ah,0x0
     ;>>>> [R1] This line calls INT 16h with AH=0 to read a key from the keyboard buffer, blocking until a key is available, then tests the character for dispatch.
     000000CF  CD16              int 0x16
     000000D1  84C0              test al,al
     ;>>>> [R2] This conditional jump checks if the keyboard scan code (AL) is zero after an INT 16h AH=0 (read key) call. If AL=0 (extended key), it jumps to 0x119 to skip processing; otherwise, it proceeds to handle the character with INT 21h AH=7 (direct console input without echo).
     000000D3  7444              jz 0x119
     ;>>>> [R4] This sets AH=7 for a keyboard input call via INT 16h (keyboard BIOS), which reads a character without echo, used in the keyboard dispatch chain to process typed input.

=== SAMPLE 11/40  (address 0x000000C7) ===
     000000B4  47                inc di
     ;>>>> [R3] The `add di,byte +0xa` at 0x0B5 advances the VRAM destination pointer by 10 bytes to skip past a field boundary (e.g., column offset or attribute gap) before copying a string.
     000000B5  83C70A            add di,byte +0xa
     000000B8  BE1500            mov si,0x15
     ;>>>> [R4] This line sets up a 40-byte (0x28) block copy operation via `rep movsb` to transfer a character row or attribute data into the video buffer, likely for a 40-column display mode.
     000000BB  B92800            mov cx,0x28
     ;>>>> [R4] Executes a `rep movsb` to copy 0x28 bytes from DS:SI (0x15) to ES:DI (a character buffer offset), used to fill a display row for video output.
     000000BE  F3A4              rep movsb
     000000C0  B402              mov ah,0x2
     000000C2  BA0018            mov dx,0x1800
     000000C5  CD10              int 0x10
     ;>>>> [R3] This line checks the keyboard buffer via INT 16h AH=01h (get keyboard status) to see if a key is pending, and jumps if zero (no key) to skip processing.
>>>> 000000C7  B401              mov ah,0x1
     ;>>>> [R2] This line calls INT 16h with AH=1 to check if a key is available in the keyboard buffer (non-blocking keyboard status check), then jumps if zero (no key pressed).
     000000C9  CD16              int 0x16
     000000CB  744C              jz 0x119
     000000CD  B400              mov ah,0x0
     ;>>>> [R1] This line calls INT 16h with AH=0 to read a key from the keyboard buffer, blocking until a key is available, then tests the character for dispatch.
     000000CF  CD16              int 0x16
     000000D1  84C0              test al,al
     ;>>>> [R2] This conditional jump checks if the keyboard scan code (AL) is zero after an INT 16h AH=0 (read key) call. If AL=0 (extended key), it jumps to 0x119 to skip processing; otherwise, it proceeds to handle the character with INT 21h AH=7 (direct console input without echo).
     000000D3  7444              jz 0x119
     ;>>>> [R4] This sets AH=7 for a keyboard input call via INT 16h (keyboard BIOS), which reads a character without echo, used in the keyboard dispatch chain to process typed input.
     000000D5  B407              mov ah,0x7
     

=== SAMPLE 12/40  (address 0x0000005C) ===
     ;>>>> [R4] This line checks if word `[0x0E]` is zero; if not zero (`jnz 0xC7`), it skips initialization, indicating this is a guard to avoid reinitializing the video buffer pointer.
     0000004C  7579              jnz 0xc7
     0000004E  8C060E00          mov [0xe],es
     ;>>>> [R2] Saves BX (a pointer into the ES segment) on the stack before reading a word from [ES:BX] to compute a VRAM destination address for clearing a character cell.
     00000052  53                push bx
     ;>>>> [R1] This line loads DX with 0x1E, which is the offset of a data structure used in the video attribute/character painting loop that follows, where BP is set to 0x40 (starting attribute value) and the code writes characters and attributes to the dual-plane VRAM.
     00000053  BA1E00            mov dx,0x1e
     ;>>>> [R4] This instruction sets BP to 0x40 (64 decimal), which serves as the initial attribute value for the first character cell being cleared in the video memory initialization routine.
     00000056  BD4000            mov bp,0x40
     ;>>>> [R4] This instruction loads a word from the ES:BX pointer, which contains a video memory address for clearing a character cell and its attribute byte in the dual-plane VRAM.
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
>>>> 0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp

=== SAMPLE 13/40  (address 0x00000001) ===
     ; EMULATOR.EXE - code/data-separated disassembly (MZ load module, 660 B, header 512 B stripped)
     ; entry @0x0000; 1 relocs. Classified CODE-unless-data-filter-fires
     ; (recursive descent under-covers C-runtime startups). Bytes: 365 code / 288 string / 7 data.
     ;
     
     ; ---- 0x0000-0x00d7 CODE ----
     ;>>>> [R2] This line pushes DS onto the stack as the first instruction of the module, saving the original data segment for later restoration during program initialization.
     00000000  1E                push ds
     ;>>>> [R1] This is the program entry point that sets DS=0x19 (the PSP segment), displays a sign-on string via INT 21h AH=9, then calculates the resident code size for possible termination-and-stay-resident setup.
>>>> 00000001  B81900            mov ax,0x19
     ;>>>> [R4] The `mov ds,ax` at 0x04 sets the data segment to 0x19 (the PSP segment) immediately after entry, as part of the standard DOS program startup sequence before calling INT 21h AH=9 to print a string.
     00000004  8ED8              mov ds,ax
     00000006  B409              mov ah,0x9
     ;>>>> [R3] The `mov dx,0x15` at 0x08 sets DS:DX to point to a $-terminated string at offset 0x15 for DOS print via INT 21h AH=9.
     00000008  BA1500            mov dx,0x15
     ;>>>> [R2] This line calls DOS INT 21h function 09h to print a $-terminated string at DS:DX, displaying a startup or status message from the terminal emulator.
     0000000B  CD21              int 0x21
     ;>>>> [R1] The line `pop es` at 0x0D loads the program’s original ES segment (saved by `push ds`), used to compute the memory block size for the INT 21h AH=4Ah (resize memory block) call that follows.
     0000000D  07                pop es
     ;>>>> [R1] The line `mov bx,sp` captures the current stack pointer, which is the first step in calculating the program’s memory footprint for the subsequent INT 21h AH=4Ah resize operation.
     0000000E  8BDC              mov bx,sp
     ;>>>> [R1] The line `add bx,byte +0xf` rounds the stack pointer up to the next paragraph boundary, preparing to compute the program’s memory size for an INT 21h AH=4Ah (resize memory block) call.

=== SAMPLE 14/40  (address 0x0000002C) ===
     ;>>>> [R1] This line subtracts the ES segment from BX (`sub bx,ax`) to compute the number of paragraphs needed for the DOS memory-resize call (INT 21h AH=4Ah), adjusting the program’s allocated memory block.
     0000001C  2BD8              sub bx,ax
     0000001E  B44A              mov ah,0x4a
     00000020  CD21              int 0x21
     ;>>>> [R3] This line sets up a DOS exit call by loading AX with 0x4C00 and then executing INT 21h to terminate the current process with return code 0.
     00000022  B83200            mov ax,0x32
     00000025  A21400            mov [0x14],al
     ;>>>> [R3] This line calls INT 10h (video BIOS) to set the video mode, likely initializing the display for the terminal emulator.
     00000028  CD10              int 0x10
     ;>>>> [R1] This line is a short jump (`jmp short 0x30`) that skips over an INT 40h call (AH=0) used to check if the kernel is ready, continuing to the main initialization loop that calls INT 40h AH=4 (get host record).
     0000002A  EB04              jmp short 0x30
     ;>>>> [R1] This line calls INT 40h with AH=0 to get a character from the serial host link (Z8530 SCC), polling until one is available, as part of the main input loop.
>>>> 0000002C  B400              mov ah,0x0
     0000002E  CD40              int 0x40
     ;>>>> [R1] This line sets AH=4 and calls INT 40h, which is the kernel’s memory allocation service that returns a segment:offset in ES:BX for the terminal’s display buffer.
     00000030  B404              mov ah,0x4
     00000032  CD40              int 0x40
     00000034  8CC0              mov ax,es
     ;>>>> [R4] The `or ax,bx` instruction at 0x36 tests whether the segment register ES (in AX) and the heap size BX are both zero, branching to a reset/retry path if so; this is part of a DOS INT 21h AH=4Ah (resize memory block) call sequence.
     00000036  0BC3              or ax,bx
     ;>>>> [R1] This is a conditional jump (jnz 0x3f) that checks if the ES:BX pointer returned by INT 40h AH=4 (memory allocation) is non-null; if so, it stores the segment and offset into variables at [0x10] and [0x12] for later use.
     00000038  7505              jnz 0x3f
     ;>>>> [R1] This line stores zero (`mov [0xe],ax`) into a memory variable, clearing the segment pointer used to track allocated memory, as part of the DOS memory-resize (INT 21h AH=4Ah) and initialization sequence.
     0000003A  A30E00            mov [0xe],ax
     ;>>>> [R1] The line `jmp short 0x2c` at 0x3D loops back to `mov ah,0x0; int 0x40` to repeatedly call the kernel’s serial get-character service (INT 40h, AH=0) until a key is received.

=== SAMPLE 15/40  (address 0x000000A4) ===
     ;>>>> [R3] This line loads AL with 0x31 (ASCII '1'), the starting digit character for a row-fill loop that writes digits 1-9 repeatedly across the screen, used for a display test pattern.
     00000095  B031              mov al,0x31
     ;>>>> [R4] This code checks if the terminal is in 132-column mode by comparing byte at [0x14] to 0x30, and sets CX to 0x50 (80 columns) if below/equal or 0x84 (132 columns) if above, controlling the number of fill characters written to VRAM.
     00000097  803E140030        cmp byte [0x14],0x30
     0000009C  7703              ja 0xa1
     ;>>>> [R2] This line sets CX to 0x50 (80 decimal) for a loop that writes digit characters to VRAM, overriding the default 0x84 (132) when the terminal is in 80-column mode (byte at [0x14] <= 0x30). It is part of a routine that fills a row with incrementing digits (0-9) for a display test.
     0000009E  B95000            mov cx,0x50
     ;>>>> [R3] This line writes the character in AL (starting at '1') to the video character plane via `STOSB`, filling a row with digits 1-9 repeatedly based on column count (0x84 or 0x50) determined by a 80/132 column flag at [0x14].
     000000A1  AA                stosb
     ;>>>> [R3] This increments AL (the current character being written) and compares it to '9', wrapping back to '0' if exceeded, as part of a loop that fills a row of the display with incrementing digits '1' through '9' in the character plane of VRAM.
     000000A2  FEC0              inc al
     ;>>>> [R1] The line `cmp al,0x39` at 0xA4 checks if the current ASCII digit character (starting from '1') has exceeded '9', wrapping back to '0' to fill a row of digit characters in VRAM for a screen test pattern.
>>>> 000000A4  3C39              cmp al,0x39
     ;>>>> [R1] This is a column-count-dependent digit-fill loop that writes ASCII digits '1' through '9' (wrapping to '0') into video memory, using either 0x84 (132-column) or 0x50 (80-column) count based on a comparison at [0x14] with 0x30.
     000000A6  7602              jna 0xaa
     ;>>>> [R2] This resets AL to ‘0’ (0x30) when the column-number counter exceeds ‘9’, wrapping the digit for a 132-column row-number display loop that writes characters to VRAM.
     000000A8  B030              mov al,0x30
     ;>>>> [R3] The line is the `loop 0xa1` instruction that fills a row of VRAM with incrementing ASCII digits (0-9) for a 132-column or 80-column display.
     000000AA  E2F5              loop 0xa1
     000000AC  268B4714          mov ax,[es:bx+0x14]
     ;>>>> [R3] The `xchg al,ah` at 0xB0 swaps bytes of a word loaded from `[es:bx+0x14]` to convert a little-endian stored value into a big-endian offset for VRAM addressing.
     000000B0  86C4              xchg al,ah
     000000B2  8BF8              mov di,ax
     000000B4  47                inc di
     ;>>>> [R3] The `add di,byte +0xa` at 0x0B5 advances the VRAM destination pointer by 10 bytes to skip past a field boundary (e.g., column offset or attribute gap) before copying a string.

=== SAMPLE 16/40  (address 0x00000171) ===
     ;>>>> [R2] This line calls INT 17h (printer service) with AH=2 to send a character to the printer port, then checks the printer status by XORing AH with 0x80 and testing bits 0xA9.
     0000015F  CD17              int 0x17
     00000161  80F480            xor ah,0x80
     00000164  F6C4A9            test ah,0xa9
     ;>>>> [R1] This line is a conditional jump (`jz 0x18f`) that branches if the printer status (from INT 17h AH=2) indicates no error, checking the `test ah,0xa9` result after XORing with 0x80 to mask the busy bit.
     00000167  7426              jz 0x18f
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
     0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
     0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
>>>> 00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.
     00000173  8CC0              mov ax,es
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
     00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
     00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c
     ;>>>> [R2] This line sets AH=0x34 and compares AL to 0x64 to determine a serial communication parameter (likely baud rate or timeout), then calls INT 40h with the computed value.
     0000017C  B434              mov ah,0x34
     0000017E  3C64              cmp al,0x64
     00000180  B002              mov al,0x2

=== SAMPLE 17/40  (address 0x0000006D) ===
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
>>>> 0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).
     00000079  7703              ja 0x7e
     0000007B  B94F00            mov cx,0x4f
     ;>>>> [R3] This line writes the attribute byte (AH, initially 0x20 = green on black) to the attribute plane (ES:DI-0x8000) for each character position, filling the row's attribute plane during screen clear.
     0000007E  2688A50080        mov [es:di-0x8000],ah
     ;>>>> [R1] This line stores AL (a space character 0x20) into the character plane of VRAM via `stosb`, filling a row of the display with spaces during screen clearing.
     00000083  AA                stosb

=== SAMPLE 18/40  (address 0x00000006) ===
     ; EMULATOR.EXE - code/data-separated disassembly (MZ load module, 660 B, header 512 B stripped)
     ; entry @0x0000; 1 relocs. Classified CODE-unless-data-filter-fires
     ; (recursive descent under-covers C-runtime startups). Bytes: 365 code / 288 string / 7 data.
     ;
     
     ; ---- 0x0000-0x00d7 CODE ----
     ;>>>> [R2] This line pushes DS onto the stack as the first instruction of the module, saving the original data segment for later restoration during program initialization.
     00000000  1E                push ds
     ;>>>> [R1] This is the program entry point that sets DS=0x19 (the PSP segment), displays a sign-on string via INT 21h AH=9, then calculates the resident code size for possible termination-and-stay-resident setup.
     00000001  B81900            mov ax,0x19
     ;>>>> [R4] The `mov ds,ax` at 0x04 sets the data segment to 0x19 (the PSP segment) immediately after entry, as part of the standard DOS program startup sequence before calling INT 21h AH=9 to print a string.
     00000004  8ED8              mov ds,ax
>>>> 00000006  B409              mov ah,0x9
     ;>>>> [R3] The `mov dx,0x15` at 0x08 sets DS:DX to point to a $-terminated string at offset 0x15 for DOS print via INT 21h AH=9.
     00000008  BA1500            mov dx,0x15
     ;>>>> [R2] This line calls DOS INT 21h function 09h to print a $-terminated string at DS:DX, displaying a startup or status message from the terminal emulator.
     0000000B  CD21              int 0x21
     ;>>>> [R1] The line `pop es` at 0x0D loads the program’s original ES segment (saved by `push ds`), used to compute the memory block size for the INT 21h AH=4Ah (resize memory block) call that follows.
     0000000D  07                pop es
     ;>>>> [R1] The line `mov bx,sp` captures the current stack pointer, which is the first step in calculating the program’s memory footprint for the subsequent INT 21h AH=4Ah resize operation.
     0000000E  8BDC              mov bx,sp
     ;>>>> [R1] The line `add bx,byte +0xf` rounds the stack pointer up to the next paragraph boundary, preparing to compute the program’s memory size for an INT 21h AH=4Ah (resize memory block) call.
     00000010  83C30F            add bx,byte +0xf
     ;>>>> [R3] This line shifts BX right by 4 bits to convert the stack pointer adjustment into a paragraph value for the INT 21h AH=4Ah (resize memory block) call.
     00000013  C1EB04            shr bx,byte 0x4

=== SAMPLE 19/40  (address 0x0000005D) ===
     0000004E  8C060E00          mov [0xe],es
     ;>>>> [R2] Saves BX (a pointer into the ES segment) on the stack before reading a word from [ES:BX] to compute a VRAM destination address for clearing a character cell.
     00000052  53                push bx
     ;>>>> [R1] This line loads DX with 0x1E, which is the offset of a data structure used in the video attribute/character painting loop that follows, where BP is set to 0x40 (starting attribute value) and the code writes characters and attributes to the dual-plane VRAM.
     00000053  BA1E00            mov dx,0x1e
     ;>>>> [R4] This instruction sets BP to 0x40 (64 decimal), which serves as the initial attribute value for the first character cell being cleared in the video memory initialization routine.
     00000056  BD4000            mov bp,0x40
     ;>>>> [R4] This instruction loads a word from the ES:BX pointer, which contains a video memory address for clearing a character cell and its attribute byte in the dual-plane VRAM.
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
>>>> 0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp

=== SAMPLE 20/40  (address 0x0000015F) ===
     0000014B  CD40              int 0x40
     0000014D  E9DCFE            jmp 0x2c
     ;>>>> [R4] This code initializes a printer port test by setting DX=0 (LPT1) and SI=0x70 (test pattern pointer), then calling INT 17h AH=2 to send a character to the printer, followed by checking printer status via XOR/AND operations on AH.
     00000150  33D2              xor dx,dx
     00000152  BE7000            mov si,0x70
     ;>>>> [R1] This is a printer status check loop that calls INT 17h with AH=2 to read printer status, then tests bit 7 (XOR with 0x80) and checks bits 7,5,3,0 (test 0xA9) to determine if the printer is ready/online.
     00000155  EB06              jmp short 0x15d
     ;>>>> [R4] The `mov dx,0x1` at 0x157 sets DX=1 (printer number) for an INT 17h AH=2 (print character) call, sending the byte at DS:SI (0xBB) to the printer.
     00000157  BA0100            mov dx,0x1
     0000015A  BEBB00            mov si,0xbb
     0000015D  B402              mov ah,0x2
     ;>>>> [R2] This line calls INT 17h (printer service) with AH=2 to send a character to the printer port, then checks the printer status by XORing AH with 0x80 and testing bits 0xA9.
>>>> 0000015F  CD17              int 0x17
     00000161  80F480            xor ah,0x80
     00000164  F6C4A9            test ah,0xa9
     ;>>>> [R1] This line is a conditional jump (`jz 0x18f`) that branches if the printer status (from INT 17h AH=2) indicates no error, checking the `test ah,0xa9` result after XORing with 0x80 to mask the busy bit.
     00000167  7426              jz 0x18f
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
     0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
     0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
     00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.

=== SAMPLE 21/40  (address 0x0000006A) ===
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
>>>> 0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).
     00000079  7703              ja 0x7e
     0000007B  B94F00            mov cx,0x4f
     ;>>>> [R3] This line writes the attribute byte (AH, initially 0x20 = green on black) to the attribute plane (ES:DI-0x8000) for each character position, filling the row's attribute plane during screen clear.
     0000007E  2688A50080        mov [es:di-0x8000],ah

=== SAMPLE 22/40  (address 0x0000016F) ===
     0000015A  BEBB00            mov si,0xbb
     0000015D  B402              mov ah,0x2
     ;>>>> [R2] This line calls INT 17h (printer service) with AH=2 to send a character to the printer port, then checks the printer status by XORing AH with 0x80 and testing bits 0xA9.
     0000015F  CD17              int 0x17
     00000161  80F480            xor ah,0x80
     00000164  F6C4A9            test ah,0xa9
     ;>>>> [R1] This line is a conditional jump (`jz 0x18f`) that branches if the printer status (from INT 17h AH=2) indicates no error, checking the `test ah,0xa9` result after XORing with 0x80 to mask the busy bit.
     00000167  7426              jz 0x18f
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
     0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
>>>> 0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
     00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.
     00000173  8CC0              mov ax,es
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
     00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
     00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c
     ;>>>> [R2] This line sets AH=0x34 and compares AL to 0x64 to determine a serial communication parameter (likely baud rate or timeout), then calls INT 40h with the computed value.
     0000017C  B434              mov ah,0x34

=== SAMPLE 23/40  (address 0x0000005E) ===
     00000052  53                push bx
     ;>>>> [R1] This line loads DX with 0x1E, which is the offset of a data structure used in the video attribute/character painting loop that follows, where BP is set to 0x40 (starting attribute value) and the code writes characters and attributes to the dual-plane VRAM.
     00000053  BA1E00            mov dx,0x1e
     ;>>>> [R4] This instruction sets BP to 0x40 (64 decimal), which serves as the initial attribute value for the first character cell being cleared in the video memory initialization routine.
     00000056  BD4000            mov bp,0x40
     ;>>>> [R4] This instruction loads a word from the ES:BX pointer, which contains a video memory address for clearing a character cell and its attribute byte in the dual-plane VRAM.
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
>>>> 0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20

=== SAMPLE 24/40  (address 0x00000173) ===
     00000161  80F480            xor ah,0x80
     00000164  F6C4A9            test ah,0xa9
     ;>>>> [R1] This line is a conditional jump (`jz 0x18f`) that branches if the printer status (from INT 17h AH=2) indicates no error, checking the `test ah,0xa9` result after XORing with 0x80 to mask the busy bit.
     00000167  7426              jz 0x18f
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
     0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
     0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
     00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.
>>>> 00000173  8CC0              mov ax,es
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
     00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
     00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c
     ;>>>> [R2] This line sets AH=0x34 and compares AL to 0x64 to determine a serial communication parameter (likely baud rate or timeout), then calls INT 40h with the computed value.
     0000017C  B434              mov ah,0x34
     0000017E  3C64              cmp al,0x64
     00000180  B002              mov al,0x2
     00000182  7206              jc 0x18a
     00000184  B000              mov al,0x0

=== SAMPLE 25/40  (address 0x00000065) ===
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
>>>> 00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).
     00000079  7703              ja 0x7e
     0000007B  B94F00            mov cx,0x4f

=== SAMPLE 26/40  (address 0x0000014B) ===
     ;>>>> [R1] This is a DOS exit call via INT 21h with AX=0x4C00, terminating the current process with return code 0.
     00000135  B8004C            mov ax,0x4c00
     ;>>>> [R3] This line executes INT 21h with AH=0x4C, performing a DOS program exit (terminate with return code in AL).
     00000138  CD21              int 0x21
     ;>>>> [R3] This line increments the byte at memory address [0x6F], which is likely a counter or status variable used by the terminal emulator.
     0000013A  FE066F00          inc byte [0x6f]
     0000013E  FE066F00          inc byte [0x6f]
     ;>>>> [R1] This line decrements a counter at [0x6F] that tracks the number of pending printer output bytes, used in a printer-spooling routine that sends data via INT 17h.
     00000142  FE0E6F00          dec byte [0x6f]
     ;>>>> [R1] This loads AL from memory location [0x6F] (a counter variable), then calls INT 40h with AH=0x20 to send a character to the host via the Z8530 SCC serial link.
     00000146  A06F00            mov al,[0x6f]
     00000149  B420              mov ah,0x20
>>>> 0000014B  CD40              int 0x40
     0000014D  E9DCFE            jmp 0x2c
     ;>>>> [R4] This code initializes a printer port test by setting DX=0 (LPT1) and SI=0x70 (test pattern pointer), then calling INT 17h AH=2 to send a character to the printer, followed by checking printer status via XOR/AND operations on AH.
     00000150  33D2              xor dx,dx
     00000152  BE7000            mov si,0x70
     ;>>>> [R1] This is a printer status check loop that calls INT 17h with AH=2 to read printer status, then tests bit 7 (XOR with 0x80) and checks bits 7,5,3,0 (test 0xA9) to determine if the printer is ready/online.
     00000155  EB06              jmp short 0x15d
     ;>>>> [R4] The `mov dx,0x1` at 0x157 sets DX=1 (printer number) for an INT 17h AH=2 (print character) call, sending the byte at DS:SI (0xBB) to the printer.
     00000157  BA0100            mov dx,0x1
     0000015A  BEBB00            mov si,0xbb
     0000015D  B402              mov ah,0x2
     ;>>>> [R2] This line calls INT 17h (printer service) with AH=2 to send a character to the printer port, then checks the printer status by XORing AH with 0x80 and testing bits 0xA9.
     0000015F  CD17              int 0x17

=== SAMPLE 27/40  (address 0x00000034) ===
     00000022  B83200            mov ax,0x32
     00000025  A21400            mov [0x14],al
     ;>>>> [R3] This line calls INT 10h (video BIOS) to set the video mode, likely initializing the display for the terminal emulator.
     00000028  CD10              int 0x10
     ;>>>> [R1] This line is a short jump (`jmp short 0x30`) that skips over an INT 40h call (AH=0) used to check if the kernel is ready, continuing to the main initialization loop that calls INT 40h AH=4 (get host record).
     0000002A  EB04              jmp short 0x30
     ;>>>> [R1] This line calls INT 40h with AH=0 to get a character from the serial host link (Z8530 SCC), polling until one is available, as part of the main input loop.
     0000002C  B400              mov ah,0x0
     0000002E  CD40              int 0x40
     ;>>>> [R1] This line sets AH=4 and calls INT 40h, which is the kernel’s memory allocation service that returns a segment:offset in ES:BX for the terminal’s display buffer.
     00000030  B404              mov ah,0x4
     00000032  CD40              int 0x40
>>>> 00000034  8CC0              mov ax,es
     ;>>>> [R4] The `or ax,bx` instruction at 0x36 tests whether the segment register ES (in AX) and the heap size BX are both zero, branching to a reset/retry path if so; this is part of a DOS INT 21h AH=4Ah (resize memory block) call sequence.
     00000036  0BC3              or ax,bx
     ;>>>> [R1] This is a conditional jump (jnz 0x3f) that checks if the ES:BX pointer returned by INT 40h AH=4 (memory allocation) is non-null; if so, it stores the segment and offset into variables at [0x10] and [0x12] for later use.
     00000038  7505              jnz 0x3f
     ;>>>> [R1] This line stores zero (`mov [0xe],ax`) into a memory variable, clearing the segment pointer used to track allocated memory, as part of the DOS memory-resize (INT 21h AH=4Ah) and initialization sequence.
     0000003A  A30E00            mov [0xe],ax
     ;>>>> [R1] The line `jmp short 0x2c` at 0x3D loops back to `mov ah,0x0; int 0x40` to repeatedly call the kernel’s serial get-character service (INT 40h, AH=0) until a key is received.
     0000003D  EBED              jmp short 0x2c
     ;>>>> [R3] The line stores the offset of a memory block (from BX) into variable [0x10], part of the same allocation tracking as S3.
     0000003F  891E1000          mov [0x10],bx
     ;>>>> [R3] The line stores the segment of a memory block (from ES) into variable [0x12], saving it for later use in a heap/arena allocation routine.
     00000043  8C061200          mov [0x12],es

=== SAMPLE 28/40  (address 0x00000062) ===
     00000053  BA1E00            mov dx,0x1e
     ;>>>> [R4] This instruction sets BP to 0x40 (64 decimal), which serves as the initial attribute value for the first character cell being cleared in the video memory initialization routine.
     00000056  BD4000            mov bp,0x40
     ;>>>> [R4] This instruction loads a word from the ES:BX pointer, which contains a video memory address for clearing a character cell and its attribute byte in the dual-plane VRAM.
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
     00000060  8BF8              mov di,ax
>>>> 00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.

=== SAMPLE 29/40  (address 0x000000A6) ===
     ;>>>> [R4] This code checks if the terminal is in 132-column mode by comparing byte at [0x14] to 0x30, and sets CX to 0x50 (80 columns) if below/equal or 0x84 (132 columns) if above, controlling the number of fill characters written to VRAM.
     00000097  803E140030        cmp byte [0x14],0x30
     0000009C  7703              ja 0xa1
     ;>>>> [R2] This line sets CX to 0x50 (80 decimal) for a loop that writes digit characters to VRAM, overriding the default 0x84 (132) when the terminal is in 80-column mode (byte at [0x14] <= 0x30). It is part of a routine that fills a row with incrementing digits (0-9) for a display test.
     0000009E  B95000            mov cx,0x50
     ;>>>> [R3] This line writes the character in AL (starting at '1') to the video character plane via `STOSB`, filling a row with digits 1-9 repeatedly based on column count (0x84 or 0x50) determined by a 80/132 column flag at [0x14].
     000000A1  AA                stosb
     ;>>>> [R3] This increments AL (the current character being written) and compares it to '9', wrapping back to '0' if exceeded, as part of a loop that fills a row of the display with incrementing digits '1' through '9' in the character plane of VRAM.
     000000A2  FEC0              inc al
     ;>>>> [R1] The line `cmp al,0x39` at 0xA4 checks if the current ASCII digit character (starting from '1') has exceeded '9', wrapping back to '0' to fill a row of digit characters in VRAM for a screen test pattern.
     000000A4  3C39              cmp al,0x39
     ;>>>> [R1] This is a column-count-dependent digit-fill loop that writes ASCII digits '1' through '9' (wrapping to '0') into video memory, using either 0x84 (132-column) or 0x50 (80-column) count based on a comparison at [0x14] with 0x30.
>>>> 000000A6  7602              jna 0xaa
     ;>>>> [R2] This resets AL to ‘0’ (0x30) when the column-number counter exceeds ‘9’, wrapping the digit for a 132-column row-number display loop that writes characters to VRAM.
     000000A8  B030              mov al,0x30
     ;>>>> [R3] The line is the `loop 0xa1` instruction that fills a row of VRAM with incrementing ASCII digits (0-9) for a 132-column or 80-column display.
     000000AA  E2F5              loop 0xa1
     000000AC  268B4714          mov ax,[es:bx+0x14]
     ;>>>> [R3] The `xchg al,ah` at 0xB0 swaps bytes of a word loaded from `[es:bx+0x14]` to convert a little-endian stored value into a big-endian offset for VRAM addressing.
     000000B0  86C4              xchg al,ah
     000000B2  8BF8              mov di,ax
     000000B4  47                inc di
     ;>>>> [R3] The `add di,byte +0xa` at 0x0B5 advances the VRAM destination pointer by 10 bytes to skip past a field boundary (e.g., column offset or attribute gap) before copying a string.
     000000B5  83C70A            add di,byte +0xa
     000000B8  BE1500            mov si,0x15

=== SAMPLE 30/40  (address 0x0000002A) ===
     ;>>>> [R1] This line moves the ES segment register into AX to compute the difference between the program's data segment and the stack-based memory top for the INT 21h AH=4Ah resize call.
     0000001A  8CC0              mov ax,es
     ;>>>> [R1] This line subtracts the ES segment from BX (`sub bx,ax`) to compute the number of paragraphs needed for the DOS memory-resize call (INT 21h AH=4Ah), adjusting the program’s allocated memory block.
     0000001C  2BD8              sub bx,ax
     0000001E  B44A              mov ah,0x4a
     00000020  CD21              int 0x21
     ;>>>> [R3] This line sets up a DOS exit call by loading AX with 0x4C00 and then executing INT 21h to terminate the current process with return code 0.
     00000022  B83200            mov ax,0x32
     00000025  A21400            mov [0x14],al
     ;>>>> [R3] This line calls INT 10h (video BIOS) to set the video mode, likely initializing the display for the terminal emulator.
     00000028  CD10              int 0x10
     ;>>>> [R1] This line is a short jump (`jmp short 0x30`) that skips over an INT 40h call (AH=0) used to check if the kernel is ready, continuing to the main initialization loop that calls INT 40h AH=4 (get host record).
>>>> 0000002A  EB04              jmp short 0x30
     ;>>>> [R1] This line calls INT 40h with AH=0 to get a character from the serial host link (Z8530 SCC), polling until one is available, as part of the main input loop.
     0000002C  B400              mov ah,0x0
     0000002E  CD40              int 0x40
     ;>>>> [R1] This line sets AH=4 and calls INT 40h, which is the kernel’s memory allocation service that returns a segment:offset in ES:BX for the terminal’s display buffer.
     00000030  B404              mov ah,0x4
     00000032  CD40              int 0x40
     00000034  8CC0              mov ax,es
     ;>>>> [R4] The `or ax,bx` instruction at 0x36 tests whether the segment register ES (in AX) and the heap size BX are both zero, branching to a reset/retry path if so; this is part of a DOS INT 21h AH=4Ah (resize memory block) call sequence.
     00000036  0BC3              or ax,bx
     ;>>>> [R1] This is a conditional jump (jnz 0x3f) that checks if the ES:BX pointer returned by INT 40h AH=4 (memory allocation) is non-null; if so, it stores the segment and offset into variables at [0x10] and [0x12] for later use.
     00000038  7505              jnz 0x3f
     ;>>>> [R1] This line stores zero (`mov [0xe],ax`) into a memory variable, clearing the segment pointer used to track allocated memory, as part of the DOS memory-resize (INT 21h AH=4Ah) and initialization sequence.

=== SAMPLE 31/40  (address 0x00000186) ===
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
     00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
     00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c
     ;>>>> [R2] This line sets AH=0x34 and compares AL to 0x64 to determine a serial communication parameter (likely baud rate or timeout), then calls INT 40h with the computed value.
     0000017C  B434              mov ah,0x34
     0000017E  3C64              cmp al,0x64
     00000180  B002              mov al,0x2
     00000182  7206              jc 0x18a
     00000184  B000              mov al,0x0
>>>> 00000186  7402              jz 0x18a
     ;>>>> [R2] This line sets AL to 0x1 as the final branch in the same conditional chain for INT 40h AH=0x34, selecting the parameter when AL equals 0x64 (the zero flag is set from the prior comparison).
     00000188  B001              mov al,0x1
     ;>>>> [R3] This line calls INT 40h AH=34h with AL set to 0, 1, or 2 based on comparison with 0x64, implementing a system function that returns status or performs an action based on the value in AL.
     0000018A  CD40              int 0x40
     ;>>>> [R1] This is an unconditional jump (jmp 0x2c) that returns control to the main loop after calling INT 40h AH=0x34 with AL=0/1/2 to set the terminal emulation mode based on a comparison with 0x64.
     0000018C  E99DFE            jmp 0x2c
     ;>>>> [R1] This line loads a byte from DS:SI (via lodsb) to fetch the next character for printing, part of a loop that sends characters to the printer via INT 17h until a '$' terminator is found.
     0000018F  AC                lodsb
     ;>>>> [R4] The `cmp al,0x24` at 0x190 checks if the character loaded from DS:SI is '$' (the DOS string terminator), and if so jumps to 0x2C to exit the printer-output loop.
     00000190  3C24              cmp al,0x24
     ;>>>> [R2] This line is a conditional jump (JNZ) that checks if the loaded byte (AL) is not the string terminator '$' (0x24), branching to print it via INT 17h AH=0, or falling through to exit the print loop.
     00000192  7503              jnz 0x197

=== SAMPLE 32/40  (address 0x00000133) ===
     0000011E  A21400            mov [0x14],al
     00000121  B400              mov ah,0x0
     ;>>>> [R1] This is a call to INT 10h with AH=0 to set the video mode, using the value in AL that was stored from AH at [0x14] (the column mode selector).
     00000123  CD10              int 0x10
     ;>>>> [R2] Clears the word at [0x0E] to zero, resetting the host record pointer to indicate no pending data, then jumps back to the polling loop.
     00000125  C7060E000000      mov word [0xe],0x0
     ;>>>> [R2] This is a `jmp 0x30` instruction that branches to the main event loop or keyboard dispatch routine, bypassing the subsequent DOS exit code (INT 21h AH=0x4c) and the serial character decrement logic.
     0000012B  E902FF            jmp 0x30
     0000012E  B409              mov ah,0x9
     ;>>>> [R4] This line loads DX=0x40 and calls INT 21h AH=9 to print a $-terminated string at offset 0x40 in the data segment, likely an error or exit message before terminating via INT 21h AH=0x4C.
     00000130  BA4000            mov dx,0x40
     ;>>>> [R3] The `int 0x21` at 0x133 calls DOS function AH=9 (print $-terminated string at DS:DX=0x40) to display an error or status message before exiting.
>>>> 00000133  CD21              int 0x21
     ;>>>> [R1] This is a DOS exit call via INT 21h with AX=0x4C00, terminating the current process with return code 0.
     00000135  B8004C            mov ax,0x4c00
     ;>>>> [R3] This line executes INT 21h with AH=0x4C, performing a DOS program exit (terminate with return code in AL).
     00000138  CD21              int 0x21
     ;>>>> [R3] This line increments the byte at memory address [0x6F], which is likely a counter or status variable used by the terminal emulator.
     0000013A  FE066F00          inc byte [0x6f]
     0000013E  FE066F00          inc byte [0x6f]
     ;>>>> [R1] This line decrements a counter at [0x6F] that tracks the number of pending printer output bytes, used in a printer-spooling routine that sends data via INT 17h.
     00000142  FE0E6F00          dec byte [0x6f]
     ;>>>> [R1] This loads AL from memory location [0x6F] (a counter variable), then calls INT 40h with AH=0x20 to send a character to the host via the Z8530 SCC serial link.
     00000146  A06F00            mov al,[0x6f]
     00000149  B420              mov ah,0x20

=== SAMPLE 33/40  (address 0x000000CB) ===
     000000B8  BE1500            mov si,0x15
     ;>>>> [R4] This line sets up a 40-byte (0x28) block copy operation via `rep movsb` to transfer a character row or attribute data into the video buffer, likely for a 40-column display mode.
     000000BB  B92800            mov cx,0x28
     ;>>>> [R4] Executes a `rep movsb` to copy 0x28 bytes from DS:SI (0x15) to ES:DI (a character buffer offset), used to fill a display row for video output.
     000000BE  F3A4              rep movsb
     000000C0  B402              mov ah,0x2
     000000C2  BA0018            mov dx,0x1800
     000000C5  CD10              int 0x10
     ;>>>> [R3] This line checks the keyboard buffer via INT 16h AH=01h (get keyboard status) to see if a key is pending, and jumps if zero (no key) to skip processing.
     000000C7  B401              mov ah,0x1
     ;>>>> [R2] This line calls INT 16h with AH=1 to check if a key is available in the keyboard buffer (non-blocking keyboard status check), then jumps if zero (no key pressed).
     000000C9  CD16              int 0x16
>>>> 000000CB  744C              jz 0x119
     000000CD  B400              mov ah,0x0
     ;>>>> [R1] This line calls INT 16h with AH=0 to read a key from the keyboard buffer, blocking until a key is available, then tests the character for dispatch.
     000000CF  CD16              int 0x16
     000000D1  84C0              test al,al
     ;>>>> [R2] This conditional jump checks if the keyboard scan code (AL) is zero after an INT 16h AH=0 (read key) call. If AL=0 (extended key), it jumps to 0x119 to skip processing; otherwise, it proceeds to handle the character with INT 21h AH=7 (direct console input without echo).
     000000D3  7444              jz 0x119
     ;>>>> [R4] This sets AH=7 for a keyboard input call via INT 16h (keyboard BIOS), which reads a character without echo, used in the keyboard dispatch chain to process typed input.
     000000D5  B407              mov ah,0x7
     
     ; ---- 0x00d7-0x00db STRING (4 B) ----
     0000D7  3c 35 74 41                                      |<5tA|
     

=== SAMPLE 34/40  (address 0x00000060) ===
     ;>>>> [R1] This line loads DX with 0x1E, which is the offset of a data structure used in the video attribute/character painting loop that follows, where BP is set to 0x40 (starting attribute value) and the code writes characters and attributes to the dual-plane VRAM.
     00000053  BA1E00            mov dx,0x1e
     ;>>>> [R4] This instruction sets BP to 0x40 (64 decimal), which serves as the initial attribute value for the first character cell being cleared in the video memory initialization routine.
     00000056  BD4000            mov bp,0x40
     ;>>>> [R4] This instruction loads a word from the ES:BX pointer, which contains a video memory address for clearing a character cell and its attribute byte in the dual-plane VRAM.
     00000059  268B07            mov ax,[es:bx]
     ;>>>> [R2] This code reads a word from the ES:BX pointer, swaps bytes (little-endian to big-endian), stores a null byte and attribute byte into dual-plane VRAM at the computed offset, and increments BX to advance through a host data record.
     0000005C  43                inc bx
     ;>>>> [R3] This line increments BX by 2 (second `inc bx`) to skip the next word in the ES:BX memory block, advancing the pointer past a 16-bit value (likely a row offset) during screen row initialization.
     0000005D  43                inc bx
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian screen address for clearing a character cell and its attribute.
     0000005E  86C4              xchg al,ah
>>>> 00000060  8BF8              mov di,ax
     00000062  32C0              xor al,al
     ;>>>> [R1] This line stores AL (zero) into the character plane of VRAM via `stosb`, clearing a character cell as part of initializing a new display row.
     00000064  AA                stosb
     ;>>>> [R3] The `mov [es:di-0x8000],al` at 0x065 writes a null byte to the attribute plane (offset -0x8000 from the character plane) to clear the attribute for a character just stored.
     00000065  2688850080        mov [es:di-0x8000],al
     ;>>>> [R1] This line copies the base pointer (BP, starting at 0x40) into AX to store as the attribute byte in the attribute plane (ES:DI-0x8000) during a character/attribute write loop for screen painting.
     0000006A  8BC5              mov ax,bp
     0000006C  AA                stosb
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83

=== SAMPLE 35/40  (address 0x00000175) ===
     ;>>>> [R1] This line is a conditional jump (`jz 0x18f`) that branches if the printer status (from INT 17h AH=2) indicates no error, checking the `test ah,0xa9` result after XORing with 0x80 to mask the busy bit.
     00000167  7426              jz 0x18f
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
     0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
     0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
     00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.
     00000173  8CC0              mov ax,es
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
>>>> 00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
     00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c
     ;>>>> [R2] This line sets AH=0x34 and compares AL to 0x64 to determine a serial communication parameter (likely baud rate or timeout), then calls INT 40h with the computed value.
     0000017C  B434              mov ah,0x34
     0000017E  3C64              cmp al,0x64
     00000180  B002              mov al,0x2
     00000182  7206              jc 0x18a
     00000184  B000              mov al,0x0
     00000186  7402              jz 0x18a
     ;>>>> [R2] This line sets AL to 0x1 as the final branch in the same conditional chain for INT 40h AH=0x34, selecting the parameter when AL equals 0x64 (the zero flag is set from the prior comparison).

=== SAMPLE 36/40  (address 0x0000000E) ===
     ;>>>> [R1] This is the program entry point that sets DS=0x19 (the PSP segment), displays a sign-on string via INT 21h AH=9, then calculates the resident code size for possible termination-and-stay-resident setup.
     00000001  B81900            mov ax,0x19
     ;>>>> [R4] The `mov ds,ax` at 0x04 sets the data segment to 0x19 (the PSP segment) immediately after entry, as part of the standard DOS program startup sequence before calling INT 21h AH=9 to print a string.
     00000004  8ED8              mov ds,ax
     00000006  B409              mov ah,0x9
     ;>>>> [R3] The `mov dx,0x15` at 0x08 sets DS:DX to point to a $-terminated string at offset 0x15 for DOS print via INT 21h AH=9.
     00000008  BA1500            mov dx,0x15
     ;>>>> [R2] This line calls DOS INT 21h function 09h to print a $-terminated string at DS:DX, displaying a startup or status message from the terminal emulator.
     0000000B  CD21              int 0x21
     ;>>>> [R1] The line `pop es` at 0x0D loads the program’s original ES segment (saved by `push ds`), used to compute the memory block size for the INT 21h AH=4Ah (resize memory block) call that follows.
     0000000D  07                pop es
     ;>>>> [R1] The line `mov bx,sp` captures the current stack pointer, which is the first step in calculating the program’s memory footprint for the subsequent INT 21h AH=4Ah resize operation.
>>>> 0000000E  8BDC              mov bx,sp
     ;>>>> [R1] The line `add bx,byte +0xf` rounds the stack pointer up to the next paragraph boundary, preparing to compute the program’s memory size for an INT 21h AH=4Ah (resize memory block) call.
     00000010  83C30F            add bx,byte +0xf
     ;>>>> [R3] This line shifts BX right by 4 bits to convert the stack pointer adjustment into a paragraph value for the INT 21h AH=4Ah (resize memory block) call.
     00000013  C1EB04            shr bx,byte 0x4
     ;>>>> [R2] This line moves the stack segment (SS) into AX to calculate the memory block size for a DOS INT 21h AH=4Ah (resize memory block) call. It computes the segment of the last used stack location, subtracts the program’s ES segment, and passes the resulting paragraph count to resize the allocated memory.
     00000016  8CD0              mov ax,ss
     ;>>>> [R1] This line computes the segment base for a memory resize operation by adding the stack segment (SS) to BX, which is part of calculating the program's memory size for INT 21h AH=4Ah (resize memory block).
     00000018  03D8              add bx,ax
     ;>>>> [R1] This line moves the ES segment register into AX to compute the difference between the program's data segment and the stack-based memory top for the INT 21h AH=4Ah resize call.
     0000001A  8CC0              mov ax,es
     ;>>>> [R1] This line subtracts the ES segment from BX (`sub bx,ax`) to compute the number of paragraphs needed for the DOS memory-resize call (INT 21h AH=4Ah), adjusting the program’s allocated memory block.
     0000001C  2BD8              sub bx,ax

=== SAMPLE 37/40  (address 0x0000016D) ===
     ;>>>> [R4] The `mov dx,0x1` at 0x157 sets DX=1 (printer number) for an INT 17h AH=2 (print character) call, sending the byte at DS:SI (0xBB) to the printer.
     00000157  BA0100            mov dx,0x1
     0000015A  BEBB00            mov si,0xbb
     0000015D  B402              mov ah,0x2
     ;>>>> [R2] This line calls INT 17h (printer service) with AH=2 to send a character to the printer port, then checks the printer status by XORing AH with 0x80 and testing bits 0xA9.
     0000015F  CD17              int 0x17
     00000161  80F480            xor ah,0x80
     00000164  F6C4A9            test ah,0xa9
     ;>>>> [R1] This line is a conditional jump (`jz 0x18f`) that branches if the printer status (from INT 17h AH=2) indicates no error, checking the `test ah,0xa9` result after XORing with 0x80 to mask the busy bit.
     00000167  7426              jz 0x18f
     00000169  B400              mov ah,0x0
     0000016B  CD40              int 0x40
>>>> 0000016D  720A              jc 0x179
     ;>>>> [R2] This line sets AH=0x4 and calls INT 40h, which is the kernel’s custom API function to check if a host record is available (likely polling for incoming data from the synchronous serial link).
     0000016F  B404              mov ah,0x4
     ;>>>> [R1] This line calls INT 40h AH=4 (get host record) to fetch the next data block from the host, with the result tested by the subsequent `or ax,bx` to see if a valid record was returned.
     00000171  CD40              int 0x40
     ;>>>> [R3] This line moves the value of the ES segment register into AX, used to check if the ES segment is zero (indicating no memory block allocated) before continuing a loop.
     00000173  8CC0              mov ax,es
     ;>>>> [R4] Performs `or ax,bx` to test if both AX (ES segment) and BX (offset) are zero after an INT 40h AH=4 call, checking for a null pointer indicating no more data.
     00000175  0BC3              or ax,bx
     ;>>>> [R1] This line is a conditional jump that loops back to retry printer output if the printer status indicates the port is still busy (INT 17h AH=2 returns status in AH, and the code tests for bit 7 indicating busy).
     00000177  75E4              jnz 0x15d
     ;>>>> [R3] This line jumps back to address 0x2C, which is part of the main loop that calls INT 40h with AH=0 (system function) to check for host communication.
     00000179  E9B0FE            jmp 0x2c

=== SAMPLE 38/40  (address 0x00000119) ===
     
     ; ---- 0x00f4-0x0102 STRING (14 B) ----
     0000F4  37 3c 36 74 23 3c 2b 74 3d 3c 2d 74 41 3c        |7<6t#<+t=<-tA<|
     
     ; ---- 0x0102-0x0103 CODE ----
     00000102  1B                db 0x1b
     
     ; ---- 0x0103-0x0119 STRING (22 B) ----
     000103  74 29 3c 70 74 47 3c 73 74 4a 3c 62 74 6b 3c 64  |t)<ptG<stJ<btk<d|
     000113  74 67 3c 65 74 63                                |tg<etc|
     
     ; ---- 0x0119-0x019d CODE ----
>>>> 00000119  E910FF            jmp 0x2c
     ;>>>> [R3] This instruction moves AH into AL and stores it at [0x14] (the column mode flag), then calls INT 10h AH=0 (set video mode) to switch between 80 and 132 column display, before zeroing word [0x0E] and jumping to the main loop.
     0000011C  8AC4              mov al,ah
     ;>>>> [R2] This line stores the character code from AL into the byte at [0x14], which is the current cursor column or a video mode parameter, before calling INT 10h with AH=0x00 to set the video mode.
     0000011E  A21400            mov [0x14],al
     00000121  B400              mov ah,0x0
     ;>>>> [R1] This is a call to INT 10h with AH=0 to set the video mode, using the value in AL that was stored from AH at [0x14] (the column mode selector).
     00000123  CD10              int 0x10
     ;>>>> [R2] Clears the word at [0x0E] to zero, resetting the host record pointer to indicate no pending data, then jumps back to the polling loop.
     00000125  C7060E000000      mov word [0xe],0x0
     ;>>>> [R2] This is a `jmp 0x30` instruction that branches to the main event loop or keyboard dispatch routine, bypassing the subsequent DOS exit code (INT 21h AH=0x4c) and the serial character decrement logic.
     0000012B  E902FF            jmp 0x30
     0000012E  B409              mov ah,0x9

=== SAMPLE 39/40  (address 0x00000083) ===
     0000006D  45                inc bp
     ;>>>> [R1] This line loads AX with 0x20 (space character) to be used as the fill character for clearing or padding a row in the video buffer, part of a column-width-dependent screen paint loop.
     0000006E  B82000            mov ax,0x20
     00000071  B98300            mov cx,0x83
     ;>>>> [R1] This line checks a configuration byte at [0x14] to determine whether the terminal is in 80-column mode (value ≤ 0x30) or 132-column mode, setting CX to 0x4F (79) or 0x83 (131) for the column count in a VRAM fill loop.
     00000074  803E140030        cmp byte [0x14],0x30
     ;>>>> [R2] This line is a conditional jump (JA) that checks if byte [0x14] > 0x30; if so, it skips setting CX to 0x4F, thus selecting the 132-column width (CX=0x83) versus 80-column (CX=0x4F).
     00000079  7703              ja 0x7e
     0000007B  B94F00            mov cx,0x4f
     ;>>>> [R3] This line writes the attribute byte (AH, initially 0x20 = green on black) to the attribute plane (ES:DI-0x8000) for each character position, filling the row's attribute plane during screen clear.
     0000007E  2688A50080        mov [es:di-0x8000],ah
     ;>>>> [R1] This line stores AL (a space character 0x20) into the character plane of VRAM via `stosb`, filling a row of the display with spaces during screen clearing.
>>>> 00000083  AA                stosb
     ;>>>> [R1] The line `loop 0x7e` decrements CX and jumps back to fill a row of attribute bytes in VRAM (at ES:DI-0x8000) with the value in AH, implementing a character-row clear loop for the terminal display.
     00000084  E2F8              loop 0x7e
     ;>>>> [R1] This line decrements DX, the row counter in a nested loop that paints a character row in VRAM, looping back to fill the next row until all rows are done.
     00000086  4A                dec dx
     ;>>>> [R4] This is the inner loop of a screen-clearing routine that fills the character plane with spaces (0x20) and the attribute plane with the current attribute byte, decrementing a row counter until zero.
     00000087  75D0              jnz 0x59
     ;>>>> [R2] This line pops BX from the stack, restoring the pointer to the next character pair in the host record being processed for display.
     00000089  5B                pop bx
     ;>>>> [R1] The line `mov ax,[es:bx]` loads a word from the ES segment (likely a pointer or character data from a buffer), which is then byte-swapped with `xchg al,ah` to convert from little-endian to big-endian for the UT200 host protocol.
     0000008A  268B07            mov ax,[es:bx]
     ;>>>> [R2] This line swaps bytes (xchg al,ah) to convert a little-endian word from VRAM into a big-endian column/row coordinate for cursor positioning.
     0000008D  86C4              xchg al,ah

=== SAMPLE 40/40  (address 0x00000032) ===
     ;>>>> [R3] This line sets up a DOS exit call by loading AX with 0x4C00 and then executing INT 21h to terminate the current process with return code 0.
     00000022  B83200            mov ax,0x32
     00000025  A21400            mov [0x14],al
     ;>>>> [R3] This line calls INT 10h (video BIOS) to set the video mode, likely initializing the display for the terminal emulator.
     00000028  CD10              int 0x10
     ;>>>> [R1] This line is a short jump (`jmp short 0x30`) that skips over an INT 40h call (AH=0) used to check if the kernel is ready, continuing to the main initialization loop that calls INT 40h AH=4 (get host record).
     0000002A  EB04              jmp short 0x30
     ;>>>> [R1] This line calls INT 40h with AH=0 to get a character from the serial host link (Z8530 SCC), polling until one is available, as part of the main input loop.
     0000002C  B400              mov ah,0x0
     0000002E  CD40              int 0x40
     ;>>>> [R1] This line sets AH=4 and calls INT 40h, which is the kernel’s memory allocation service that returns a segment:offset in ES:BX for the terminal’s display buffer.
     00000030  B404              mov ah,0x4
>>>> 00000032  CD40              int 0x40
     00000034  8CC0              mov ax,es
     ;>>>> [R4] The `or ax,bx` instruction at 0x36 tests whether the segment register ES (in AX) and the heap size BX are both zero, branching to a reset/retry path if so; this is part of a DOS INT 21h AH=4Ah (resize memory block) call sequence.
     00000036  0BC3              or ax,bx
     ;>>>> [R1] This is a conditional jump (jnz 0x3f) that checks if the ES:BX pointer returned by INT 40h AH=4 (memory allocation) is non-null; if so, it stores the segment and offset into variables at [0x10] and [0x12] for later use.
     00000038  7505              jnz 0x3f
     ;>>>> [R1] This line stores zero (`mov [0xe],ax`) into a memory variable, clearing the segment pointer used to track allocated memory, as part of the DOS memory-resize (INT 21h AH=4Ah) and initialization sequence.
     0000003A  A30E00            mov [0xe],ax
     ;>>>> [R1] The line `jmp short 0x2c` at 0x3D loops back to `mov ah,0x0; int 0x40` to repeatedly call the kernel’s serial get-character service (INT 40h, AH=0) until a key is received.
     0000003D  EBED              jmp short 0x2c
     ;>>>> [R3] The line stores the offset of a memory block (from BX) into variable [0x10], part of the same allocation tracking as S3.
     0000003F  891E1000          mov [0x10],bx
     ;>>>> [R3] The line stores the segment of a memory block (from ES) into variable [0x12], saving it for later use in a heap/arena allocation routine.
